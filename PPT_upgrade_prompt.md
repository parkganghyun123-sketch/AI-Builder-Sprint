# Claude에게 줄 프롬프트 — 발표자료 퀄리티 업

> 아래 `---` 사이 전체를 복사해 Claude에게 주세요.
> `FairSign_발표자료.pptx` 와 `페어사인 로고` 파일을 같이 첨부하세요.

---

첨부한 `FairSign_발표자료.pptx`를 **디자인 퀄리티만** 끌어올려 주세요.
내용·문구·숫자·슬라이드 순서는 절대 바꾸지 마세요. 시각적 완성도만 다시 만듭니다.

## 만드는 방법

`pptxgenjs`로 **새로 생성**하세요. 기존 파일을 부분 수정하지 말고, 기존 파일에서 텍스트만 그대로 가져와 처음부터 다시 그리세요.

## 브랜드 토큰 (이 값만 쓸 것)

```
딥틸   #006A6A   주색 — 화면 비중 60~70%
딥틸D  #004E4E   호버·강조
스카이 #38BDF8   보조색 (강조 1곳에만)
네이비 #102A43   본문 텍스트 / 다크 배경
보조텍스트 #4A6379
틴트   #F0FAFF   카드 배경
라인   #D8EEF5   테두리
앰버   #B45309   "기준 미달" 표시 전용 — 다른 곳에 쓰지 말 것
```

폰트: 라틴·숫자 = **Inter**, 한글 = **Malgun Gothic**
(Inter에는 한글 글리프가 없습니다. pptxgenjs는 `<a:ea>`를 안 쓰므로, 생성 후 pptx 안의 모든 `<a:latin typeface="Inter"/>` 뒤에 `<a:ea typeface="Malgun Gothic"/><a:cs typeface="Malgun Gothic"/>`를 삽입하는 후처리를 반드시 하세요.)

슬라이드 크기: `pres.layout = "LAYOUT_WIDE"` (13.33 × 7.5)

## 지금 무엇이 부족한가 — 이 6가지를 고치세요

**1. 제목과 본문 사이가 텅 비어 있다**
제목 블록이 끝나는 지점과 콘텐츠 시작 사이에 1인치 넘는 죽은 공간이 반복됩니다. 제목 아래 0.5인치 안에 콘텐츠가 시작하게 하고, 남는 공간은 콘텐츠를 키우는 데 쓰세요.

**2. 아이콘이 없다 — 빈 원만 있다**
지금 원형 배지가 색만 채워진 빈 동그라미입니다. `react-icons`를 SVG로 렌더링(`ReactDOMServer.renderToStaticMarkup`) → `sharp`로 256px 이상 PNG 래스터화 → `addImage({ data: "image/png;base64," + buf.toString("base64") })`로 넣으세요. 각 슬라이드 주제에 맞는 실제 아이콘을 쓰세요 (문서, 대화, 서명, 자물쇠, 체크 등).

**3. 모든 본문 슬라이드가 흰 배경이라 단조롭다**
섹션이 바뀌는 지점(3단 구조 시작 / 기술 파트 시작)에 **풀블리드 딥틸 배경 슬라이드**를 넣지 말고 — 장수를 늘리지 않는 대신 — 슬라이드 절반을 딥틸 색 블록으로 채우는 **비대칭 레이아웃**(60/40 또는 40/60)을 3~4장에 적용하세요. 좌우 대칭 3열 카드가 연속으로 나오지 않게 하세요.

**4. 타입 대비가 약하다**
제목 34pt / 본문 13pt는 무난할 뿐입니다. 각 슬라이드의 **주인공 한 요소**를 정하고 그것만 압도적으로 키우세요. 숫자 슬라이드의 수치는 72pt 이상, 인용 슬라이드의 문구는 28pt 이상.

**5. 스크린샷 자리가 "미완성"으로 보인다**
점선 사각형 대신, **폰 목업 프레임**을 도형으로 그리세요 — 둥근 모서리(rectRadius 0.3) 세로 직사각형 + 얇은 네이비 테두리 + 안쪽에 틴트 면. 그 안에 "여기에 화면 캡처" 안내를 아주 작게 넣으세요. 목업 프레임이 있으면 비어 있어도 의도된 디자인으로 보입니다.

**6. 카드가 전부 똑같이 생겼다**
지금 모든 카드가 동일한 라운드 사각형 + 같은 그림자입니다. 카드 종류를 3가지로 나누세요:
- 강조 카드 — 딥틸 배경 + 흰 글씨
- 기본 카드 — 틴트 배경 + 테두리 없음 + 옅은 그림자
- 목록 행 — 배경 없이 아이콘 + 텍스트만 (구분선도 쓰지 말 것)

## 절대 하지 말 것

- 제목 아래 밑줄·언더라인
- 슬라이드 가장자리나 카드 한쪽에 색 띠(accent stripe/bar)
- 크림·베이지 배경
- 본문 텍스트 가운데 정렬 (제목과 인용문만 가운데)
- 모든 슬라이드에 같은 레이아웃 반복
- 여백 없이 꽉 채우기 — 상하좌우 최소 0.6인치

## 9번 슬라이드는 특히 중요합니다 — 이 서사를 살려 주세요

9번은 단순한 기능 목록이 아니라 **아키텍처 증거**입니다. 세 가지가 반드시 읽혀야 합니다.

1. **하나의 엔진, 여러 보호 대상** — 대상마다 기능을 따로 만들지 않았습니다. 같은 판정 엔진에 규칙만 추가하는 구조라 확장됩니다. (연소자 · 임신 중 · 산후 1년 이내 · 장애 · 주 15시간 미만, 총 14개 항목)
2. **판정과 안내를 구분한다** — 판정 5개 / 안내 9개. 확정할 수 있는 것만 판정하고 나머지는 안내만 합니다.
3. **장애는 자동 판정하지 않는다** — 최저임금 감액 예외 제도가 있어 자동 판정하면 오판이 납니다. 그래서 그 지점에서 판정을 멈추고 확인을 유도합니다.

3번 문장이 이 슬라이드에서 가장 눈에 띄어야 합니다. "장애인은 어떻게 처리하냐"는 날카로운 질문에 미리 답해두는 카드입니다.

## ⚠️ 이 표현은 절대 쓰지 마세요 (사실과 다릅니다)

- ❌ "민감정보를 서버로 보내지 않습니다"
- ❌ "브라우저에서만 판정합니다"

임신·장애 여부는 **판정을 위해 서버로 전송됩니다.** 저장만 하지 않습니다.
무대에서 "안 보낸다"고 말하면 개발자 도구 네트워크 탭 한 번에 무너집니다.

정확한 표현:
- ✅ **"판정에 쓰고 저장하지 않습니다"**
- ✅ **"자기신고이며 증빙을 요구하지 않습니다"**
- ✅ **"서명이 끝나면 브라우저에서도 지웁니다"**

## 반드시 지킬 것 (내용)

- 숫자 `19.5%` `23.5%` `40.4%` `9,500원` `10,320원` `820원` `588` `14개` `5개` `9개` 그대로
  (권리 항목은 **14개**입니다. 13개가 아닙니다.)
- 법 조항 `제17조` `제5조` `제66·69·70조` `제74조` `제71조` `제6조` `제18조` 그대로
- 발표자 노트(`addNotes`) 전부 유지
- "발송을 체결로 말하지 않습니다", "판정에 LLM을 한 줄도 쓰지 않습니다" — 이 두 문장은 핵심 주장이니 오히려 더 눈에 띄게

## 검수 (필수)

1. `python scripts/office/validate.py 결과물.pptx` — 통과할 때까지
2. PDF로 변환 → 이미지로 렌더 → **14장 전부 눈으로 확인**
3. 특히 볼 것: 텍스트가 상자 밖으로 넘치는지, 요소가 겹치는지, 좌우 여백이 0.6인치 미만인지, 로고가 다른 요소와 닿는지
4. 문제를 찾으면 생성 스크립트를 고쳐서 다시 만드세요. pptx를 직접 손대지 마세요.

첨부한 로고는 배경이 흰색일 수 있습니다. 다크 슬라이드에 얹기 전에 흰 배경을 투명 처리하세요.

---

## 프롬프트 끝

### 참고 — 이 프롬프트가 안 통할 때

Claude가 "기존 파일을 수정하겠다"고 하면 다시 지시하세요:
**"부분 수정하지 말고 pptxgenjs로 처음부터 새로 생성하세요."**

부분 수정은 기존 좌표를 그대로 물려받아서 레이아웃이 그대로 남습니다.
