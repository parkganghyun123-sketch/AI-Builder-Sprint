# 남은 작업 전체 — 2026-07-30 밤 기준

> 마감: **2026-08-03(월) 18:00** · 실질 작업일 **3.5일** (7/31 · 8/1 · 8/2 · 8/3 오전)
> 담당: **A** AI 파이프라인 · **B** 검증 엔진 · **C** 백엔드·배포·팀장 · **D** 프론트엔드
> 근거: [분석보고서_2026-07-30.md](분석보고서_2026-07-30.md)

---

## 0. 오늘 끝난 것 (재작업 불필요)

| 항목 | 상태 |
|---|---|
| 웹 UI에서 서명 발송 | ✅ 실측 완주 |
| Vercel 배포 + 로그인 없이 접근 | ✅ |
| Railway CORS | ✅ |
| 업로드 크기(10MB)·MIME 제한 | ✅ |
| 이메일·이름 형식 검증 | ✅ |
| 웹훅 이력 기록 결함 | ✅ |
| 웹훅 토큰 검증 | ✅ |
| anchor 오프셋 단일 출처화 | ✅ |
| **계약 이력 Supabase 영속 저장** | ✅ `/health` store=postgres |
| 테스트 240 · ruff 0 · tsc 0 | ✅ |

오늘 발견해 고친 잠재 버그 3건: `blocking_fields` 500 오류 · `datetime.UTC` 3.11 의존 · **`greenlet` 누락**

---

## 1. 역할 선택(사업주/근로자) 판단

### 결론: **넣는다. 단 회원가입·로그인은 넣지 않는다.**

**왜 넣는가**

README의 문제 정의가 이렇습니다.

> 미작성 이유 1위: **"작성해야 하는지 몰라서" (40.4%)**
> "나쁜 마음이 아니라 무지가 원인이라는 뜻이고, 이건 **사장님 쪽도 마찬가지**입니다.
> 그래서 우리는 고발 도구를 만들지 않습니다."

**지금은 이 문장이 말뿐입니다.** 코드에 사업주를 돕는 기능이 없습니다. 역할 선택과 사업주 경로가 들어가면 이 주장이 **실제 기능으로 증명**됩니다.

배점상 걸리는 곳:
- 예선 창의성 "핵심 기능 참신성" 10점 — 양측을 돕는 구조
- 특별상 **"계약 생애주기 커버리지" 12점** — 사업주 작성 → 근로자 확인 → 양측 서명으로 생애주기가 닫힘
- 특별상 **"인간다움" 15점** — "계약·서명 경험을 더 사람답게"
- 실용성 "실제 활용·확산 가능성" — B2B 확장 가능성

**왜 로그인은 넣지 않는가**

1. **로그인은 12~17시간을 끌고 옵니다.** 카카오 로그인 5~7h + 전 API 인가 검증 2~3h + 개인정보 정책 재작성 1~2h. 여기에 사업주 경로까지 붙이면 25h+가 됩니다. 제출물이 아직 0%인 상황에서 감당할 수 없습니다.
2. **인가 검증 없는 로그인은 취약점입니다.** 현재 `/contracts/{id}/status`에 소유자 검증이 없습니다. 지금은 "로그인 없는 설계"라 정직한 한계지만, 로그인을 만들어놓고 남의 `document_id`로 남의 계약서를 볼 수 있으면 그건 한계가 아니라 결함입니다. 심사에서 훨씬 나쁘게 받아들여집니다.
3. **회원가입은 이 제품의 벽입니다.** 열여섯 살이 첫 계약서를 확인하려고 회원가입부터 해야 한다면 그 벽을 넘지 못합니다. 이건 발표에서 **의도된 설계**로 말할 수 있습니다.

**대신 이렇게 한다**

회원가입이 아니라 **첫 화면에서 고른다.** 세션에만 저장하고 서버에 계정을 만들지 않습니다.

```
┌─────────────────────────────────────┐
│  누구신가요?                          │
│                                     │
│  ▸ 알바를 시작해요 (근로자)            │
│    계약서를 확인하고 싶어요             │
│                                     │
│  ▸ 사람을 고용해요 (사업주)            │
│    계약서를 만들고 싶어요               │
└─────────────────────────────────────┘
```

### 사업주 경로는 왜 싼가

**경로 B(근로자 직접 입력)와 같은 화면·같은 API를 재사용합니다.** 발신자만 뒤집으면 됩니다.

| | 경로 B (근로자) | 사업주 경로 |
|---|---|---|
| 입력 폼 | `web/app/review/?path=B` | **같은 화면 재사용** |
| 판정 | `/contracts/validate` | **그대로** |
| 진행 차단 | `/contracts/validation-state` | **그대로** |
| PDF | `/contracts/preview` | **그대로** |
| 발송 | `/contracts/analyze-sign` | **그대로** |
| 서명 순서 | 근로자 → 사업주 | **사업주 → 근로자** ← 여기만 다름 |
| 문서 제목 | 근로조건 확인 요청서 | 근로계약서 |
| 출처 문구 | "근로자가 구두로 안내받은 내용" | (없음) |

즉 **백엔드 2~3시간, 프론트 3~4시간**입니다. 새 사용자군을 추가하는 게 아니라 기존 흐름의 방향을 하나 더 만드는 것입니다.

### 발표에서 이렇게 말한다

> 회원가입을 만들지 않았습니다. 열여섯 살이 첫 계약서를 확인하려고 가입부터 해야 한다면 그 벽을 넘지 못합니다. 대신 시작할 때 역할만 고릅니다.
>
> 사업주 경로를 넣은 이유는, 미작성 1위 이유가 "몰라서"이기 때문입니다. 나쁜 마음이 아니라 무지가 원인이고 그건 양쪽 다 그렇습니다. 그래서 고발 도구가 아니라 양쪽이 같은 기준을 보게 만들었습니다.

---

## 2. P0 — 이걸 못 하면 채점조차 안 된다

OT 적격성 심사(Pass/Fail): *"미충족 항목 발견 시 채점 없이 심사 제외"*

### P0-1 · main 브랜치 동기화 【C】 5분
```bash
cd ~/AI-Builder-Sprint
git checkout main && git merge --ff-only origin/develop && git push origin main
git checkout develop
```
- **왜**: `origin/main`이 오늘 작업 전체보다 뒤처져 있음. 심사자가 기본 브랜치를 봄. Vercel도 main을 봄
- **완료 조건**: `origin/main` == `origin/develop`

### P0-2 · 발표 자료 【C 총괄, 전원 기여】 4h
OT 요구 항목: 문제 정의 · 사용자 · 핵심 기능 · AI 활용 방식 · 기대 효과

권장 구성 (본선 7분 발표와 같은 순서):
1. 문제 — 벽 3개(모른다/말 못 한다/증거 없다) + 19.5% · 경상권 23.5% · "몰라서" 40.4%
2. 해결 — ① 알려준다 ② 말하게 한다 ③ 남긴다
3. **차별점 — 판정에 LLM을 쓰지 않는다** (`validation/rules.py` 754줄, 네트워크 import 0건)
4. AI 활용 — Document Parse + Information Extract + 코드 방어층
5. 기대 효과 + 확장 방향(사업주 경로 → B2B)
- **완료 조건**: 파일 존재, 팀 외부인이 읽고 이해함

### P0-3 · 데모 영상 + 백업 2본 【전원】 3h
- 가상 인물로 실제 서명 완주 녹화 (사진 업로드 → 확인 → 판정 → **문구 복사** → 요청서 → 서명 → 완료)
- **백업 2본을 로컬에 보관** — 발표 당일 네트워크 장애 대비
- **선행**: P1-1(문구 UI) 완료 후 촬영. 문구가 없으면 서사 ②가 영상에 안 나옴
- **완료 조건**: 재생되는 파일 3개(본편 1 + 백업 2)

### P0-4 · AI 활용 기술서 【A】 2h
OT 요구: 모델 · API 사용 위치 · 프롬프트/설정 · 테스트·검증 산출물

| 항목 | 근거 위치 |
|---|---|
| 모델 | Upstage `document-parse`, `information-extract` |
| API 호출 위치 | `app/ai/document_parse.py:31`, `app/ai/extract.py:452` |
| 추출 스키마 | `extract.py:88 build_extraction_schema()` |
| 신뢰도 임계값 | `extract.py:249 CONFIDENCE_THRESHOLD = 0.90` |
| 코드 방어층 | `:313 apply_sanity_check`, `:200 _looks_like_form_label`, `:176 _CHECKMARK` |
| 근거 문장 매칭 | `:411 _find_source_text` |
| 검증 산출물 | `app/evaluation/results.json` (230/230) + `spikes/fixtures/*_runs.json` |
| **AI를 쓰지 않은 곳** | `app/validation/` — 판정·계산 전부. **반드시 명시** |

- **완료 조건**: 위 표의 모든 경로가 실제로 존재하고 기술서에 담김

### P0-5 · 스크린샷 + 테스트 안내 【D】 1h
- 8화면 스크린샷
- 테스트 계정: 로그인이 없으므로 "계정 불필요. 가상 계약서 사진을 첨부해 주세요"로 안내
- 가상 계약서 샘플 이미지 1장 함께 제공 (`app/evaluation/contracts/contract_01.png` 사용 가능)

---

## 3. P1 — 점수에 직접 걸리는 것

### P1-1 · 말 꺼내기 문구 UI + 결과 화면 두 갈래 【D】 2h
**투입 대비 점수 효과가 가장 큽니다.**

백엔드는 완성·검증됐습니다(`numbers_verified: true` 실측). `web/`에서 호출이 **0건**입니다.

1. `lib/api.ts`에 `buildOwnerMessage(terms)` → `POST /contracts/message`
2. `app/result/page.tsx`에 문구 카드 + **클립보드 복사 버튼**
3. `message`가 `null`이면 표시 안 함 (문제 없는 계약)
4. **`numbers_verified: false`면 표시하지 말 것** — 숫자 대조 실패 신호
5. 결과 화면을 두 갈래로:
   - "계약서를 받으셨나요?" → **[사장님께 말해보기]** ← 경로 A 기본
   - "계약서를 못 받으셨나요?" → [근로조건 확인 요청서 보내기] ← 경로 B 기본

**왜 두 갈래인가**: 현재 `/result → /contract → /sign`이 일방통행이라, 계약서를 이미 받은 사람도 "새 계약서를 만들어 사장님에게 보내는" 길로 떠밀립니다. 힘의 관계상 현실적이지 않고, Q&A에서 찔릴 지점입니다.

- **관련 배점**: 창의성 참신성 10점 · 특별상 인간다움 15점 · 주제 부합성
- **완료 조건**: 시급 9,500원 검증 → 문구 표시 → 복사 → 메모장 붙여넣기 성공

### P1-2 · 역할 선택 + 사업주 경로 (백엔드) 【C】 2~3h
1. `schemas.py` `EntryPath`에 `EMPLOYER = "EMPLOYER"` 추가
2. `modusign.request_signature`에 서명 순서 파라미터 추가 (`employer_first: bool = False`)
   - 사업주 경로: 사업주 `signingOrder=1`, 근로자 `2`
   - **⚠️ anchor 텍스트와 필드 매핑은 그대로 유지할 것.** 순서만 바꾼다
3. `contracts.py` 제목·출처 문구 분기
   - `EMPLOYER` → 제목 "근로계약서", 출처 문구 없음
   - `MANUAL` → 기존 `MANUAL_ENTRY_NOTICE` 유지
4. 통합 테스트 2건 추가 (`tests/test_api_gates.py`): 사업주 경로 200, 서명 순서 확인
- **완료 조건**: `pytest` 통과 + 사업주 경로로 발송 시 사업주가 먼저 서명

### P1-3 · 역할 선택 + 사업주 경로 (프론트) 【D】 3~4h
1. 홈(`app/page.tsx`)에 역할 선택 카드 2개
2. 근로자 → 기존 흐름 (`/upload` 또는 `/review?path=B`)
3. 사업주 → `/review?path=EMPLOYER` — **기존 입력 폼 재사용**, 문구만 사업주 시점으로
4. `lib/session.ts`에 `role` 필드 추가 (세션 저장, 서버 전송 없음)
5. 사업주 화면 문구 예: "근로자에게 줄 계약서를 만듭니다. 법정 기준을 함께 확인해 드려요."
- **완료 조건**: 홈에서 사업주 선택 → 조건 입력 → 판정 → PDF → 발송 완주

### P1-4 · 손글씨 반복 측정 산출물 【A】 1h
`spikes/check_extract.py`는 완성돼 있으나 산출물이 저장소에 없습니다.

```bash
cd ~/AI-Builder-Sprint
set -a; source .env; set +a
python3 spikes/check_extract.py spikes/fixtures --runs 3
```

- 합성 벤치마크 100%보다 **"실사진 3회 모두 정확, 흔들림 0"이 훨씬 강한 근거**
- 정확도가 낮게 나와도 **숨기지 말 것.** 사용자 확인 단계가 있어 OCR 정확도가 급소가 아니라는 게 이 제품의 논리(README:97-101). 낮은 수치도 그 논리의 근거가 됩니다
- **선행**: P1-7(개인정보 fixture 방침) 결정
- **완료 조건**: `*_runs.json` 커밋 + 요약 3줄

### P1-5 · 이름 불일치 상세 표시 【D】 0.5h
백엔드가 `conflicts: [{on_contract, typed}]`를 보내는데 화면은 `message`·`fields`만 렌더합니다.

```
┌─────────────────────────────────────┐
│ ↩️ 계약서 이름과 입력한 이름이 달라요   │
│                                     │
│  계약서에서 읽음:  김가상             │
│  입력하신 이름:    김가상아님          │
│                                     │
│  어느 쪽이 맞나요?                    │
└─────────────────────────────────────┘
```

- **왜**: 백엔드가 일부러 고르지 않고 되돌려주는 설계인데(`contracts.py:319-355`), 화면이 두 값을 안 보여주면 사용자가 고를 수 없습니다. 설계 의도가 화면에서 끊깁니다
- 스키마가 `.passthrough()`라 데이터는 이미 도착합니다. 렌더만 추가

### P1-6 · 실발송 anchor 위치 검증 【C】 1h
오늘 `anchors.py` 실측 보정값이 프로덕션에 처음 반영됐습니다. **아직 실발송으로 확인하지 않았습니다.**

```bash
python3 spikes/modusign_spike.py send   # TEST_WORKER_EMAIL / TEST_OWNER_EMAIL 확인 후
```

- **왜**: 서명란이 엉뚱한 곳에 놓이면 특별상 핵심 시연이 망가집니다
- **완료 조건**: 서명란이 `(근로자 서명)`·`(사업주 서명)` 위에 정확히 놓인 스크린샷

### P1-7 · 개인정보 fixture 처리 결정 【팀장 판단 필요】
`spikes/fixtures/handwritten_01.png`(실제 손글씨 계약서) + `handwritten_01.json`(실명)이 **기본 브랜치에** 있습니다.

`AGENTS.md` 위반: *"테스트와 데모에는 가상 계약서와 가상 인물만 사용합니다"*, *"실제 계약서, 개인정보를 커밋하지 않습니다"*

선택: (a) 삭제 (b) 가상 인물로 재촬영 (c) 이름만 마스킹
- git 히스토리 잔존 수용 여부도 결정 필요
- **왜 중요**: AI 스크리너가 AGENTS.md와 저장소를 대조하면 자기 규칙 위반으로 잡힙니다

### P1-8 · 웹훅 실동작 로그 확보 【C】 0.5h
서명을 하나 더 하고 Railway → Deploy Logs 확인:
```
모두싸인 webhook: event=document_signed document=xxxxx
문서 xxxxx 동기화: ON_GOING (1/2 서명)
```
- 오늘 `remember_document()` 고치기 전에는 `이력에 없는 문서`에서 끊겼습니다
- 특별상 **"연동의 견고성 8점"** 증빙. 스크린샷 필수

---

## 4. P2 — 시간이 남으면

### P2-1 · 계약서 보관함 【D】 3~4h
이제 서버(Supabase)에 이력이 남으므로 **3층 구조**로 설명할 수 있습니다.

```
브라우저 localStorage   document_id 목록만        ← "내 문서" 판별
Supabase                진행 상태 영속 이력        ← 재배포에도 살아남음
모두싸인                 계약서 원본 + 법적 효력    ← 우리가 없어져도 남음
```

1. 발송 성공 시 `document_id` + 생성일시를 `localStorage`에 append
2. 보관함 열면 각 ID로 `GET /contracts/{id}/status` 조회
3. **저장 금지**: 이름·시급·사업체명·다운로드 링크(유효 10분)
4. 한계를 화면에 한 줄 명시 — "이 브라우저에서 만든 문서만 보입니다"

- 백엔드 변경 0. `archive/page.tsx`의 껍데기(ScreenShell step 6, LegalDisclaimer) 유지
- **관련 배점**: 특별상 계약 생애주기 커버리지 12점

### P2-2 · 422 상세 표시 【D】 0.5h
`INVALID_CONTRACT_VALUES` 응답의 `blocking_fields`·`issues`를 화면에 표시. 현재는 "입력한 값을 확인해 주세요"만 뜹니다.

### P2-3 · 문서 최종 정리 【C】 1h
- README 기능표를 최종 상태로 갱신 (오늘 백엔드/화면 분리 표기로 바꿔둠)
- `docs/`의 "심사위원" 언급 중립화 검토 (위험 낮음, 팀장 판단)
- 제출 체크리스트(`README.md:583`) 체크

---

## 5. 잘라낸 것 — 명시적으로 포기 선언할 것

> ⚠️ **애매하게 남겨두는 것이 가장 비쌉니다.** 팀원이 "혹시 해야 하나" 고민하며 시간을 씁니다.
> 8/1까지 팀에 공지하세요.

| 기능 | 판단 | 근거 |
|---|---|---|
| **카카오 로그인 / 회원가입** | 포기 (본선까지 미룸) | 12~17h + 인가 검증 필수. 배점 직접 없음. "로그인 없음"을 설계로 설명 |
| **계약 비서 챗봇** | 포기 | README:524 *"숫자 검증 없는 챗봇은 배포하지 않습니다"*. 안전장치까지 3.5일 내 불가. **포기 근거를 발표 강점으로 전환** |
| **Solar LLM 문구 생성** | 포기 | 템플릿 버전이 이미 `numbers_verified: true`로 동작. 얹으면 리스크만 |
| 지역 협력기관 접촉 | 팀장 판단 | 본선 가점 +5. 3.5일 내 성사 가능성 판단 필요 |
| 사업주 전용 대시보드 | 포기 | 사업주 경로는 발송까지만. 관리 화면은 범위 밖 |

---

## 6. 일정

### 7/31 (금) — 기능 완성

| | D | C | A | B |
|---|---|---|---|---|
| 오전 | **P1-1** 문구 UI + 두 갈래 | **P0-1** main 동기화 → **P1-2** 사업주 경로 백엔드 | **P1-4** 손글씨 측정 | **P1-2** 테스트 지원 |
| 오후 | **P1-3** 사업주 경로 프론트 | **P1-6** 실발송 anchor 검증 | **P0-4** AI 기술서 초안 | 판정 로직 최종 점검 |
| 저녁 | **P0-4** 빌드·배포 확인 | **P1-8** 웹훅 로그 확보 | — | — |

**성공 기준**: 배포 링크에서 **①알려준다 ②말하게한다 ③남긴다 + 역할 2종**이 전부 동작
**병렬 가능**: P1-1(D) ∥ P1-2(C) ∥ P1-4(A) — 파일 충돌 없음
**팀장 결정**: P1-7(개인정보 fixture) — 오전 중

### 8/1 (토) — 제출물

| | 전원 |
|---|---|
| 오전 | **P0-2** 발표 자료 (C 총괄) |
| 오후 | **P0-3** 데모 영상 촬영 + 백업 2본 |
| 저녁 | **P0-4** AI 기술서 완성 · **P0-5** 스크린샷 |

**성공 기준**: 제출물 4종이 실재하고 팀 외부인이 링크만으로 이해함
**이 시점에 챗봇·로그인·DB 확장 공식 포기 선언**

### 8/2 (일) — 버퍼 + 선택 작업

| | |
|---|---|
| 오전 | P2 항목 (보관함 / 422 상세 / 문서 정리) |
| 오후 | **리허설 3회** — 시크릿 창에서 배포 링크 완주, 발표 타이밍 측정 |
| 저녁 | 발견된 문제 수정. **새 기능 착수 금지** |

### 8/3 (월) — 제출

| | |
|---|---|
| 오전 | 최종 점검: `pytest` · `ruff` · `tsc` · `npm run build` · `/health` 4개 신호 · 배포 완주 3회 |
| **13:00** | **제출** — 마감 5시간 전. Railway·Vercel 장애 대비 버퍼 |
| 이후 | README 최종 검토(프롬프트 인젝션 의심 문구), 본선 발표 준비 착수 |

---

## 7. 시간 합계

| 우선순위 | 작업 | 시간 |
|---|---|---|
| P0 | 제출물 4종 + main 동기화 | **10h** |
| P1 | 문구 UI · 사업주 경로 · 측정 · 검증 | **11h** |
| P2 | 보관함 · 상세 표시 · 문서 | **5h** |
| | **합계** | **26h** |

4명 × 3.5일이면 여유가 있습니다. **단 로그인(12~17h)을 넣으면 P0가 밀립니다.** 그게 이번 판단의 전부입니다.

---

## 8. 제출 전 최종 체크리스트

**실행 환경**
- [ ] `pytest` 전부 통과
- [ ] `ruff check backend/` 0 errors
- [ ] `npx tsc --noEmit` exit 0
- [ ] `npm run build` exit 0
- [ ] `<railway>/health` → `modusign:true` · `upstage:true` · `store:"postgres"` · `webhook_token:true` · `cors_origins`에 Vercel 도메인
- [ ] `<railway>/health/pdf` 브라우저에서 한글 정상 (□□□ 아님)
- [ ] Upstage · 모두싸인 크레딧 잔량 확인
- [ ] `git status` 클린, `.env` 미추적
- [ ] `origin/main` == `origin/develop`

**제출물**
- [ ] 발표 자료
- [ ] 코드 저장소 (README · 실행 방법 · 커밋 내역 · **AI 지침 파일**)
- [ ] 배포 링크 (Vercel + Railway)
- [ ] 데모 영상 + 백업 2본
- [ ] 스크린샷 8종 + 테스트 안내
- [ ] AI 활용 기술서

**실패 대비**
- [ ] 완주 녹화 2본 로컬 보관
- [ ] Upstage 장애 시 **경로 B로 전환** (이미 구현됨 — 강점으로 설명 가능)
- [ ] 모두싸인 장애 시 체결 완료 문서 스크린샷 준비
- [ ] Railway 콜드스타트 대비 발표 10분 전 `/health` 워밍업
- [ ] 로컬 실행본(백엔드 + `npm run dev`) 노트북에 준비

**발표에서 강조할 것 (근거 있는 것만)**
1. **판정에 LLM을 쓰지 않는다** — `validation/rules.py` 754줄, 네트워크·LLM import 0건, 테스트가 고정
2. **AI가 자신 있게 틀리는 경우까지 방어했다** — 실측 `박강현→박강헌`(HIGH)에서 `ALWAYS_CONFIRM` 관문 도출
3. **코드가 판단할 수 없는 건 사용자에게 되돌린다** — 이름 불일치를 조용히 덮어쓰지 않음
4. **확인 못 한 법령은 코드에 넣지 않았다** — 대타 근무 개근, 2026-12-10 시행 예정 휴게시간 개정
5. **평가 방법의 한계를 스스로 문서화했다** — 합성 벤치마크임을 `evaluate.py` docstring에 명시
6. **양쪽을 돕는다** — 미작성 1위 이유가 "몰라서"이므로 고발 도구를 만들지 않음. 사업주 경로가 그 증거
