# FairSign 프로젝트 종합 분석 보고서

**작성**: 2026-07-30 · **기준 브랜치/커밋**: `develop` @ `e43bc29`
**제출 마감까지**: 2026-08-03 18:00 (약 4일)
**분석 방식**: 문서 주장 대조 + 실제 코드 판독 + 실행 검증(테스트·API probe·PDF 텍스트 추출)

---

# 1. 경영진 요약

1. 백엔드는 **실제로 끝까지 동작한다.** 사진→추출→판정→PDF→서명요청 전 구간을 직접 호출해 200 응답과 정상 PDF까지 확인했다(§9).
2. 그런데 **웹 화면에서는 전자서명 발송이 100% 실패한다.** 프론트가 `confirmed_fields`를 보내지 않아 `/contracts/analyze-sign`이 항상 HTTP 409로 막힌다. 재현 확인했다(§5-8, §10-C1).
3. **배포 백엔드의 CORS 설정이 비어 있다.** 실측한 `/health` 응답의 `cors_origins`가 localhost 뿐이라, 프론트를 Vercel에 올리는 순간 브라우저가 모든 요청을 차단한다(§10-C2).
4. 프로젝트 최대 차별점인 **"말 꺼내기 문구"(서사 ②)가 화면에 아예 없다.** 백엔드는 완성·검증됐는데 UI에서 호출하지 않아 심사자에게 보이지 않는다(§10-H1).
5. 가장 잘된 점: **"LLM은 판정하지 않는다"는 설계 원칙이 코드에서 실제로 지켜졌다.** `backend/app/validation/`에 LLM·네트워크 호출이 단 한 줄도 없고, 208개 테스트가 이를 고정한다.
6. 문서·에이전트 지침 체계(AGENTS.md + agents/ 4종, 총 4,967줄)는 예선 "에이전트 설정·지침 체계성" 항목에서 강점이다.
7. 가장 큰 위험: **적격성 심사(Pass/Fail)의 "제출물 완비"** — 배포 링크·데모 영상이 아직 없다. 미충족 시 채점 없이 심사 제외다.
8. 예상 완성도: 기능 **72%** · 배포 준비도 **45%** · 대회 준비도 **40%** (§보고서 표현 방식 참조)
9. 예상 예선 점수: **80점 만점에 52~62점** (분석상 추정치, 공식 점수 아님). P0 3건만 고치면 **62~70점**으로 올라갈 여지가 있다.
10. **지금 당장 할 일**: D에게 `confirmed_fields` 전송 + 409 복구 UI, C(팀장)에게 Railway `CORS_ORIGINS` 설정 + 프론트 배포. 이 두 건이 오늘 안 끝나면 데모가 성립하지 않는다.

---

# 2. 프로젝트 개요

**목적**: 처음 아르바이트를 시작하는 근로자가 근로계약서를 이해·확인·바로잡고 서명하도록 돕는다. 확정적 법률 자문은 제공하지 않는다.

**서사 3단계** (README.md:44-46)
```
① 알려준다        → ② 말하게 한다        → ③ 남긴다
법정 기준 판정        사장님께 보낼 문구       양측이 계약서 보유
```

**핵심 차별점**: 계약서 사진 → AI 추출 → **사람이 확인/수정** → 그 확정값을 **순수 코드(비-LLM)** 가 법정 기준과 대조해 판정. LLM은 읽기·추출에만 관여한다.

**주요 사용자**: 만 15~18세 첫 알바 근로자 (근거: 청소년 계약서 미작성 19.5%, 경상권 23.5% 전국 최고 — README.md:110-112)

**기술 스택** (실제 코드 확인)

| 영역 | 스택 | 위치 | 검증 |
|---|---|---|---|
| 백엔드 | Python 3.10 / FastAPI 0.115.6 | `backend/` | ✅ 실행 확인 |
| 스키마 | Pydantic 2.10.4 | `backend/app/schemas.py` (243줄) | ✅ |
| 판정 엔진 | 순수 Python | `backend/app/validation/rules.py` (754줄) | ✅ LLM/네트워크 import 0건 |
| AI | Upstage Document Parse + Information Extract (httpx) | `backend/app/ai/` | ✅ 배포 환경 `upstage:true` |
| PDF | WeasyPrint 63.1 | `backend/app/pdf/` | ✅ 273KB PDF 생성, anchor 텍스트 확인 |
| 전자서명 | 모두싸인 REST (anchor 배치) | `backend/app/signing/` | ⚠️ 오프셋 상수 이중화 (§10-H2) |
| 프론트 | Next.js 15.5.21 / React 19 / TS 5.5 / Tailwind 3.4 | `web/` (3,168줄, 화면 8종) | ⚠️ 핵심 API 3개 미연결 |
| DB | PostgreSQL 예정 | — | ❌ 미연결 (메모리 dict) |
| 배포 | 백엔드 Railway ✅ / 프론트 Vercel ❌ | — | §9 |

**Solar LLM은 사용하지 않는다.** 전체 코드 grep 결과 `spikes/bridge_spike.py:18`의 미구현 주석(`call_solar` — A가 구현) 한 줄 외에 호출부가 없다. 인수인계 문서의 "Solar LLM" 기재는 실제 코드와 불일치다(§6).

---

# 3. 분석한 자료와 분석 범위

## 확인한 자료

**저장소**: 로컬 `~/AI-Builder-Sprint`, 브랜치 `develop` @ `e43bc29` (2026-07-29 22:32 KST)
- 로컬 브랜치: `develop`(HEAD), `feat/ai-extract`, `feat/sign`, `main`
- 원격: `origin/develop`, `origin/feat/ai-extract`, `origin/feat/sign`, `origin/feat/validation`, `origin/feat/web`, `origin/feat/web-backend-integration`, `origin/main`
- 총 커밋 56건, 기간 2026-07-26 ~ 07-29

**문서** (총 4,967줄): `README.md`(681) · `DESIGN.md`(598) · `KB.md`(358) · `AGENTS.md`(113) · `CLAUDE.md`(6) · `agents/` 4종(223) · `docs/` 11종(2,988)

**OT 자료**: `AI_Builder_Sprint_OT.pdf` 16페이지 전체 판독 — 적격성 심사표, 예선 80점 배점표, 에이전트 평가 운영, 본선 100+10점, 모두싸인 특별상 100+10점

**설정**: `.env`(미추적, 키만 확인) · `.env.example` · `web/.env.example` · `web/.env.local` · `.gitignore` · `backend/pyproject.toml` · `backend/requirements.txt` · `backend/Dockerfile` · `web/package.json` · `web/tsconfig.json` · `web/eslint.config.mjs`

**테스트**: 208개 (7파일) — `app/ai/tests/`, `app/bridge/tests/`, `app/review/tests/`, `app/validation/tests/`, `tests/test_document_safety.py`

**평가 산출물**: `backend/app/evaluation/results.json` (합성 계약서 10종 × 23필드 = 230/230)

## 실행한 검증

| # | 명령 / 방법 | 결과 |
|---|---|---|
| V1 | `python3 -m pytest -q` (backend) | ✅ **208 passed in 0.66s** |
| V2 | `python3 -m ruff check backend/` | ⚠️ **6 errors** (§8) |
| V3 | `npx tsc --noEmit` (web) | ✅ exit 0 |
| V4 | `npx eslint .` (web) | ✅ exit 0 |
| V5 | `npx next build` | ❌ **확인 불가** — 샌드박스 세션 타임아웃(45s)으로 완주 못 함 |
| V6 | FastAPI TestClient로 `/health`, `/contracts/validate`, `/validation-state`, `/message`, `/review-items`, `/analyze-sign`, `/preview` 호출 | ✅ 모두 응답, §5 표 참조 |
| V7 | 프론트와 **동일한 body**로 `/contracts/analyze-sign` 호출 | ❌ **409 UNCONFIRMED_FIELDS** 재현 |
| V8 | `confirmed_fields` 5개 포함해 재호출 (모두싸인은 fake로 대체) | ✅ **200**, PDF 277,498 bytes, 모두싸인 호출부 도달 |
| V9 | WeasyPrint 실제 렌더 후 `pdfminer`로 텍스트 추출 | ✅ `(근로자 서명)` / `(사업주 서명)` 존재, 한글 정상 |
| V10 | 배포 백엔드 `GET /health` (web_fetch) | ✅ `{"status":"ok","commit":"e43bc29","modusign":true,"upstage":true,"cors_origins":["http://localhost:3000","http://127.0.0.1:3000"]}` |
| V11 | git history 비밀키/`.env` 추적 여부 | ✅ `.env` 미추적, 키 패턴 매치 0건 |

**외부 유료 API는 호출하지 않았다.** 모두싸인 `request_signature`는 fake 함수로 대체했고, Upstage 실호출·`evaluate.py` 재실행은 하지 않았다. 데이터 삭제·배포·운영 변경 없음.

## 확인하지 못한 자료 / 분석 한계

| 항목 | 이유 |
|---|---|
| GitHub 이슈·PR 본문 | 웹 접근 미수행. PR #6 머지는 커밋 `842d95e`로만 확인 |
| `npm run build` 성공 여부 | 실행 환경 타임아웃 (V5). `.next/`에 `BUILD_ID` 없고 `archive`/`complete` 라우트 산출물 누락 → 기존 `.next`는 `next dev` 산출물로 추정 |
| Upstage 실제 추출 정확도 (실사진) | 유료 API 미호출. `results.json`은 **합성 벤치마크**이며 `evaluate.py` docstring이 그 한계를 명시 |
| 손글씨 반복 측정 결과 | `spikes/fixtures/*_runs.json` 이 저장소에 **없음** (§6) |
| Railway 배포본의 한글 PDF 렌더 | `/health/pdf` 바이너리 응답 미취득. 로컬 Linux + `fonts-noto-cjk` 환경에서는 정상이었으므로 Dockerfile 기준 정상일 것으로 **추정** |
| 프론트 Vercel 배포 URL | 저장소·문서 어디에도 없음 → 미배포로 추정 |
| 모두싸인 실제 서명란 좌표 | 실발송 미수행. 상수 이중화 문제(§10-H2)로 어긋날 가능성 있음 |

---

# 4. 아키텍처 및 코드 구조

## 디렉터리 구조 (자동생성물 제외)

```
AI-Builder-Sprint/
├── AGENTS.md / CLAUDE.md          # 에이전트 공통 지침 (단일 기준)
├── KB.md                          # 법정 기준 지식베이스 (출처 우선순위 명시)
├── README.md / DESIGN.md
├── agents/                        # lead / contract-safety / implementation / qa-demo
├── docs/                          # 11종 — 설계결정·인터뷰·작업분담·챗봇설계
├── backend/
│   ├── Dockerfile                 # pango/cairo + fonts-noto-cjk + poppler-utils
│   ├── requirements.txt           # 전 의존성 버전 고정 ✅
│   ├── pyproject.toml             # ruff(E,F,I,UP,B) + pytest 설정
│   ├── make_test_pdf.py           # PDF 육안 확인 스크립트
│   ├── scripts/check_minimum_wage.py
│   └── app/
│       ├── main.py                # FastAPI 앱, CORS, /health, /health/pdf
│       ├── config.py              # pydantic-settings, ../.env
│       ├── schemas.py  (243)      # ★ 단일 진실 공급원 (A↔B↔D 인터페이스)
│       ├── ai/         (696)      # document_parse.py, extract.py
│       ├── validation/ (1,206)    # rules.py, constants.py, severity.py, cli.py
│       ├── review/     (215)      # fields.py — 확인 관문 판별
│       ├── bridge/     (257)      # templates.py(문구 생성), numbers.py(숫자 대조)
│       ├── pdf/        (172)      # generator.py + templates/employment_contract.html
│       ├── signing/    (207)      # modusign.py, anchors.py ← ⚠️ 미연결
│       ├── evaluation/ (745)      # 합성 벤치마크 생성·측정
│       └── routers/    (880)      # contracts.py, extract.py, sign.py
├── spikes/                        # 수동 검증 스크립트 + fixtures
└── web/                (3,168)
    ├── app/                       # page, upload, review, result, contract, sign, complete, archive
    ├── components/                # ScreenShell, CheckResultCard, FieldInput, ui, ...
    └── lib/                       # api.ts, schemas.ts(zod), types.ts, session.ts, constants.ts
```

## 데이터 및 호출 흐름 (실제 코드 기준)

```
경로 A (사진)
 web/app/upload/page.tsx
   └ extractTerms(file)  ──POST /contracts/extract──▶ routers/extract.py
                                                       └ ai/extract.extract_contract_terms()
                                                          ├ ai/document_parse.parse_document()   [Upstage DP]  → full_text
                                                          ├ ai/extract.call_information_extract() [Upstage IE]  → values+confidence
                                                          ├ _find_source_text(full_text, ...)     [근거 문장 매칭]
                                                          └ apply_sanity_check()                  [코드 상식검사]
                                                       └ build_review_items() → 로그만 남김 ⚠️ 응답에 미포함
 web/app/review/page.tsx  (사용자 확인·수정 → confidence를 HIGH로 덮어씀)
   └ validateTerms()     ──POST /contracts/validate──▶ routers/contracts.py
                                                       └ _minimize_contact_fields()  [전화·주소 즉시 삭제]
                                                       └ validation/rules.validate() [순수함수 · LLM 없음]
 web/app/result/page.tsx  (CheckResultCard 표시)
 web/app/contract/page.tsx
   └ previewPdf()        ──POST /contracts/preview──▶ _reject_if_blocking() → render_contract_pdf(is_draft=True)
 web/app/sign/page.tsx
   └ analyzeAndSign()    ──POST /contracts/analyze-sign──▶ ❌ 409에서 항상 정지 (§10-C1)
                            [정상 시] ① unconfirmed_high_priority → ② _name_conflicts
                                      → ③ _reject_if_blocking → ④ validate
                                      → render_contract_pdf(is_draft=False) → modusign.request_signature()
 web/app/complete/page.tsx
   └ getSignStatus()     ──GET /contracts/{id}/status──▶ sign.reconcile() [모두싸인 API가 유일한 상태 근거]
 모두싸인 ──POST /webhooks/modusign/{token}──▶ "확인해보라" 신호로만 사용 → reconcile()

경로 B (구두계약)  web/app/review?path=B → createEmptyTerms() → 이후 경로 A와 동일 모듈
```

**설계상 가장 잘 된 부분**: `reconcile()`이 상태를 쓰는 **유일한 함수**이고, 웹훅 이벤트를 상태 근거로 쓰지 않는다(`routers/sign.py:107-130`). 웹훅 유실 시 다음 조회에서 자동 복구된다. 모두싸인 특별상 "연동의 견고성(8점)"에 직결되는 설계다.

## 외부 의존성

| 서비스 | 용도 | 실패 시 | 복구 로직 |
|---|---|---|---|
| Upstage Document Parse | 원문 텍스트 (근거 매칭용) | 502 반환 | ❌ 재시도 없음 |
| Upstage Information Extract | 23필드 추출 | 502 반환 | ❌ 재시도 없음, 경로 B로 우회 가능 |
| 모두싸인 `/documents` | 서명 요청 발송 | `ModusignError`→502 | ⚠️ `httpx.HTTPError`는 미처리 → 500 (§10-M1) |
| 모두싸인 `/documents/{id}` | 상태 조회 | 502 | ✅ 웹훅 이벤트로 근사 반영 후 다음 조회에서 정정 |
| PostgreSQL | — | — | ❌ **미연결**. `sqlalchemy`, `psycopg[binary]`가 requirements에만 존재, 코드 사용 0건 |

---

# 5. 기능별 구현 현황

상태 구분: ✅완료·검증됨 / 🟡구현·미검증 / 🟠부분구현 / ⚫코드만 존재·미연결 / ❌미구현 / ❔확인불가

| # | 기능 | 상태 | 근거 (파일·함수) | 실행 검증 | 테스트 |
|---|---|---|---|---|---|
| 1 | 계약 조건 추출 (Upstage) | 🟡 | `ai/extract.py:588 extract_contract_terms`, `ai/document_parse.py:31 parse_document` | 배포 `upstage:true`. 실호출은 유료라 미수행 | ✅ 68건 (mock) |
| 2 | 신뢰도 판정 + 상식 검사 | ✅ | `ai/extract.py:249 CONFIDENCE_THRESHOLD=0.90`, `:313 apply_sanity_check` | 단위 테스트 통과 | ✅ |
| 3 | 양식 라벨 유출 차단 | ✅ | `ai/extract.py:200 _looks_like_form_label`, `:176 _CHECKMARK` | 테스트 통과 | ✅ |
| 4 | 최저임금 판정 | ✅ | `validation/rules.py:433 check_minimum_wage`, `constants.py MINIMUM_WAGE_BY_YEAR` | **실측: `시급 9,500원 < 2026년 최저임금 10,320원` → VIOLATION** | ✅ |
| 5 | 주휴 시간요건 판정 | ✅ | `rules.py:493 check_weekly_holiday` | **실측: `주 16.5시간 ≥ 15시간` → OK** | ✅ |
| 6 | 휴게시간 판정 | ✅ | `rules.py:578 check_break_time`, `BREAK_RULES` | **실측: `5.5시간: 휴게 30분 ≥ 30분` → OK** | ✅ |
| 7 | 18세 미만 근로시간·야간 | ✅ | `rules.py:255 check_minor_working_hours`, `:360 check_minor_night_work` | 생년월일 없으면 검사 미생성 (설계대로) | ✅ |
| 8 | 필수 기재사항 9종 | ✅ | `rules.py:684 check_required_fields` | **실측: 9건 전부 응답** | ✅ |
| 9 | 입력값 유효성 + 진행 차단 | ✅ | `validation/severity.py:build_validation_state`, `WAGE_RANGES` | **실측: `can_proceed:true`, warning 1건** | ✅ 21건 |
| 10 | 개인정보 최소화 | ✅ | `contracts.py:34 _minimize_contact_fields`, `session.ts minimizeTermsForValidation` | 검증·PDF 경로 양쪽 확인 | ✅ |
| 11 | 확인 관문 (409) | ✅ 백엔드 / ❌ 프론트 | `review/fields.py:unconfirmed_high_priority`, `contracts.py:370` | **실측 409 재현. 프론트 미대응** | ✅ 26건 |
| 12 | 이름 불일치 감지 (409) | ✅ 백엔드 / ❌ 프론트 | `contracts.py:319 _name_conflicts` | 프론트에 `NAME_MISMATCH` 처리 없음 | ✅ |
| 13 | "말 꺼내기" 문구 생성 | ✅ 백엔드 / ❌ 프론트 | `bridge/templates.py build_message`, `bridge/numbers.py verify` | **실측: 정상 문구 + `numbers_verified:true`. UI 호출 0건** | ✅ 22건 |
| 14 | 계약서 PDF 생성 | ✅ | `pdf/generator.py:96 render_contract_pdf` | **실측: 273,682 bytes, 한글 정상** | ✅ 13건 |
| 15 | anchor 텍스트 삽입 | ✅ | `templates/employment_contract.html:224,243 .sign-anchor` | **pdfminer 추출: `(근로자 서명)` `(사업주 서명)` 모두 True** | ✅ |
| 16 | 초안 워터마크 조건 분기 | ✅ | `generator.py is_draft`, `contracts.py:432` 주석 | preview=True / sign=False 확인 | ✅ |
| 17 | 경로 B 출처 문구 | ✅ | `contracts.py:214 MANUAL_ENTRY_NOTICE` | 코드 확인 | ✅ |
| 18 | 모두싸인 서명 요청 | 🟡 | `signing/modusign.py:66 request_signature` | **호출부 도달 확인(fake)**. 실발송 미수행 | ❌ |
| 19 | anchor 오프셋 실측 보정 | ⚫ | `signing/anchors.py:69-70` (`-0.010`/`-0.246`) | **`modusign.py:31-32`가 자체값(`0.028`/`-0.25`) 사용, anchors.py import 0건** | ❌ |
| 20 | 서명 상태 동기화 | 🟡 | `sign.py:100 reconcile` | 로직 확인. 실문서 미검증 | ❌ |
| 21 | 웹훅 수신 | 🟡 | `sign.py:237 webhook` | 로직 확인 | ❌ |
| 22 | 웹훅 토큰 검증 | 🟠 | `sign.py:216 _check_token`, `config.py webhook_path_token` | **`.env`에는 `MODUSIGN_WEBHOOK_SECRET`만 있음 → 이름 불일치로 검증 비활성** | ❌ |
| 23 | 합성 추출 벤치마크 | ✅ | `evaluation/evaluate.py`, `results.json` | 230/230 (100%). docstring에 한계 명시 | — |
| 24 | 손글씨 반복 정확도 측정 | 🟠 | `spikes/check_extract.py` | **스크립트만 존재. `*_runs.json` 산출물 없음** | — |
| 25 | 최저임금 고시 변경 감시 | 🟡 | `scripts/check_minimum_wage.py` | 미실행 | ❌ |
| 26 | 프론트 화면 8종 | ✅ | `web/app/` — page/upload/review/result/contract/sign/complete/archive | tsc·eslint 통과. `next build` ❔ | ❌ |
| 27 | 프론트 세션 관리 | ✅ | `lib/session.ts` (sessionStorage + zod) | 코드 확인 | ❌ |
| 28 | 프론트 API 계층 | 🟠 | `lib/api.ts` — 5개 연결 / **3개 미연결** | `/message`, `/validation-state`, `/review-items` 호출 0건 | ❌ |
| 29 | 경로 B 직접 입력 폼 | ✅ | `web/app/review/page.tsx?path=B`, `createEmptyTerms()`, `web/app/page.tsx:90` | **인수인계 문서는 "미착수"라 기재 → 문서 오류** | ❌ |
| 30 | 계약서 보관함 | ⚫ | `web/app/archive/page.tsx` (34줄, "준비 중" 안내) | 의도적 스텁. 목 목록을 보관 기능처럼 표시하지 않음(정직) | ❌ |
| 31 | 계약 비서 챗봇 | ❌ | `docs/챗봇_설계.md` (293줄, 설계만) | 코드 0건 | — |
| 32 | DB 영속화 | ❌ | `sign.py:25 _store: dict` | 재시작 시 이력 소실 | ❌ |
| 33 | 로그인/인증 | ❌ | — | 설계상 미도입 (README 명시) | — |

## 발견된 코드 위생 문제

- **TODO/FIXME/HACK: 0건.** grep 결과 매치는 전부 UI `placeholder` 속성과 `_store` 관련 정당한 "임시" 주석뿐이다. 코드 위생은 양호하다.
- **미사용 파일/함수**: `app/signing/anchors.py` (프로덕션 미연결) · `pdf/generator.py:159 verify_anchors` (`make_test_pdf.py`만 사용, docstring에 신뢰 불가 명시)
- **미사용 의존성**: `sqlalchemy==2.0.36`, `psycopg[binary]==3.2.3` (코드 사용 0건)
- **미사용 환경변수**: `PUBLIC_DATA_API_KEY` (사용 여부 미정 — 의도적)
- **하드코딩**: 법정 기준값은 `validation/constants.py`에 상수로 **의도적으로** 내장(재현성 확보 근거를 주석에 명시). 문제 아님.
- **프론트↔백엔드 인터페이스 불일치**: `web/lib/types.ts AnalyzeSignRequest`에 `confirmed_fields` 필드 자체가 없다. `violationBlockedEnvelopeSchema`는 `problems`를 필수로 요구하는데 백엔드의 `UNCONFIRMED_FIELDS`/`NAME_MISMATCH` 응답에는 그 키가 없다.

---

# 6. 진행상황 문서와 실제 코드의 차이

인수인계 문서 §5 "현재 구현 상태" 표를 코드와 1:1 대조했다.

| 문서 주장 | 판정 | 근거 |
|---|---|---|
| 1. 계약 조건 추출 ✅ 구현 | **일치** | `ai/extract.py` 620줄, 68개 테스트 |
| 2. 법정 기준 검증 ✅ 구현 | **일치** | `validation/rules.py` 754줄, 실측 12개 check 응답 |
| 3. 계약서 생성 + 전자서명 ✅ 구현 | **일부만 일치** | PDF·anchor·요청 코드는 완성. **웹 UI에서 발송 불가**(§10-C1), **오프셋 상수 이중화**(§10-H2), **실발송 미검증** |
| 4. 서명 전 확인 관문 ✅ 구현 | **일부만 일치** | 백엔드 완성·테스트 26건. **프론트가 `confirmed_fields`를 안 보내 관문이 관문이 아니라 벽이 됐다** |
| 5. "말 꺼내기" 문구 ✅ 템플릿 버전 | **일부만 일치** | 백엔드 완성·검증(`numbers_verified:true`). **UI 노출 0건** |
| 6. 추출 정확도 평가셋 ✅ 구현 | **일부만 일치** | 합성 벤치마크 230/230은 실재. **손글씨 반복 측정(`check_extract.py`) 산출물 `*_runs.json`이 저장소에 없음** |
| 7. 프론트엔드 🚧 API 연결 중 | **일치** | 8개 중 5개 연결, 3개 미연결 |
| 8. 계약서 보관함 🚧 화면만 | **일치 (정직)** | `archive/page.tsx`가 "저장하지 않습니다"를 명시 |
| 9. **경로 B 직접 입력 폼 📋 미착수** | **문서 오류 — 실제로는 구현됨** | `review/page.tsx` `wantsManual`/`createEmptyTerms()`, 홈 `page.tsx:90`에 `/review?path=B` 링크. **문서가 코드보다 뒤처졌다** |
| 10. 챗봇 📋 조건부 | **일치** | 설계 문서만 |

## 인수인계 문서 §2 스택 표

| 문서 주장 | 판정 | 근거 |
|---|---|---|
| "AI 연동 — Document Parse / Information Extract / **Solar LLM**" | **문서 오류** | 전체 grep 결과 Solar 호출부 0건. `spikes/bridge_spike.py:18`의 미구현 주석뿐 |
| "웹훅 서명 검증 관련 진행 상황 코드로 재확인 필요 (`MODUSIGN_WEBHOOK_SECRET`)" | **코드가 다르다** | 코드는 `WEBHOOK_PATH_TOKEN`(URL 경로 토큰)으로 재설계됐고 HMAC이 아님을 주석에 명시. 그러나 `.env`에는 `MODUSIGN_WEBHOOK_SECRET`만 있어 **토큰 검증이 사실상 비활성** |
| "PDF 템플릿 폰트 의존성 — Railway 반영 여부 재확인" | **README가 stale** | `Dockerfile`에 `fonts-noto-cjk` + `fc-cache -fv` 있음. 로컬 Linux 렌더 정상 확인. README.md:605의 "macOS 시스템 폰트 의존" 기술은 낡았다 |

## AGENTS.md

| 주장 | 판정 |
|---|---|
| `AGENTS.md:22` "프론트엔드 … `web/` **(미착수)**" | **문서 오류** — 3,168줄 구현됨. 인수인계 문서가 이미 지적했으나 파일은 아직 안 고쳐졌다 |

**결론**: 문서가 코드를 **과장한 곳**(3·4·5·6번, Solar LLM)과 **과소평가한 곳**(9번 경로 B, `web/` 미착수, 폰트 의존)이 둘 다 있다. 심사자용 README는 어느 쪽도 위험하다. 과장은 Q&A에서 무너지고, 과소평가는 만든 걸 못 보여준다.

---

# 7. 대회 평가 기준별 분석

## 7-0. 적격성 심사 (Pass/Fail — 미충족 시 채점 없이 심사 제외)

| 항목 | 판정 기준 | 현재 | 근거 / 부족한 점 |
|---|---|---|---|
| 주제 부합성 | "AI를 통해 인간다움을 드러내는 서비스" | **PASS (강함)** | "말 꺼내기 문구"(서사 ②)가 주제에 정면으로 맞는다. 단, 화면에 없어 심사 시 근거 제시가 약하다 |
| 개발 방식 | 코드 기반 (노코드 단독 불가) | **PASS** | Python 4,621줄 + TS 3,168줄 |
| 개발 기간 준수 | 커밋상 주요 개발이 대회 기간(7/27~8/3) 내 | **PASS** | 56건 중 55건이 7/27 이후. `c336094`(7/26 "Add README") 1건만 이전이며 코드 아님 |
| **제출물 완비** | 데모 영상·배포 링크·코드 저장소·발표 자료·AI 활용 기술서 | **❌ FAIL 위험 (최대 리스크)** | 저장소·AI 증빙은 있음. **배포 링크(프론트 미배포)·데모 영상·발표 자료가 전부 없음.** README.md:583-587 체크리스트 5개 전부 미체크 |

**→ 이 한 칸이 나머지 전부보다 중요하다.**

## 7-1. 예선 1차 제출물 심사 (80점 만점)

| 심사 기준 | 세부 항목 | 배점 | 충족 수준 | 근거 | 부족한 점 / 개선 작업 | 예상 |
|---|---|---|---|---|---|---|
| **창의성 (20)** | 문제 정의·아이디어 독창성 | 10 | **4** | "탐지가 아니라 대면이 벽"이라는 문제 재정의(README.md:56-66). 청소년 미작성 19.5%·경상권 23.5% 1차 출처 인용 | 지역사회 근거는 강한데 지역 협력기관이 "현재 확정 없음"(README.md:648) | 8~9 |
| | 핵심 기능 구성의 참신성 | 10 | **3** | ①알려준다-②말하게한다-③남긴다 3단 구조. "LLM은 판정하지 않는다"는 역설계 | **②가 화면에 없다.** 심사자가 데모에서 볼 수 있는 건 ①③뿐 → 흔한 "OCR+서명" 서비스로 보인다 | 6~8 |
| **AI 활용도 (20)** | 개발 프로세스 전반의 AI 통합 | 10 | **4** | Document Parse + Information Extract 실연동, confidence 활용, `source_text` 근거 매칭, 상식검사·라벨유출 차단까지 코드 방어 | Solar LLM 미사용. 챗봇 미착수. "AI를 어디에 쓰지 **않았는가**"가 오히려 차별점인데 문서가 이를 세일즈 포인트로 못 쓰고 있다 | 7~8 |
| | 에이전트 설정·지침 체계성 | 5 | **5** | `AGENTS.md`(113) + `agents/` 4역할(223) + `CLAUDE.md` import 구조 + 검토 결과 형식(PASS/PASS_WITH_CONCERNS/BLOCKED) + 파일 소유권 규칙 | **이 프로젝트 최고 강점.** `AGENTS.md:22`의 "web/ 미착수" 한 줄만 고치면 만점권 | 4~5 |
| | 결과물 품질 기여도 | 5 | **3** | `results.json` 230/230, `evaluate.py` docstring이 합성 벤치마크 한계를 스스로 명시 | 손글씨 실측 산출물(`*_runs.json`) 없음 → "3회 모두 정확, 흔들림 0"이라는 **더 강한 증거**가 저장소에 없다 | 3~4 |
| **완성도 (20)** | 핵심 기능 구현·정상 작동 | 10 | **2** | 백엔드는 실측 200 완주. 208 테스트 통과 | **웹에서 서명 발송 불가(§10-C1). 프론트 미배포. 배포 CORS 미설정(§10-C2).** 심사자가 링크로 들어가 완주할 수 없다 | 3~5 |
| | 서비스 이용 흐름 | 5 | **3** | 화면 8종, 6단계 stepper, 상태 뱃지, 접근성 속성(`aria-live`, `aria-current`) | sign 화면에서 막히고 복구 안내가 없다. 보관함은 스텁 | 2~3 |
| | 코드 품질·오류 처리 | 5 | **4** | `lib/api.ts` 8종 에러코드 분류, zod 응답 검증, 백엔드 예외에 응답 본문 미노출, ruff/pytest 게이트 | ruff 6건, `httpx.HTTPError` 미처리 → 500, 파일 크기 제한 없음 | 3~4 |
| **실용성 (20)** | 문제의 구체성·타당성 | 10 | **5** | 1차 출처 기반 수치, 근로기준법 제17조 교부 의무·제114조 벌금까지 연결, `KB.md` 출처 우선순위 규칙 | 없음. 이 항목은 만점권 | 9~10 |
| | 실제 활용 가능성·기대 효과 | 10 | **3** | 모두싸인으로 실제 법적 효력 있는 문서가 남는 설계. 경로 A/B 이중 진입 | 로그인·DB 없음(재시작 시 이력 소실), 보관함 없음 → "실제 활용"의 지속성 근거가 약하다 | 6~7 |

**예선 예상 총점: 52~62 / 80** (분석상 추정치. 공식 점수 아님)
**P0 3건 해결 후 추정: 62~70 / 80**

## 7-2. 에이전트 평가 운영 (실격 위험 점검)

| 항목 | 상태 |
|---|---|
| README·코드 주석에 심사 지시 삽입 (프롬프트 인젝션) | ✅ **없음.** grep 결과 매치는 전부 "넣지 말자"는 **자기 경고문**(README.md:521, docs/작업지시_A_B_D.md:329)과 전략 메모다 |
| 참고 | `docs/2일_작업계획.md:255,268`, `docs/팀_공유_0729.md:31`, `docs/D_작업요청.md:4`에 "심사위원" 언급이 있다. 내부 전략 메모이며 지시문이 아니라 안전하다고 판단하나, **AI 스크리너가 문맥 없이 읽을 위험을 0으로 만들려면 제출 전 표현을 중립화하는 것을 검토**할 수 있다 (팀장 판단 필요) |
| 신뢰도 확보 | 팀당 독립 3회 실행 후 평균 → **README가 3회 모두 같은 결론을 내도록 일관돼야 한다.** 현재 README·AGENTS.md·인수인계 문서가 서로 다른 상태를 주장(§6)하므로 편차가 커질 수 있다 |

## 7-3. 본선 발표 심사 (100점 + 가점 10)

예선 4항목(80) 재평가 + 발표력 20(구성·논리성 10 / 시연·Q&A 대응 10)

| 가점 | 배점 | 현재 | 근거 |
|---|---|---|---|
| Upstage API 활용 | +5 | **+4~5 유력** | Document Parse + Information Extract 실연동, confidence·`source_text` 심화 활용, 정확도 측정 산출물 보유 |
| 지역사회 기여도 | +5 | **+2~3** | 부산 금정구 배경, 경상권 미작성률 23.5% 전국 최고 인용. **확정 협력기관 없음**(README.md:648)이 감점 요인 |

**Q&A 최대 취약점**: "판정에 LLM을 안 쓴다면서 화면에서는 왜 AI 문구 생성 기능이 안 보이나요?" / "메모리에 저장하면 서버 재시작 시 계약 이력은 어떻게 되나요?" — 둘 다 현재 정직하게 답하면 감점이다.

## 7-4. 모두싸인 특별상 (100점 + 가점 10)

OT는 Task 1(부산 관광 상생 계약 비서) / Task 2(마음을 남기는 서명)를 제시한다. **FairSign은 어느 Task에도 정확히 해당하지 않는다** — 근로계약이지 관광 계약도, 기부·유산기부 약정도 아니다.

⚠️ **팀장 판단 필요**: 특별상이 (a) 제시된 두 Task 중 하나를 반드시 선택해야 하는지, (b) 예시일 뿐이고 "전자서명·CLM을 계약 경험에 실제 구현한 팀"이면 되는지 OT 자료만으로는 확정할 수 없다. **운영진에게 문의해 확인해야 한다.** (b)라면 아래 배점으로 강력한 후보다.

| 평가 항목 | 세부 | 배점 | 충족 | 근거 / 부족한 점 | 예상 |
|---|---|---|---|---|---|
| **CLM·API 활용도 (35)** | 전자서명 플로우 실제 구현 (요청→서명→완료) | 15 | **2** | 코드 전 구간 존재 + PDF anchor 검증 통과. **그러나 웹에서 발송 불가(§10-C1)이고 실발송 검증 기록도 없다** | 4~7 |
| | 계약 생애주기 커버리지 (다단계 연계) | 12 | **4** | 8단계 `DocumentStatus`가 모두싸인 상태와 1:1 대응. DRAFTING→REVIEW_REQUESTED→TERMS_CONFIRMED→...→COMPLETED. 추출→검증→생성→서명→상태추적까지 연계 | 보관·재열람 단계 없음(`archive` 스텁). 이력 영속화 없음 | 8~10 |
| | 연동의 견고성 (상태 동기화·예외) | 8 | **4** | **`reconcile()`이 상태의 유일한 근거**이고 웹훅을 신호로만 쓴다 — 이벤트 유실에 자동 복구되는 설계(`sign.py:107-130`). API 실패 시 `EVENT_TO_STATUS` 근사 폴백. 웹훅 실패에도 200 반환 | `httpx.HTTPError` 미처리(§10-M1), 웹훅 토큰 비활성(§10-M2), 재시도·백오프 없음 | 5~6 |
| **실용성 (25)** | 문제 정의의 구체성·타당성 | 12 | **5** | §7-1 동일 근거 | 11~12 |
| | 실제 활용·확산 가능성 | 13 | **3** | 경로 A/B 이중 진입으로 계약서 없는 19.5%까지 포섭 | DB·로그인·보관 없음, 협력기관 없음 | 7~9 |
| **인간다움 (15)** | 계약·서명 경험을 더 사람답게 만든 정도 | 15 | **4** | 서사 자체가 이 항목이다. "고발이 아니라 문의", 이름 불일치 시 **코드가 고르지 않고 사람에게 되돌린다**(`contracts.py:319-355` 주석), 체결 문서에 '초안' 표기 제거 판단(`contracts.py:432-449`) — 모두 사람을 존중하는 설계 결정이고 주석에 이유가 남아 있다 | **말 꺼내기 문구가 화면에 없다.** 인간다움의 가장 강한 증거가 시연 불가 | 10~13 |
| **AI 활용도 (15)** | AI를 계약 파이프라인에 실질 통합 | 10 | **4** | 추출→확인→판정→문서 생성까지 AI가 파이프라인 안에 있고, 판정만 의도적으로 배제 | Solar LLM 미사용 | 7~8 |
| | AI가 결과 품질(정확성·안정성)에 기여 | 5 | **3** | confidence 임계값 0.90, 상식검사, `ALWAYS_CONFIRM` 관문 — AI 오류를 코드가 방어하는 구조 | 손글씨 실측 산출물 없음 | 3~4 |
| **완성도 (10)** | 데모 정상 동작·UI/UX·문서화 | 10 | **3** | 문서화는 최상급(4,967줄). UI 8종 완성 | **데모 정상 동작이 안 된다** | 4~6 |
| 가점 | 부산 지역·관광 결합 +5 | 5 | **1** | 부산 배경이지만 관광 결합 없음 | 1~2 |
| 가점 | 사회적 임팩트·약자 보호 +5 | 5 | **5** | 청소년 근로자 보호가 핵심. 근거 있는 사회 문제 | 4~5 |

**특별상 예상 총점: 60~75 / 100 (+5~7 가점)** — P0 해결 시 **78~88** 가능. 다만 Task 부합성 확인이 선행 조건이다.

---

# 8. 코드 품질 평가

| 항목 | 점수 | 판단 근거 | 잘된 점 | 문제점 | 개선 방법 | 심각도 |
|---|---|---|---|---|---|---|
| 구조와 모듈화 | **9**/10 | `ai/validation/review/bridge/pdf/signing/routers` 책임 분리 명확. `schemas.py`가 단일 진실 공급원 | `validation/`이 순수함수만 담아 테스트 가능성 극대화 | `signing/anchors.py`가 "단일 출처"인데 미연결 | anchors.py를 modusign.py가 import | High |
| 가독성과 일관성 | **9**/10 | 한국어 주석이 **결정 이유**를 남긴다(예: `contracts.py:432-449` 워터마크 제거 근거) | "왜 이렇게 했나"가 코드에 남아 인수인계 비용이 낮다 | 일부 주석이 코드보다 최신(README와 역전) | — | Low |
| 명명 품질 | **9**/10 | `unconfirmed_high_priority`, `_reject_if_blocking`, `build_validation_state` 등 의도가 드러남 | 스키마 필드명이 표준근로계약서 항목 번호와 대응 | `FIELD_LABELS`가 `review/fields.py`와 `validation/severity.py`에 중복 정의 | 한 곳으로 통합 | Medium |
| 중복 코드 관리 | **5**/10 | `SIGN_OFFSET_*` 2곳, `FIELD_LABELS` 2곳, `_minimize_contact_fields` 2곳(`contracts.py:34`, `sign.py:29`) | — | **오프셋 이중화는 실제 버그로 이어진다** | anchors.py를 유일 출처로, 나머지는 공용 모듈로 | High |
| 관심사 분리 | **10**/10 | 판정(rules) / 유효성(severity) / 확인관문(review) / 문구(bridge) / 문서(pdf) / 발송(signing)이 서로 모른다 | `severity.py` docstring이 rules와의 역할 차이를 명시 | 없음 | — | — |
| 확장성·유지보수성 | **8**/10 | `MINIMUM_WAGE_BY_YEAR` 연도별 테이블 + `minimum_wage_for()` 근사 처리 + 고시 감시 스크립트 | 법령 변경에 대비한 구조 | `_store` 교체 지점이 인터페이스화되지 않음 | 저장소 프로토콜 추출 | Medium |
| 타입 안정성 | **8**/10 | Pydantic 전면 + zod 응답 검증 + `tsc --noEmit` 통과 | 프론트가 서버 응답을 런타임 검증한다 | `worker_email: str` (EmailStr 아님). `ReviewResponse.items: list[dict]` (구조 미고정) | `EmailStr` 도입, `ReviewItem` 모델화 | High |
| 오류·예외 처리 | **6**/10 | 백엔드가 외부 예외 본문을 사용자에 노출하지 않음(`extract.py:44`, `document_parse.py:60` 주석) | `log.error("... error_type=%s", type(e).__name__)` — 민감정보 미기록 | **`modusign.py`에서 `httpx.HTTPError` 미포착 → 500**. 재시도/백오프 없음. `ModusignError(f"HTTP {code}: {res.text}")`가 응답 본문을 예외 메시지에 담는다 | `httpx.HTTPError`를 `ModusignError`로 감싸고, 메시지에서 `res.text` 제거 | High |
| 입력값 검증 | **5**/10 | `severity.py`가 `WAGE_RANGES`·시간 형식·필수항목을 검사하고 `_reject_if_blocking`이 모든 문서 생성 경로에서 재검사 | 프론트 우회를 전제한 이중 방어 설계가 훌륭하다 | **`/contracts/extract`에 파일 크기·MIME 검증 전무**(`extract.py:39 await file.read()`). 이메일 검증 백엔드 부재 | `MAX_UPLOAD_BYTES` + content_type 화이트리스트, `EmailStr` | **Critical** |
| 보안 | **5**/10 | `.env` 미추적 확인, 커밋 내 키 패턴 0건, 로그에 계약 내용·키 미기록, 타이밍 공격 대비 `compare_digest` | 개인정보 최소화(`_minimize_contact_fields`)를 서버·클라이언트 양쪽에 구현 | 인증 없음(설계) + 크기 제한 없음 → **공개 엔드포인트로 유료 Upstage/모두싸인 무제한 호출 가능**. 웹훅 토큰 비활성. `spikes/fixtures/handwritten_01.png` + 실명 커밋 | §10-H3, §10-H4 | **Critical** |
| 성능 | **7**/10 | httpx 타임아웃 60s/30s 명시 | 무한 루프·명백한 누수 없음 | 대용량 파일 전체 메모리 로드. WeasyPrint 동기 렌더가 이벤트 루프 블로킹 | `run_in_threadpool` 검토 | Medium |
| 테스트 가능성 | **9**/10 | 판정 로직이 순수함수라 I/O 없이 테스트 가능 | `conftest.py`로 bridge 픽스처 분리 | 라우터 레벨 테스트 없음 | TestClient 통합 테스트 추가 | Medium |
| 테스트 커버리지 | **6**/10 | **208 passed** — validation 54 / extract 68 / review 26 / bridge 22 / severity 21 / safety 13 / parse 4 | 핵심 판정·관문 로직은 촘촘하다 | **`routers/`·`pdf/`·`signing/` 테스트 0건.** 프론트 테스트 0건. E2E 없음 | analyze-sign 409/200 회귀 테스트 최우선 | **High** |
| 문서화 | **9**/10 | 4,967줄. `KB.md`가 법령 상수마다 출처 ID(`SRC-MINWAGE-2026`)와 확인일자 부여 | 출처 우선순위 규칙(국가법령정보센터 > 고용노동부 > ...)까지 문서화 | README·AGENTS.md가 코드보다 뒤처짐(§6) | §11-P1-5 | High |
| 설정·환경 관리 | **6**/10 | requirements 전 버전 고정. `.env.example` 제공. `/health`가 설정 상태 노출 | `RAILWAY_GIT_COMMIT_SHA`로 배포 커밋 확인 — 실제로 `e43bc29` 확인됨 | **`.env`(`MODUSIGN_WEBHOOK_SECRET`) ↔ `.env.example`/`config.py`(`WEBHOOK_PATH_TOKEN`) 키 이름 불일치.** `.env`에 `CORS_ORIGINS` 없음. 미사용 의존성 2개 | 키 이름 통일, CORS 설정 | **Critical** |
| 로깅·관찰 가능성 | **8**/10 | fontTools 등 소음 라이브러리 억제(`main.py:20`). `reconcile()`이 저장소 미존재까지 로깅 | 민감정보 미기록 원칙 준수 | 구조화 로깅·요청 ID 없음 | — | Low |
| 접근성·UX | **7**/10 | `aria-live="polite"`, `aria-current="step"`, `aria-hidden`, `min-h-14` 터치 타깃 | 상태별 아이콘+색상+텍스트 3중 표시(색맹 대응) | sign 실패 시 복구 경로 없음. 로딩 20초 안내 여부 미확인 | §11-P0-1 | High |
| 배포 준비도 | **4**/10 | Dockerfile이 pango/cairo/폰트/poppler까지 완비. Railway 실제 배포 확인(`commit:e43bc29`) | 백엔드 배포는 실제로 성공했다 | **CORS 미설정, 프론트 미배포, DB 미연결** | §11-P0-2, P0-3 | **Critical** |

**ruff 6건** (`python3 -m ruff check backend/`)
```
app/ai/tests/test_extract.py:361:5  F811  Redefinition of unused `_normalize_extracted_value`  ← 테스트 신뢰성 영향
app/schemas.py:8:22                 F401  `datetime.date` imported but unused
app/validation/severity.py:28:1     I001  Import block un-sorted
app/signing/modusign.py:42:11       UP012 Unnecessary UTF-8 encoding argument
app/pdf/generator.py:81:8           UP038 Use `X | Y` in isinstance
make_test_pdf.py:22:1               E402  Module level import not at top
```
4건은 `--fix`로 자동 해결. **F811은 실제 위험** — `test_extract.py:361`에서 import한 함수를 재정의하고 있어 상단 20줄의 import가 죽고 테스트가 다른 대상을 검증할 수 있다.

---

# 9. 테스트, 실행 및 배포 준비 상태

## 실제로 확인한 것

```bash
# V1
$ cd backend && python3 -m pytest -q
208 passed in 0.66s          ✅

# V2
$ python3 -m ruff check backend/
Found 6 errors (4 fixable)    ⚠️

# V3, V4
$ cd web && npx tsc --noEmit  → exit 0   ✅
$ npx eslint .                → exit 0   ✅

# V10
$ GET https://ai-builder-sprint-production.up.railway.app/health
{"status":"ok","commit":"e43bc29","modusign":true,"upstage":true,
 "cors_origins":["http://localhost:3000","http://127.0.0.1:3000"]}   ⚠️ CORS 미설정
```

**V6 — 백엔드 API 실호출 결과** (FastAPI TestClient, 시급 9,500원 / 09:00~15:00 / 주3일 가상 계약)

| 엔드포인트 | 상태 | 응답 요약 |
|---|---|---|
| `POST /contracts/validate` | 200 | 12개 check: `MINIMUM_WAGE` VIOLATION (`시급 9,500원 < 2026년 최저임금 10,320원`), 나머지 11건 OK |
| `POST /contracts/validation-state` | 200 | `can_proceed:true`, `blocking_fields:[]`, `counts:{error:0,warning:1,info:0}`, issue에 `field`/`fix`/`step`/`source` 포함 |
| `POST /contracts/message` | 200 | 정상 문구 생성 + `numbers_verified:true`. 숫자가 `ValidationReport`와 대조 통과 |
| `POST /contracts/review-items` | 200 | `must_confirm: [employer_business_name, employer_name, wage_amount, wage_type, worker_name]` |
| `POST /contracts/preview` | 200 | `application/pdf`, 279,239 bytes |
| **`POST /contracts/analyze-sign` (프론트와 동일 body)** | **409** | `{"code":"UNCONFIRMED_FIELDS","fields":["사업체명","대표자","임금 금액","임금 형태","근로자 성명"]}` |
| `POST /contracts/analyze-sign` (`confirmed_fields` 5개 포함) | **200** | `document_id`, `status:ON_GOING`, PDF 277,498 bytes, 모두싸인 호출부 도달 |

**V9 — PDF 검증** (`pdfminer.high_level.extract_text`)
```
(근로자 서명)  → True     ✅ anchor 존재
(사업주 서명)  → True     ✅
'확인 전 초안'  → False    ✅ is_draft=False 시 워터마크 없음 (설계대로)
한글 렌더      → "표준근로계약서 / OO카페 (이하 "사업주"라 함)..." 정상
```

## 확인하지 못한 것

| 항목 | 이유 | 사용자가 직접 확인할 명령 | 예상 문제 지점 |
|---|---|---|---|
| `npm run build` | 세션 타임아웃(45s). `.next/`에 `BUILD_ID` 없고 `archive`·`complete` 라우트 산출물 누락 | `cd web && npm run build` | `useSearchParams` 사용 페이지의 Suspense 경계, `next/dynamic` 이슈 |
| Railway 한글 PDF | 바이너리 응답 미취득 | 브라우저로 `<railway-url>/health/pdf` 열기 | □□□로 깨지면 폰트 미탑재 |
| 실제 서명 발송·anchor 좌표 | 유료 API·실메일 발송이라 미수행 | `python3 spikes/modusign_spike.py send` (가상 인물) | 오프셋 이중화(§10-H2)로 서명란이 22.5% 어긋날 가능성 |
| Upstage 실사진 추출 | 유료 미호출 | `python3 spikes/check_extract.py spikes/fixtures/handwritten_01.png --runs 3` | 손글씨 정확도, 호출 간 흔들림 |
| 프론트 실브라우저 완주 | 백엔드+프론트 동시 구동 필요 | `uvicorn app.main:app --reload` + `npm run dev` → localhost:3000 완주 | sign 화면에서 409 |

**코드나 설정을 변경해 테스트를 통과시킨 일은 없다.** 파일 수정 0건.

---

# 10. 위험 요소

## Critical

### C1. 웹 UI에서 전자서명 발송이 구조적으로 불가능
- **근거**: `web/lib/api.ts analyzeAndSign()`과 `web/app/sign/page.tsx:104-118`이 `confirmed_fields`를 보내지 않는다. `web/lib/types.ts AnalyzeSignRequest`에 필드 자체가 없다. 백엔드 `review/fields.py ALWAYS_CONFIRM`은 `worker_name`·`employer_name`·`employer_business_name`·`wage_amount`·`wage_type` 5개를 **신뢰도와 무관하게 항상 high**로 처리하고, `contracts.py:370`이 1단계에서 409를 던진다.
- **실측 재현**: V7에서 프론트와 동일한 body로 호출 → 409. `confirmed_fields` 추가 → 200 (V8).
- **2차 피해**: `web/lib/schemas.ts violationBlockedEnvelopeSchema`가 `problems` 배열을 필수로 요구하는데 `UNCONFIRMED_FIELDS` 응답에는 그 키가 없다 → `safeParse` 실패 → `responseError()`로 떨어져 `"/contracts/analyze-sign 요청을 완료하지 못했습니다."`라는 **복구 불가능한 일반 오류**만 표시된다. `proceed_with_violations:true`로도 우회 불가(확인 관문이 1단계로 먼저 실행됨).
- **왜 중요한가**: 모두싸인 특별상 "전자서명 플로우 실제 구현(15점)"과 예선 "핵심 기능 구현·정상 작동(10점)"의 직접 근거가 시연 불가다. 서사 ③이 무너진다.
- **검증 방법**: 브라우저로 upload→review→result→contract→sign 완주해 `/complete`에서 `document_id`가 표시되는지 확인 + `confirmed_fields` 누락 시 409를 확인하는 회귀 테스트 추가.

### C2. 배포 백엔드의 CORS_ORIGINS 미설정
- **근거**: V10 실측 `cors_origins: ["http://localhost:3000","http://127.0.0.1:3000"]`. `config.py:44 allowed_origins`는 `CORS_ORIGINS` 환경변수가 비면 localhost만 반환한다. `.env`에 `CORS_ORIGINS` 항목 자체가 없다(`.env.example`에는 있음).
- **왜 중요한가**: 프론트를 Vercel에 배포하는 순간 브라우저가 preflight부터 전부 차단한다. **배포 링크 제출(적격성 Pass/Fail)이 "링크는 있는데 아무것도 안 되는" 상태가 된다.** `config.py:39` 주석이 이미 이 위험을 경고했는데 설정만 안 된 상태다.
- **검증 방법**: Vercel 배포 후 `/health` 응답의 `cors_origins`에 Vercel 도메인이 포함되는지 확인 + 브라우저에서 실제 업로드 성공.

### C3. 제출물 미비 — 적격성 Fail 위험
- **근거**: README.md:583-587 체크리스트 5개 전부 미체크. 프론트 배포 URL·데모 영상·발표 자료가 저장소·문서에 없다. OT p.9: "미충족 항목 발견 시 채점 없이 심사 제외".
- **검증 방법**: 배포 링크 접속 → 데모 영상 재생 → 발표 자료 파일 존재 → AI 활용 기술서 존재. 4개 전부 ✅.

### C4. 공개 엔드포인트의 유료 API 무제한 호출 가능
- **근거**: `routers/extract.py:39 file_bytes = await file.read()` — 크기 제한·MIME 검증·인증·레이트리밋 전무. 이 요청 하나가 Upstage Document Parse + Information Extract를 **2회 호출**한다. `SignRequestBody.worker_email: str`(EmailStr 아님)이라 API 직접 호출로 임의 주소에 모두싸인 서명 요청 발송 가능.
- **왜 중요한가**: 배포 링크가 공개되는 순간 API 크레딧 소진 → **데모 당일 서비스 정지**. CORS는 브라우저만 막고 curl은 못 막는다.
- **검증 방법**: 10MB 초과 파일 업로드 시 413, 이미지·PDF 외 MIME 거부, 잘못된 이메일 형식에 422 반환 확인.

## High

### H1. "말 꺼내기 문구"가 UI에 없다
- **근거**: `web/` 전체 grep — `/contracts/message` 호출 0건. `lib/api.ts`에 함수 자체가 없고 `app/result/page.tsx`에 문구·복사 버튼 관련 코드가 없다.
- **왜 중요한가**: README가 이 기능을 **핵심 서사 ②**로 내세우고(README.md:56-66), 대회 주제 "AI로 인간다움 표현"에 가장 정확히 부합하는 기능이다. 백엔드는 완성·검증(`numbers_verified:true`)됐는데 심사자에게 안 보인다. 창의성 "핵심 기능 참신성(10점)"과 특별상 "인간다움(15점)"에 직접 손실.
- **투입 대비 효과가 이 프로젝트에서 가장 크다.** 백엔드 작업 0, 프론트 1~2시간.

### H2. anchor 오프셋 상수 이중화 — 실측 보정값이 실제 발송에 미반영
- **근거**: `signing/anchors.py:69-70`이 `SIGN_OFFSET_X=-0.010`, `SIGN_OFFSET_Y=-0.246`을 실측 보정값으로 정의하고 "단일 출처"라 선언한다(`anchors.py:4-11`이 과거 불일치 버그를 기록). 그러나 `signing/modusign.py:31-32`가 자체적으로 `0.028`, `-0.25`를 정의하고 `:56`에서 사용한다. **`modusign.py`는 `anchors`를 import하지 않는다** (grep 확인: anchors import는 `spikes/` 2개 파일뿐).
- **왜 중요한가**: `anchors.py`가 문서화한 "모두싸인이 세로로 22.5% 밀어놓는다"는 실측 보정이 프로덕션 경로에 없다. X축은 `0.028` vs `-0.010`으로 부호까지 다르다. 실발송 시 서명란이 엉뚱한 곳에 놓이면 특별상 핵심 시연이 망가진다.
- **검증 방법**: `modusign.py`가 `anchors.py`에서 import하도록 고친 뒤 `spikes/modusign_spike.py send`로 가상 인물에게 1회 발송, 서명란 위치 육안 확인.

### H3. 실제 계약서 사진 + 실명이 공개 저장소에 커밋됨
- **근거**: `git ls-files`에 `spikes/fixtures/handwritten_01.png`(2.7MB, 실제 촬영 손글씨 계약서)가 추적 중. `handwritten_01.json`에 `"worker_name": "박강현"`, `"employer_business_name": "편의점"`, 계약기간·시급 실값이 담겨 있고 `_note`에 "실제 손글씨 계약서"라 명시돼 있다. 커밋 `e289b66`.
- **자체 규칙 위반**: `AGENTS.md` — "테스트와 데모에는 **가상 계약서와 가상 인물만** 사용합니다", "`.env`, 자격증명, **실제 계약서, 개인정보**를 커밋하지 않습니다". `.gitignore`도 `spikes/*.png`를 무시하도록 설정돼 있는데 이 파일은 추적되고 있다(강제 추가 또는 규칙 추가 이전 커밋 추정).
- **왜 중요한가**: 본인 이름이라 법적 문제는 제한적이나, **AI 스크리너가 AGENTS.md의 규칙과 저장소 내용을 대조하면 자기 규칙 위반으로 판정될 수 있다.** "약자 보호"를 내세우는 프로젝트에서 개인정보 취급 일관성은 신뢰도 문제다.
- ⚠️ **팀장 판단 필요**: 손글씨 실측 근거는 AI 활용도 증빙에서 가치가 크다. 삭제할지, 가상 인물로 재촬영할지, 이름만 마스킹할지 결정이 필요하다. **git 히스토리에 남는다는 점을 고려할 것.**

### H4. 프론트가 `/contracts/validation-state`를 호출하지 않는다
- **근거**: `contracts.py:93-99` docstring — "프론트엔드는 **이 응답만 보고** 다음 단계 버튼을 켜고 끈다. 같은 규칙을 화면에 복사하지 말 것 — 두 곳에 두면 반드시 어긋난다." 커밋 `8ca792d` 제목은 "입력값 유효성 검증과 진행 차단 (프론트·백엔드 공통 기준)". **그러나 `web/` grep 결과 호출 0건.**
- **왜 중요한가**: 백엔드가 설계한 단일 기준이 절반만 적용됐다. `web/lib/constants.ts`가 `MINIMUM_WAGE_2026 = 10_320`을 표시용으로 중복 보유하고 있어(주석은 "판정 계산에 쓰지 않는다"고 경고), 백엔드 상수 변경 시 화면 문구가 어긋난다. Q&A에서 "프론트·백엔드 기준을 하나로 뒀다"고 말하면 코드와 다르다.

### H5. 라우터·PDF·서명 계층 테스트 0건
- **근거**: 208개 테스트가 전부 `ai/`·`bridge/`·`review/`·`validation/`에 있다. `routers/`·`pdf/`·`signing/` 테스트 없음.
- **왜 중요한가**: C1(409 블로커)이 208개 테스트를 전부 통과한 채로 존재했다. **테스트가 통과한다는 사실이 데모가 동작한다는 뜻이 아니다.** 지금 상태에서 가장 값싸고 효과적인 안전망은 `analyze-sign` 통합 테스트다.

## Medium

- **M1. `httpx.HTTPError` 미처리 → 500**: `signing/modusign.py:88-96`의 `request_signature`·`get_document`가 `httpx` 예외를 감싸지 않는다. `contracts.py:467`과 `sign.py:78`은 `ModusignError`만 잡으므로 네트워크 타임아웃 시 502가 아닌 **500**이 나가고 프론트는 "서버에서 요청을 처리하지 못했습니다"(재시도 가능)로 오분류한다. 특별상 "연동의 견고성(8점)"에 직접 영향. 추가로 `ModusignError(f"HTTP {code}: {res.text}")`가 응답 본문을 예외 메시지에 담아, 상위에서 실수로 로깅하면 계약 정보가 새어 나갈 수 있다(현재는 `type(e).__name__`만 기록하므로 실제 유출은 없다).
- **M2. 웹훅 토큰 검증 사실상 비활성**: `config.py:20 webhook_path_token`은 `WEBHOOK_PATH_TOKEN`을 읽지만 `.env`에는 `MODUSIGN_WEBHOOK_SECRET`만 있다. 값이 비면 `_check_token`이 검증을 건너뛴다(`sign.py:225`). 상태는 `reconcile()`이 API에서 읽으므로 **위조로 상태 조작은 불가**하지만, 엔드포인트 남용(모두싸인 API 쿼터 소모)은 열려 있다. 키 이름 통일만으로 해결된다.
- **M3. DB 미연결 — 이력 소실**: `sign.py:25 _store: dict`. 재배포·크래시 시 계약 이력 전부 소실. `requirements.txt`에 `sqlalchemy`·`psycopg`가 있으나 코드 사용 0건. 실용성 "실제 활용 가능성(10/13점)" 감점 요인이며, 데모 중 Railway 재배포가 걸리면 `/complete` 화면이 깨진다.
- **M4. 파일 크기·WeasyPrint 블로킹**: 대용량 업로드 전체 메모리 로드 + `render_contract_pdf`가 동기 함수라 async 이벤트 루프를 블로킹한다.
- **M5. ruff F811** (`test_extract.py:361`): import한 `_normalize_extracted_value`를 재정의해 상단 import가 죽는다. 테스트가 의도한 대상을 검증하지 않을 수 있다.
- **M6. 문서 stale**: `AGENTS.md:22` "web/ 미착수", `README.md:605` "macOS 시스템 폰트 의존"(Dockerfile에 `fonts-noto-cjk` 있음), 인수인계 문서 "Solar LLM"·"경로 B 미착수". AI 스크리너 3회 실행의 편차 원인이 될 수 있다.
- **M7. `FIELD_LABELS` 중복**: `review/fields.py`와 `validation/severity.py`에 동일 딕셔너리 2개.

## Low

- `docs/` 4개 파일의 "심사위원" 언급 — 내부 전략 메모이며 지시문이 아니다. 안전하다고 판단하나 표현 중립화는 검토 가치가 있다.
- 구조화 로깅·요청 ID 없음.
- `pdf/generator.py:159 verify_anchors`가 압축 스트림 때문에 신뢰 불가(docstring에 명시). `pdftotext` 기반으로 교체하면 CI에서 anchor 회귀를 잡을 수 있다.
- `.DS_Store` 파일 다수 (gitignore됨, 미추적 — 문제 없음).

---

# 11. 남은 과제 백로그 (담당자별)

> 담당 코드: **A** AI 파이프라인 · **B** 검증 엔진 · **C** PDF·서명·배포·DB(팀장) · **D** 프론트엔드

## P0 — 제출·실행·핵심 기능을 막는 문제

### P0-1 · 확인 관문 UI 연결 — 서명 발송 복구 【담당: D】
| | |
|---|---|
| 설명 | `/contracts/review-items`로 `must_confirm`을 받아 확인 체크박스 UI를 만들고, `confirmed_fields`를 `analyze-sign`에 전달한다. `UNCONFIRMED_FIELDS`·`NAME_MISMATCH` 409 응답을 파싱해 복구 화면을 띄운다 |
| 필요한 이유 | §10-C1. **현재 웹에서 서명 발송이 0% 성공한다** |
| 관련 평가 기준 | 예선 완성도 10점 / 특별상 CLM 15점 |
| 관련 파일 | `web/lib/api.ts`(+`fetchReviewItems`), `web/lib/types.ts`(`confirmed_fields` 추가), `web/lib/schemas.ts`(409 스키마 3종 분기), `web/app/review/page.tsx` 또는 `sign/page.tsx` |
| 우선순위 / 심각도 | **P0 / Critical** |
| 난이도 / 작업량 | 중 / 3~4시간 |
| 선행 작업 | 없음 (백엔드 준비 완료 — V8에서 200 확인) |
| 완료 조건 | 브라우저에서 upload→…→sign 완주 시 `/complete`에 `document_id` 표시. 확인 안 한 항목이 있으면 어떤 항목인지 화면에 표시되고 그 자리에서 확인 가능 |
| 검증 방법 | 실브라우저 완주 스크린샷 + `confirmed_fields` 누락 시 409 안내가 뜨는 것 확인 |
| 미이행 위험 | **데모 불가. 특별상 사실상 탈락** |

### P0-2 · Railway CORS_ORIGINS 설정 + 프론트 Vercel 배포 【담당: C(팀장)】
| | |
|---|---|
| 설명 | Railway 환경변수에 `CORS_ORIGINS=<vercel-domain>` 설정. Vercel에 `web/` 배포 + `NEXT_PUBLIC_API_BASE_URL` 설정 |
| 필요한 이유 | §10-C2. 현재 배포본은 localhost만 허용 |
| 관련 평가 기준 | **적격성 Pass/Fail** (배포 링크) |
| 관련 파일 | Railway·Vercel 대시보드, `.env`에 `CORS_ORIGINS` 추가 |
| 우선순위 / 심각도 | **P0 / Critical** |
| 난이도 / 작업량 | 하 / 1시간 |
| 선행 작업 | `npm run build` 성공 확인 (P0-4) |
| 완료 조건 | 배포된 `/health`의 `cors_origins`에 Vercel 도메인 포함. 브라우저에서 파일 업로드 성공 |
| 검증 방법 | 시크릿 창에서 배포 링크로 완주 |
| 미이행 위험 | 링크는 있는데 아무것도 안 되는 상태 → 적격성 Fail |

### P0-3 · 제출물 4종 준비 【담당: C(팀장) 총괄, 팀 전원】
| | |
|---|---|
| 설명 | ① 발표 자료(문제정의·사용자·핵심기능·AI 활용·기대효과) ② 데모 영상 + 스크린샷 + 테스트 계정 안내 ③ AI 활용 기술서(모델·API 사용 위치·프롬프트/설정·테스트·검증 산출물) ④ 배포 링크 |
| 필요한 이유 | §10-C3. **적격성 Pass/Fail** |
| 관련 평가 기준 | 적격성 전 항목 + 예선 4항목 전부의 전달 경로 |
| 관련 파일 | 신규. `README.md:583` 체크리스트 갱신 |
| 우선순위 / 심각도 | **P0 / Critical** |
| 난이도 / 작업량 | 중 / 8~10시간 (분산) |
| 선행 작업 | P0-1, P0-2 (데모 영상은 동작하는 화면이 있어야 촬영 가능) |
| 완료 조건 | 4종 파일·링크가 실재하고 외부인이 접근 가능 |
| 검증 방법 | 팀원 아닌 사람에게 링크·영상 전달 → 이해되는지 확인 |
| 미이행 위험 | **채점 없이 심사 제외** |

### P0-4 · 프론트 프로덕션 빌드 검증 【담당: D】
| | |
|---|---|
| 설명 | `npm run build` 완주 확인. 실패 시 수정 |
| 필요한 이유 | §9 V5 확인 불가. `.next/`에 `BUILD_ID` 없고 일부 라우트 산출물 누락 |
| 관련 평가 기준 | 적격성(배포) / 완성도 |
| 관련 파일 | `web/` 전체 |
| 우선순위 / 심각도 | **P0 / Critical** |
| 난이도 / 작업량 | 하~중 / 0.5~2시간 |
| 완료 조건 | `npm run build` exit 0, 8개 라우트 전부 산출 |
| 검증 방법 | `npm run build && npm start` 후 8화면 접속 |
| 미이행 위험 | Vercel 배포 실패 → P0-2 연쇄 실패 |

### P0-5 · 업로드 크기·MIME 검증 + 이메일 검증 【담당: C】
| | |
|---|---|
| 설명 | `/contracts/extract`에 `MAX_UPLOAD_BYTES`(예: 10MB)와 content_type 화이트리스트(jpeg/png/pdf) 추가, 초과 시 413. `worker_email`/`employer_email`을 `EmailStr`로 변경 |
| 필요한 이유 | §10-C4. 공개 링크로 유료 API 무제한 호출 가능 → **데모 당일 크레딧 소진 위험** |
| 관련 평가 기준 | 완성도 코드품질·오류처리 5점 / 특별상 연동 견고성 |
| 관련 파일 | `backend/app/routers/extract.py`, `backend/app/schemas.py`, `backend/app/routers/sign.py`, `backend/app/routers/contracts.py` |
| 우선순위 / 심각도 | **P0 / Critical** |
| 난이도 / 작업량 | 하 / 1시간 |
| 완료 조건 | 초과 파일에 413, 잘못된 MIME에 415/400, 잘못된 이메일에 422 |
| 검증 방법 | 회귀 테스트 3건 추가 |
| 미이행 위험 | 데모 중 API 정지 |

## P1 — 평가 점수와 핵심 UX에 큰 영향

### P1-1 · "말 꺼내기 문구" 화면 노출 【담당: D】
| | |
|---|---|
| 설명 | `/contracts/message`를 `lib/api.ts`에 추가하고 `app/result/page.tsx`에 문구 카드 + **클립보드 복사 버튼** 표시. `numbers_verified:false`면 표시하지 않음 |
| 필요한 이유 | §10-H1. **핵심 서사 ②가 시연 불가.** 투입 대비 점수 효과가 가장 크다 |
| 관련 평가 기준 | 창의성 참신성 10점 / 특별상 인간다움 15점 / 주제 부합성 |
| 관련 파일 | `web/lib/api.ts`, `web/lib/schemas.ts`, `web/lib/types.ts`, `web/app/result/page.tsx` |
| 우선순위 / 심각도 | **P1 / High** |
| 난이도 / 작업량 | 하 / 1~2시간 |
| 선행 작업 | 없음 (백엔드 검증 완료, `numbers_verified:true` 실측) |
| 완료 조건 | 최저임금 미달 계약 검증 시 결과 화면에 문구가 나오고 복사가 된다 |
| 검증 방법 | 시급 9,500원으로 검증 → 문구 표시 → 복사 → 메모장 붙여넣기 |
| 미이행 위험 | 흔한 "OCR + 전자서명" 서비스로 보인다 |

### P1-2 · anchor 오프셋 단일 출처화 + 실발송 1회 검증 【담당: C】
| | |
|---|---|
| 설명 | `modusign.py`가 `signing/anchors.py`에서 `SIGN_OFFSET_X/Y`, `SIGN_BOX_W/H`, `ANCHOR_*`를 import하도록 변경. 자체 상수 제거. 가상 인물로 1회 실발송해 서명란 위치 확인 |
| 필요한 이유 | §10-H2. 실측 보정값이 프로덕션에 미반영. X축은 부호까지 다르다 |
| 관련 평가 기준 | 특별상 전자서명 플로우 15점 |
| 관련 파일 | `backend/app/signing/modusign.py:31-32`, `backend/app/signing/anchors.py` |
| 우선순위 / 심각도 | **P1 / High** |
| 난이도 / 작업량 | 하(코드) / 중(검증) / 2시간 |
| 완료 조건 | `SIGN_OFFSET` 정의가 저장소에 1곳. 실발송 문서에서 서명란이 anchor 위에 정확히 놓임 |
| 검증 방법 | `SIGN_OFFSET` grep 결과가 `anchors.py` 1곳 + 실발송 스크린샷 |
| 미이행 위험 | 특별상 핵심 시연에서 서명란이 엉뚱한 곳에 |

### P1-3 · analyze-sign 통합 테스트 추가 【담당: C 또는 B】
| | |
|---|---|
| 설명 | TestClient로 `/contracts/analyze-sign`의 409(UNCONFIRMED)·409(NAME_MISMATCH)·422(INVALID_VALUES)·200 4경로 테스트. 모두싸인은 monkeypatch |
| 필요한 이유 | §10-H5. **C1이 208개 테스트를 전부 통과한 채 존재했다** |
| 관련 평가 기준 | 완성도 코드품질 5점 / AI 활용도 결과물 품질 기여 |
| 관련 파일 | 신규 `backend/app/routers/tests/test_analyze_sign.py` |
| 우선순위 / 심각도 | **P1 / High** |
| 난이도 / 작업량 | 하 / 2시간 |
| 완료 조건 | 4경로 테스트 통과. 프론트가 `confirmed_fields`를 빼면 CI가 실패로 알려준다 |
| 검증 방법 | `pytest -v -k analyze_sign` |
| 미이행 위험 | 같은 종류의 통합 결함 재발 |

### P1-4 · `modusign.py` httpx 예외 포착 【담당: C】
| | |
|---|---|
| 설명 | `request_signature`·`get_document`를 `try/except httpx.HTTPError → ModusignError`로 감싸고, `ModusignError` 메시지에서 `res.text` 제거 |
| 필요한 이유 | §10-M1. 네트워크 오류가 500으로 나가 프론트가 오분류 |
| 관련 평가 기준 | 특별상 연동 견고성 8점 |
| 관련 파일 | `backend/app/signing/modusign.py:88-131` |
| 우선순위 / 심각도 | **P1 / High** |
| 난이도 / 작업량 | 하 / 30분 |
| 완료 조건 | 네트워크 실패 시 502 + 사용자 안내 문구 |
| 검증 방법 | BASE_URL을 잘못된 값으로 monkeypatch해 502 확인하는 테스트 |
| 미이행 위험 | 데모 중 네트워크 순단 시 500 스택트레이스 노출 |

### P1-5 · 문서-코드 일치화 【담당: C(팀장)】
| | |
|---|---|
| 설명 | `AGENTS.md:22` "web/ (미착수)" → 현황 반영. `README.md:605` macOS 폰트 기술 삭제(Dockerfile 근거). 인수인계 문서의 "Solar LLM" 제거, "경로 B 미착수" → 구현됨. README 기능표를 **§5 표 기준**으로 재작성 |
| 필요한 이유 | §6. AI 스크리너 3회 실행의 편차 원인. 과장은 Q&A에서 무너지고 과소평가는 만든 걸 못 보여준다 |
| 관련 평가 기준 | AI 활용도 에이전트 지침 체계성 5점 / 에이전트 평가 신뢰도 |
| 관련 파일 | `AGENTS.md`, `README.md`, 인수인계 문서 |
| 우선순위 / 심각도 | **P1 / High** |
| 난이도 / 작업량 | 하 / 1~2시간 |
| 완료 조건 | 문서의 모든 ✅ 주장에 대응 파일·함수가 실재하고 UI 연결 여부까지 구분 표기 |
| 검증 방법 | 문서 주장 → 코드 grep 왕복 점검 |
| 미이행 위험 | 3회 실행 편차로 심사위원 정밀 재검토 대상이 됨 |

### P1-6 · 손글씨 반복 측정 실행 + 산출물 커밋 【담당: A】
| | |
|---|---|
| 설명 | `spikes/check_extract.py --runs 3` 실행 후 `*_runs.json`을 저장소에 커밋. "3회 모두 정확 / 흔들림 N건" 요약을 AI 활용 기술서에 기재 |
| 필요한 이유 | §6. 합성 벤치마크 100%보다 **실사진 반복 측정이 훨씬 강한 증거**인데 산출물이 없다 |
| 관련 평가 기준 | AI 활용도 결과물 품질 기여 5점 / 본선 Upstage 가점 +5 |
| 관련 파일 | `spikes/check_extract.py`, `spikes/fixtures/*_runs.json` |
| 우선순위 / 심각도 | **P1 / High** |
| 난이도 / 작업량 | 하 / 1시간 (+ API 비용 소량) |
| 선행 작업 | H3 결정 (실계약서·실명 사용 여부) |
| 완료 조건 | `*_runs.json` 커밋 + 요약 수치 문서화 |
| 검증 방법 | 산출물 파일과 문서 수치가 일치 |
| 미이행 위험 | Q&A "정확도 근거?"에 합성 벤치마크만 제시 → 설득력 약화 |

### P1-7 · 개인정보 fixture 처리 방침 결정 【담당: 팀장 판단 필요】
| | |
|---|---|
| 설명 | `spikes/fixtures/handwritten_01.png` + 실명 라벨의 처리 결정: (a) 삭제 (b) 가상 인물로 재촬영 후 교체 (c) 이름만 마스킹. git 히스토리 잔존 여부 판단 포함 |
| 필요한 이유 | §10-H3. AGENTS.md 자체 규칙 정면 위반 |
| 관련 평가 기준 | 에이전트 평가 신뢰도 / 특별상 사회적 임팩트(약자 보호 일관성) |
| 우선순위 / 심각도 | **P1 / High** |
| 난이도 / 작업량 | 결정: 즉시 / 실행: 1~3시간(선택지별) |
| 완료 조건 | 저장소에 실제 계약서·실명이 없거나, 남기는 근거가 문서화됨 |
| 미이행 위험 | 자기 규칙 위반이 지적되면 문서 신뢰도 전체가 흔들린다 |

## P2 — 품질·안정성 개선

| # | 작업 | 담당 | 관련 파일 | 완료 조건 | 작업량 |
|---|---|---|---|---|---|
| P2-1 | 웹훅 키 이름 통일 (`WEBHOOK_PATH_TOKEN`) + Railway 설정 | C | `.env`, `config.py:20`, `.env.example` | `_check_token`이 실제로 검증 수행 | 30분 |
| P2-2 | `/contracts/validation-state` 프론트 연결 | D | `web/lib/api.ts`, `review/page.tsx` | 다음 단계 버튼 상태가 백엔드 응답으로만 결정됨 | 2~3시간 |
| P2-3 | ruff 6건 해소 (특히 **F811**) | B 또는 A | `test_extract.py:361` 등 | `ruff check backend/` 0 errors | 30분 |
| P2-4 | `FIELD_LABELS` 중복 제거 | B | `review/fields.py`, `validation/severity.py` | 정의 1곳 | 30분 |
| P2-5 | 미사용 의존성 정리 (`sqlalchemy`, `psycopg`) 또는 DB 연결 | C | `requirements.txt` | 선언과 사용 일치 | 15분~4시간 |
| P2-6 | `verify_anchors`를 pdftotext 기반으로 교체 + 회귀 테스트 | C | `pdf/generator.py:159` | anchor 누락 시 테스트 실패 | 1시간 |
| P2-7 | WeasyPrint 렌더 `run_in_threadpool` | C | `pdf/generator.py`, 호출부 | 동시 요청 시 응답 지연 완화 | 1시간 |
| P2-8 | 20초 로딩 UX (진행 표시·취소) | D | `web/app/upload/page.tsx` | 업로드 중 진행 상태 표시 | 1시간 |
| P2-9 | 프론트 오류 화면에 1350 안내 상시 노출 확인 | D | `components/LegalDisclaimer.tsx` | 모든 오류 경로에서 노출 | 30분 |

## P3 — 선택적 개선 / 대회 이후 기술 부채

| # | 작업 | 담당 | 비고 |
|---|---|---|---|
| P3-1 | Supabase 연결 (`_store` → DB) | C | 저장소 프로토콜 추출 후 교체. 실용성 점수에는 도움되나 P0 이후 |
| P3-2 | 계약서 보관함 구현 | D + C | 현재 스텁이 **정직해서** 감점이 크지 않다. 시간 없으면 스텁 유지 |
| P3-3 | 계약 비서 챗봇 | A | README가 "숫자 검증 없는 챗봇은 배포하지 않는다"고 못 박았다(README.md:524). **4일 남은 상황에서는 포기 권장** |
| P3-4 | Solar LLM 기반 문구 생성 (템플릿 위에 얹기) | A | `bridge/templates.py` docstring의 원래 설계. 숫자 검증 통과 필수 |
| P3-5 | 라우터 전체 통합 테스트 · 프론트 테스트 | D + C | — |
| P3-6 | `docs/` "심사위원" 표현 중립화 | 팀장 판단 필요 | 위험 낮음. 시간 남으면 |
| P3-7 | 구조화 로깅 + 요청 ID | C | — |
| P3-8 | 지역 협력기관 접촉 | 팀장 판단 필요 | 본선 지역사회 가점 +5에 직결. 4일 내 현실성 판단 필요 |

---

# 12. 권장 실행 로드맵 (마감 2026-08-03 18:00 · 실제 4일)

## Day 1 — 7/31(금): 데모를 성립시킨다

**가장 먼저**: P0-1(D) · P0-2 준비(C) — 이 둘이 안 되면 나머지가 무의미

| 시간 | D | C(팀장) | A | B |
|---|---|---|---|---|
| 오전 | **P0-1** 확인 관문 UI + `confirmed_fields` | **P0-5** 업로드 크기·MIME·이메일 검증 | **P1-6** 손글씨 반복 측정 실행 | **P2-3** ruff F811 해소 |
| 오후 | **P0-4** `npm run build` 검증 | **P1-2** anchor 단일 출처화 + 실발송 1회 | P1-6 산출물 커밋 + 요약 | **P1-3** analyze-sign 통합 테스트 |
| 저녁 | 로컬 완주 (upload→complete) | **P0-2** Vercel 배포 + CORS 설정 | — | — |

**Day 1 성공 기준**: **배포 링크로 사진 업로드부터 서명 요청 발송까지 한 번 완주.** 이게 안 되면 Day 2를 전부 여기 쓴다.
**병렬 가능**: P0-1(D) ∥ P0-5·P1-2(C) ∥ P1-6(A) ∥ P1-3·P2-3(B) — 파일 충돌 없음

## Day 2 — 8/1(토): 차별점을 보이게 한다

| 시간 | D | C | A/B |
|---|---|---|---|
| 오전 | **P1-1** 말 꺼내기 문구 노출 + 복사 버튼 | **P1-4** httpx 예외 포착 · **P2-1** 웹훅 키 통일 | **P1-7** 결정 반영 |
| 오후 | **P2-2** validation-state 연결 · **P2-8** 로딩 UX | **P1-5** 문서-코드 일치화 (README·AGENTS.md) | AI 활용 기술서 초안 (A) |
| 저녁 | 8화면 스크린샷 촬영 | 배포 안정화 + `/health/pdf` 육안 확인 | — |

**Day 2 성공 기준**: 배포 링크에서 ①알려준다 ②말하게한다 ③남긴다가 **전부 화면에 보인다**
**포기해도 되는 범위**: P2-2(validation-state)는 Day 3로 밀 수 있다. 챗봇(P3-3)·보관함(P3-2)·DB(P3-1)는 이 시점에 **공식 포기 선언**할 것

## Day 3 — 8/2(일): 제출물

| 시간 | 전원 |
|---|---|
| 오전 | **P0-3** 발표 자료 작성 (C 총괄, 문제정의·사용자·핵심기능·AI 활용·기대효과) |
| 오후 | **데모 영상 촬영** — 가상 인물로 실제 서명 완주 녹화. 실패 대비 백업 녹화 2본 |
| 저녁 | AI 활용 기술서 완성 (모델·API 위치·프롬프트/스키마·검증 산출물). 스크린샷·테스트 계정 안내 정리 |

**Day 3 성공 기준**: 제출물 4종이 실재하고 팀 외부인이 링크만으로 이해할 수 있다

## Day 4 — 8/3(월) ~18:00: 검증과 제출

| 시간 | 작업 |
|---|---|
| 오전 | 전 항목 최종 점검(§13 체크리스트). `pytest` · `ruff` · `tsc` · `build` 재실행. 시크릿 창에서 배포 링크 완주 3회 |
| 13:00 | **제출** (마감 5시간 전 완료 — Railway·Vercel 장애 대비 버퍼) |
| 이후 | README 최종 검토 (프롬프트 인젝션 의심 문구 재확인), 본선 발표 준비 착수 |

## 축소·포기 권장 범위

| 기능 | 판단 | 근거 |
|---|---|---|
| 계약 비서 챗봇 | **포기** | README.md:524가 "숫자 검증 없는 챗봇은 배포하지 않는다"고 명시. 4일 내 안전장치까지 구현 불가. **포기 자체를 Q&A 강점으로 쓸 수 있다** |
| Supabase DB 연결 | **포기 or 최후순위** | 메모리 저장의 한계를 정직하게 밝히는 편이 반쯤 붙은 DB보다 안전 |
| 보관함 구현 | **현 스텁 유지** | "목 목록을 보관 기능처럼 표시하지 않는다"는 현재 문구가 오히려 신뢰를 준다 |
| Solar LLM 문구 생성 | **포기** | 템플릿 버전이 이미 `numbers_verified:true`로 동작. LLM을 얹는 건 리스크만 늘린다 |
| 지역 협력기관 접촉 | **팀장 판단 필요** | 본선 가점 +5이나 4일 내 성사 가능성 판단이 필요 |

---

# 13. 대회 제출 및 데모 체크리스트

## 제출 파일 / 링크

- [ ] 발표 자료 — 문제 정의 · 사용자 · 핵심 기능 · AI 활용 방식 · 기대 효과
- [ ] 코드 저장소 (포크) — README · 실행 방법 · 주요 코드 · 커밋 내역 · **AI 지침 파일**(`AGENTS.md`, `CLAUDE.md`, `agents/`)
- [ ] 배포 링크 (프론트 Vercel + 백엔드 Railway) **또는** 데모 영상
- [ ] 스크린샷 (8화면)
- [ ] 테스트 계정 안내 (로그인 없음 → "계정 불필요, 가상 계약서 사진 첨부" 안내로 대체)
- [ ] AI 활용 증빙 — 모델(Upstage Document Parse / Information Extract) · API 사용 위치(`ai/document_parse.py:31`, `ai/extract.py:452`) · 추출 스키마(`build_extraction_schema`) · **검증 산출물**(`results.json` 230/230 + 손글씨 `*_runs.json`)

## 실행 환경 사전 점검

- [ ] `pytest` 208 passed
- [ ] `ruff check backend/` 0 errors
- [ ] `npx tsc --noEmit` exit 0
- [ ] `npm run build` exit 0
- [ ] `<railway>/health` → `upstage:true`, `modusign:true`, **`cors_origins`에 Vercel 도메인 포함**, `commit`이 최신 HEAD와 일치
- [ ] `<railway>/health/pdf` 브라우저에서 한글 정상 (□□□ 아님)
- [ ] Upstage·모두싸인 API 크레딧 잔량 확인
- [ ] `git status` 클린, `.env` 미추적

## 데모 시나리오 (7분 발표 기준)

1. **문제 제시 (60초)** — 청소년 미작성 19.5%, 경상권 23.5% 전국 최고, 1위 이유 "몰라서" 40.4%. 벽 3개(모른다/말 못 한다/증거 없다)
2. **① 알려준다 (90초)** — 가상 계약서 사진 업로드 → 23항목 추출 → **LOW 신뢰도 항목이 노란색으로 표시되는 화면** → 사용자 수정 → 검증 → `시급 9,500원 < 10,320원` VIOLATION. **"이 판정은 코드가 했습니다. LLM은 관여하지 않습니다"** + `validation/rules.py`에 네트워크 import가 0건임을 화면으로 제시
3. **② 말하게 한다 (90초)** — 결과 화면의 문구 카드 → 복사 → **"이 문장의 모든 숫자는 판정 결과와 대조 검증됩니다(`bridge/numbers.py`)"**. 이 서비스의 인간다움이 여기 있다
4. **③ 남긴다 (120초)** — PDF 미리보기 → 확인 관문(반드시 확인 항목 5개) → 서명 요청 발송 → 모두싸인 메일 → 서명 → `/complete`에서 상태·다운로드
5. **한계 정직 공개 (30초)** — 메모리 저장(재시작 시 이력 소실), 월급·일급은 UNKNOWN 처리, 주휴는 시간 요건까지만 판정, 법률 자문 아님
6. **마무리 (30초)** — 대타 근무 개근 인정 여부는 확인 못 해 **코드에 넣지 않았다**. "확인 못 한 건 넣지 않는다"가 이 팀의 원칙

## 실패 대비책

| 실패 지점 | 대비책 |
|---|---|
| 네트워크 장애 | 완주 녹화 영상 **2본**을 로컬에 보관하고 바로 재생 |
| Upstage API 장애/크레딧 소진 | **경로 B(직접 입력)로 즉시 전환** — 이미 구현돼 있다. "계약서 없는 19.5%를 위한 경로"라고 설명하면 오히려 강점 |
| 모두싸인 API 장애 | 사전 체결 완료 문서의 상태 화면 + 다운로드 PDF를 스크린샷으로 준비 |
| Railway 콜드스타트 | 발표 10분 전 `/health` 워밍업 호출 |
| 배포 CORS 문제 | 로컬 실행본(백엔드 + `npm run dev`)을 노트북에 준비 |
| 서명란 위치 어긋남 | P1-2 실발송 검증을 미리 완료. 어긋나면 그 문서는 데모에 쓰지 않는다 |

## 평가 기준별 제시 증거

| 기준 | 제시할 증거 |
|---|---|
| 창의성 | 3단 서사 화면 + 말 꺼내기 문구 실시연 + `bridge/numbers.py` 숫자 대조 코드 |
| AI 활용도 (개발 통합) | `ai/extract.py build_extraction_schema` + confidence 0.90 임계값 + `apply_sanity_check` + `_looks_like_form_label` |
| AI 활용도 (에이전트 체계) | `AGENTS.md` + `agents/` 4역할 + PASS/BLOCKED 검토 형식 + 파일 소유권 규칙 |
| AI 활용도 (결과 품질) | `results.json` 230/230 + 손글씨 `*_runs.json` + **한계를 명시한 `evaluate.py` docstring** |
| 완성도 | 배포 링크 실완주 + `pytest 208 passed` + `lib/api.ts` 8종 에러 분류 |
| 실용성 | 1차 출처 인용 + 근로기준법 제17조·제114조 연결 + 경로 A/B 이중 진입 |
| 특별상 CLM | 8단계 `DocumentStatus` 매핑 + `reconcile()` 단일 상태 근거 설계 + anchor 기반 배치 |
| 특별상 인간다움 | `contracts.py:319-355` 이름 불일치 처리 주석 + `:432-449` 워터마크 제거 판단 근거 |

## 발표에서 강조할 강점 (근거 있는 것만)

1. **"LLM은 판정하지 않는다"** — `validation/rules.py` 754줄에 네트워크·LLM import 0건, 208개 테스트가 고정
2. **AI가 자신 있게 틀리는 경우까지 방어했다** — 실측 `박강현→박강헌`(HIGH) 사례에서 `ALWAYS_CONFIRM` 관문을 도출
3. **코드가 판단할 수 없는 건 사용자에게 되돌린다** — 이름 불일치를 조용히 덮어쓰지 않는다
4. **확인 못 한 법령은 코드에 넣지 않았다** — 대타 근무 개근 인정 여부, 2026-12-10 시행 예정 휴게시간 개정
5. **평가 방법의 한계를 스스로 문서화했다** — 합성 벤치마크임을 `evaluate.py` docstring에 명시

---

# 14. 최종 판정

## 현재 상태로 제출 가능한가?
**아니다.** 적격성 "제출물 완비"(배포 링크·데모 영상·발표 자료) 미충족으로 **채점 없이 심사 제외** 위험이 있다. 저장소와 AI 증빙만으로는 통과할 수 없다.

## 현재 상태로 데모 가능한가?
**아니다.** 세 가지가 동시에 막고 있다.
1. 웹에서 서명 발송 100% 실패 (§10-C1, 실측 재현)
2. 프론트 미배포
3. 배포 백엔드 CORS 미설정 (§10-C2, 실측 확인)

**단, 백엔드 단독 데모는 지금도 가능하다.** `/docs`와 `spikes/full_pipeline.py`로 전 구간을 보여줄 수 있다. 다만 "사용자가 사용할 수 있는 실현 위주"(OT p.8 작성 팁)와는 거리가 있다.

## 가장 먼저 수정해야 할 5가지
1. **P0-1** 프론트 `confirmed_fields` 전송 + 409 복구 UI 【D】 — 이것 없이는 나머지가 무의미
2. **P0-2** Railway `CORS_ORIGINS` 설정 + Vercel 배포 【C】
3. **P0-4** `npm run build` 성공 확인 【D】
4. **P0-5** 업로드 크기·MIME·이메일 검증 【C】 — 링크 공개 후 크레딧 소진 방지
5. **P0-3** 제출물 4종 【C 총괄】

## 점수를 가장 많이 높일 수 있는 5가지 (투입 대비 효과 순)
1. **P1-1 말 꺼내기 문구 UI 노출** — 1~2시간, 창의성 10점 + 인간다움 15점에 직접 영향. **투입 대비 최고**
2. **P0-1 서명 발송 복구** — 3~4시간, 완성도 10점 + CLM 15점
3. **P0-2/P0-4 배포 완성** — 1~3시간, 적격성 통과 + 완성도 전 항목의 전달 경로
4. **P1-5 문서-코드 일치화** — 1~2시간, 에이전트 지침 체계성 5점(현재 유일한 만점권) + AI 스크리너 3회 편차 제거
5. **P1-6 손글씨 반복 측정 산출물** — 1시간, AI 활용도 결과물 품질 5점 + Upstage 가점 +5

## 삭제 또는 축소를 고려할 기능
- **챗봇** (P3-3) — 포기. README 스스로가 "안전장치 없는 챗봇은 없느니만 못하다"고 명시했다. 포기 근거를 발표에서 강점으로 전환할 수 있다
- **Supabase DB** (P3-1) — 포기 또는 최후순위. 한계를 정직히 밝히는 편이 낫다
- **보관함** (P3-2) — 현재 스텁 유지. 지금 문구가 오히려 신뢰를 준다
- **Solar LLM 문구 생성** (P3-4) — 포기. 템플릿 버전이 이미 검증 통과
- **`app/signing/anchors.py`** — 삭제하지 말고 **연결하라**(P1-2). 삭제하면 실측 지식이 사라진다

## 추가 확인이 필요한 질문 (팀장 답변 필요)

1. **모두싸인 특별상의 Task 부합성** — OT는 Task 1(부산 관광 상생 계약 비서) / Task 2(마음을 남기는 서명)를 제시한다. FairSign은 근로계약이라 어느 쪽도 아니다. 두 Task 중 선택이 **필수**인지, 아니면 "전자서명·CLM을 계약 경험에 실제 구현"이면 되는지 **운영진 확인이 필요하다.** 필수라면 특별상 전략 자체를 재검토해야 한다.
2. **`spikes/fixtures/handwritten_01.png` + 실명 처리 방침** — 삭제 / 가상 인물 재촬영 / 마스킹? git 히스토리 잔존은 수용 가능한가? (§10-H3)
3. **프론트 배포 대상** — Vercel 확정인가? 계정·도메인은 준비됐는가?
4. **서비스명** — "페어사인"으로 확정인가? (README는 "가안"이라 표기)
5. **C(PDF·서명·배포·DB) 담당자** — `docs/작업지시_A_B_D.md`상 팀장(C)이 겸임 중인데, 4일간 배포·제출물·백엔드 수정을 모두 감당할 수 있는가? 일부를 A/B에 재배분해야 하는가?
6. **지역 협력기관** — 본선 가점 +5. 4일 내 접촉 가능한 곳이 있는가?
7. **API 크레딧 잔량** — Upstage / 모두싸인 각각 얼마나 남았는가? P1-6 실측과 데모 리허설에 쓸 여유가 있는가?
8. **`docs/` "심사위원" 표현** — 제출 전 중립화할 것인가? (위험은 낮다고 판단하나 팀장 결정 사항)
9. **본선 발표자** — 7분 발표 + 5분 Q&A 담당은 누구인가? Q&A 취약 질문(§7-3) 대비가 필요하다

## 분석하지 못한 영역과 그 이유

| 영역 | 이유 |
|---|---|
| `npm run build` 성공 여부 | 실행 환경 45초 타임아웃. 빌드 미완주 |
| 실사진 Upstage 추출 정확도 | 유료 API 호출 금지 원칙 |
| 모두싸인 실발송·서명란 실좌표 | 유료 API + 실메일 발송 위험 |
| Railway 배포본 한글 PDF | 바이너리 응답 취득 실패 |
| GitHub 이슈·PR 본문·CI 설정 | 웹 접근 미수행. **CI/CD 설정 파일은 저장소에 없음**(`.github/` 부재 확인) |
| 프론트 실브라우저 동작 | 브라우저 실행 환경 부재. tsc·eslint 정적 검사로 대체 |
| `KB.md` 법령 인용의 정확성 | 법령 원문 대조 미수행. 출처 링크 존재만 확인 |
| 사용자 인터뷰 실데이터 | `docs/`에 정리된 범위만 확인 |

## 추가로 제공하면 분석 정확도가 높아지는 자료

1. `npm run build` 실행 로그 전문
2. `spikes/modusign_spike.py send` 실발송 결과 + 서명란 위치 스크린샷
3. `spikes/check_extract.py --runs 3` 실행 결과 (`*_runs.json`)
4. Railway `/health/pdf` PDF 파일 또는 스크린샷
5. Vercel 배포 URL (배포 후)
6. Railway·Vercel 환경변수 목록 (**값 없이 키 이름만**)
7. GitHub PR #1~#6 본문 및 리뷰 코멘트
8. Upstage·모두싸인 대시보드 사용량/잔량 스크린샷
9. 발표 자료 초안 (있는 경우)
10. 대회 운영진 Q&A 채널의 특별상 Task 관련 답변
11. 팀원별 실제 가용 시간 (4일간)
12. `docs/` 인터뷰 원본 (사용자 검증 항목 판정에 필요)

---

## 백분율 추정 근거

| 지표 | 추정 | 산정 근거 | 확인 불가로 제외한 항목 |
|---|---|---|---|
| **기능 완성도** | **72%** | §5의 33개 기능 중 ✅완료·검증 16개(48%) + 🟡구현·미검증 7개(0.6 가중) + 🟠부분 4개(0.4) + ⚫미연결 3개(0.1) + ❌미구현 3개 | 실사진 추출 정확도, 실발송 결과 |
| **배포 준비도** | **45%** | 백엔드 배포 ✅(30%) + Dockerfile 완비 ✅(10%) + `/health` 진단 ✅(5%) − CORS 미설정 − 프론트 미배포 − DB 미연결 | `npm run build`, Railway PDF |
| **대회 준비도** | **40%** | 코드 저장소 ✅(15%) + AI 증빙 부분 ✅(10%) + 에이전트 지침 ✅(10%) + 커밋 기간 ✅(5%) − 배포 링크 ❌ − 데모 영상 ❌ − 발표 자료 ❌ | 특별상 Task 부합성 |
| **예선 예상 점수** | **52~62 / 80** | §7-1 항목별 합산 | — |
| **코드 품질 평균** | **7.2 / 10** | §8 18개 항목 평균 (중복코드 5·입력검증 5·보안 5·배포 4가 하방 압력) | — |

**모든 백분율과 점수는 분석상 추정치이며 공식 채점 결과가 아니다.** 확인하지 못한 항목(§3)은 산정에서 제외했거나 보수적으로 처리했다.

---

*이 보고서는 파일을 수정하지 않고 작성했다. 실행한 검증은 §3의 V1~V11이며, 실패한 항목은 실패 사실과 원인을 그대로 기록했다.*
