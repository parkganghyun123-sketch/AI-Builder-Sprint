# FairSign 디자인 가이드

> 이 문서는 `feat/web`의 프론트엔드 스타일을 Google Material Design 3(M3)에 맞춰
> 유지·확장하기 위한 단일 디자인 기준입니다.
>
> 기준 스냅샷: `feat/web` 커밋 `f939f2a`
>
> 작성 기준일: 2026-07-28
>
> 적용 대상: `web/`의 모든 사용자 화면과 공통 컴포넌트

## 1. 문서 우선순위

Material Design은 제품 디자인 가이드이며 법률·안전 정책을 대신하지 않습니다. 문서가
충돌하면 다음 순서를 따릅니다.

1. `AGENTS.md` — 개인정보, 안전, 협업 규칙
2. `KB.md` — 허용 문구, 판정 범위, 문서 상태
3. `README.md` — 제품 범위와 사용자 흐름
4. `DESIGN.md` — 시각·상호작용 규칙
5. `feat/web` 구현 — 현재 화면의 참고 기준

현재 구현이 상위 문서와 다르면 상위 문서에 맞게 수정합니다. `DESIGN.md`만으로 법정
기준이나 사용자에게 보이는 법률 설명을 새로 만들지 않습니다.

## 2. 외부 기준

FairSign 웹은 다음 기준을 적용합니다.

- [Material Design 3](https://m3.material.io/) — 색상 역할, 타입, 형태, 컴포넌트
- [M3 interaction states](https://m3.material.io/foundations/interaction/states/overview) —
  enabled, hover, focus, pressed, disabled 상태와 복수의 시각 신호
- [M3 canonical layouts](https://m3.material.io/foundations/layout/canonical-examples/overview) —
  compact, medium, expanded 화면 대응
- [Google accessibility guidance](https://developer.android.com/guide/topics/ui/accessibility/apps) —
  48dp 터치 영역과 색상 대비
- [WCAG 2.2](https://www.w3.org/TR/WCAG22/) — 웹 접근성 AA 구현 기준

Google의 48dp 터치 영역은 웹에서 최소 `48px × 48px` 상호작용 영역으로 적용합니다.
WCAG 2.2 AA를 최소 완료 기준으로 삼습니다.

## 3. 디자인 원칙

### 3.1 친절하지만 가볍지 않게

- 청록과 하늘색, 큰 라운드 카드, 쉬운 문장을 유지합니다.
- 법률 서비스처럼 위압적이지 않고, 사용자가 내용을 차분히 확인하도록 돕습니다.
- 축하 이모지는 완료 상태에 제한하고, 위험·판정 화면을 장식적으로 만들지 않습니다.

### 3.2 사람이 최종 확인

- AI가 읽은 값은 확정값처럼 보이지 않게 신뢰도와 원문 근거를 표시합니다.
- 모든 추출 항목은 사용자가 수정할 수 있어야 합니다.
- “조건 확정” 같은 되돌리기 어려운 행동 직전에 확인 대상을 명확히 보여줍니다.

### 3.3 근거가 먼저

- 계약서 사실, 법정 기준, 계산 결과, 판정 한계를 서로 다른 구역으로 나눕니다.
- 숫자와 판정은 백엔드 `ValidationReport` 값만 표시합니다.
- 법정 기준에는 적용 연도 또는 적용 기준일과 근거를 함께 표시합니다.

### 3.4 모르는 것은 모른다고 표시

- 누락과 판정 불가를 오류처럼 숨기지 않습니다.
- 색상만으로 상태를 표현하지 않고 아이콘, 레이블, 설명을 함께 제공합니다.
- 실제로 구현되지 않은 보관·삭제·연동은 동작하는 기능처럼 표시하지 않습니다.

### 3.5 모바일 우선

- 계약서 촬영과 확인은 휴대전화 세로 화면을 기본으로 설계합니다.
- 한 손으로 주요 행동을 실행할 수 있도록 주 CTA를 화면 폭에 맞게 제공합니다.
- 데스크톱에서도 읽기 폭을 과도하게 늘리지 않습니다.

## 4. 현재 시각 정체성

`feat/web`에서 가져오는 FairSign의 시각 특징은 다음과 같습니다.

| 항목 | 현재 스타일 | 유지 원칙 |
|---|---|---|
| 브랜드 | 청록 `#22C7C7` → 하늘 `#38BDF8` | 장식·강조용 그라데이션으로 유지 |
| 본문 | 네이비 `#102A43` | 기본 `on-surface`로 유지 |
| 서체 | Pretendard, Inter, Noto Sans KR | Pretendard 우선 유지 |
| 카드 | 흰 배경, 옅은 청록 테두리, 큰 라운드 | M3 surface와 shape token으로 관리 |
| 버튼 | pill 형태 | M3 filled/outlined/text 계층으로 유지 |
| 배경 | 옅은 하늘색에서 흰색으로 이어지는 그라데이션 | 장식 배경으로 유지 |
| 정보 구조 | 최대 768px의 세로형 단계 흐름 | compact 기본 흐름으로 유지 |
| 상태 표현 | 이모지, 칩, 테두리 | 아이콘·텍스트·색 3중 표현으로 보완 |

브랜드 그라데이션은 로고, 장식선, 큰 제목의 일부에만 사용합니다. 본문, 작은 텍스트,
주요 버튼 배경에는 대비가 검증된 단색 역할 토큰을 사용합니다.

## 5. 색상 시스템

### 5.1 M3 역할 토큰

현재 색감을 유지하면서 접근성을 충족하는 목표 토큰은 다음과 같습니다.

| M3 역할 | 목표값 | 용도 |
|---|---:|---|
| `primary` | `#006A6A` | 주 CTA, 활성 단계, 포커스 |
| `on-primary` | `#FFFFFF` | primary 위 텍스트·아이콘 |
| `primary-container` | `#9CF1EF` | 선택, 강조 카드, 완료된 단계 |
| `on-primary-container` | `#002020` | primary-container 위 콘텐츠 |
| `secondary` | `#00658A` | 보조 강조, 링크, 정보 상태 |
| `on-secondary` | `#FFFFFF` | secondary 위 콘텐츠 |
| `secondary-container` | `#C4E7FF` | 정보 카드, 진행 상태 |
| `on-secondary-container` | `#001E2B` | secondary-container 위 콘텐츠 |
| `surface` | `#FFFFFF` | 카드와 입력 표면 |
| `surface-container-low` | `#F0FAFF` | 페이지 구역, 근거 묶음 |
| `surface-container` | `#F8FDFF` | 페이지 배경 |
| `on-surface` | `#102A43` | 기본 본문 |
| `on-surface-variant` | `#405F6B` | 보조 본문, 설명 |
| `outline` | `#6F7979` | 입력 경계, 중요한 구분선 |
| `outline-variant` | `#D8EEF5` | 장식적 카드 경계 |
| `error` | `#BA1A1A` | 처리 실패, 기준보다 낮은 결과 |
| `on-error` | `#FFFFFF` | error 위 콘텐츠 |
| `error-container` | `#FFDAD6` | 오류·기준 미달 카드 배경 |
| `on-error-container` | `#410002` | error-container 위 콘텐츠 |
| `success` | `#006D46` | 지원 항목의 기준 충족, 체결 완료 |
| `on-success` | `#FFFFFF` | success 위 콘텐츠 |
| `success-container` | `#8DF8C1` | 성공 카드 배경 |
| `on-success-container` | `#002113` | success-container 위 콘텐츠 |
| `warning` | `#795900` | 누락, 낮은 추출 신뢰도, 확인 필요 |
| `on-warning` | `#FFFFFF` | warning 위 콘텐츠 |
| `warning-container` | `#FFE08A` | 경고 카드 배경 |
| `on-warning-container` | `#261900` | warning-container 위 콘텐츠 |

`success`와 `warning`은 M3 기본 역할을 보완하는 FairSign custom color role입니다. 색 이름이
아니라 의미로 사용하며, 각 container에는 대응하는 `on-*` 색을 함께 정의합니다.

### 5.2 기존 색상의 사용 제한

현재 색상 대비를 계산하면 다음과 같습니다.

| 조합 | 대비 | 판정 |
|---|---:|---|
| 흰색 / 기존 `brand #22C7C7` | `2.09:1` | 작은 글자와 버튼에 사용 금지 |
| 흰색 / 기존 `brand-sky #38BDF8` | `2.14:1` | 작은 글자와 버튼에 사용 금지 |
| 기존 `ink-muted #627D98` / 흰색 | `4.28:1` | 작은 본문에 사용 금지 |
| 기존 `ink-soft #8FA6BC` / 흰색 | `2.52:1` | 정보 전달 텍스트에 사용 금지 |
| 기존 `brand-deep #0891B2` / `brand-tint` | `3.48:1` | 작은 본문에 사용 금지 |
| 흰색 / 목표 `primary #006A6A` | `6.42:1` | WCAG AA 통과 |

기존 `brand`, `brand-sky`, `ink-soft`는 장식이나 큰 비본문 요소에만 사용합니다. 정보가
담긴 텍스트는 `on-surface`, `on-surface-variant`, `primary`, `secondary` 역할을 씁니다.

### 5.3 색상 사용 규칙

- 본문과 배경의 대비는 최소 `4.5:1`입니다.
- 큰 텍스트와 필수 아이콘·입력 경계는 최소 `3:1`입니다.
- 링크, 상태, 선택 여부는 색 외에 밑줄·아이콘·레이블·형태 중 하나를 추가합니다.
- 빨강은 처리 실패 또는 지원 범위 안에서 확인된 수치 비교 결과에만 사용합니다.
- `UNKNOWN`과 `OUT_OF_SCOPE`를 성공 또는 실패 색으로 표현하지 않습니다.
- 라이트 모드만 지원하는 동안 `color-scheme: light`를 유지하고 다크 모드 지원을
  주장하지 않습니다.

## 6. 타이포그래피

서체는 다음 순서로 사용합니다.

```css
font-family: Pretendard, Inter, "Noto Sans KR", sans-serif;
```

M3의 타입 역할을 FairSign 브랜드에 맞춘 사용자 정의 scale에 다음과 같이 매핑합니다.
아래 크기와 굵기는 M3 기본값을 그대로 복제한 값이 아니라 `feat/web`의 시각 계층을
유지하는 FairSign 테마값입니다.

| 역할 | 크기/행간 | 굵기 | 사용처 |
|---|---|---:|---|
| `display-large` | `48/56px` | 800 | 넓은 화면 랜딩 hero |
| `display-medium` | `44/52px` | 800 | 모바일 랜딩 hero |
| `headline-large` | `32/40px` | 800 | 화면 제목 |
| `headline-medium` | `28/36px` | 800 | 주요 섹션 제목 |
| `title-large` | `22/28px` | 700 | 결과 요약, 카드 제목 |
| `title-medium` | `16/24px` | 700 | 카드·폼 그룹 제목 |
| `body-large` | `16/24px` | 400~600 | 기본 본문 |
| `body-medium` | `14/20px` | 400~600 | 설명, 목록 |
| `label-large` | `14/20px` | 700 | 버튼, 입력 레이블 |
| `label-small` | `12/16px` | 600~700 | helper, 상태 칩 |

- 본문은 기본 `16px`이며 핵심 설명을 `14px`보다 작게 만들지 않습니다.
- `12px`는 보조 설명과 칩에만 사용하고 대비를 `4.5:1` 이상 유지합니다.
- 사용자가 브라우저 글자를 200% 확대해도 내용과 기능이 손실되지 않아야 합니다.
- 제목은 `tracking-tighter`를 사용할 수 있지만 본문에는 음수 자간을 쓰지 않습니다.
- 숫자는 천 단위 구분을 사용하되, 프론트에서 새 숫자를 계산하지 않습니다.

## 7. 간격, 형태, 높이

### 7.1 간격

4px 배수의 간격 시스템을 사용합니다.

| 토큰 | 값 | 용도 |
|---|---:|---|
| `space-1` | 4px | 아이콘과 짧은 레이블 |
| `space-2` | 8px | 관련 텍스트 |
| `space-3` | 12px | 버튼 그룹, helper |
| `space-4` | 16px | 카드 내부 기본 간격 |
| `space-5` | 20px | 모바일 좌우 여백 |
| `space-6` | 24px | 카드 padding |
| `space-7` | 28px | 화면 블록 간격 |
| `space-8` | 32px | 큰 섹션 간격 |
| `space-12` | 48px | 랜딩 구역 |
| `space-14` | 56px | 랜딩 대형 구역 |

### 7.2 형태

| 역할 | 값 | 현재 매핑 |
|---|---:|---|
| `shape-small` | 8px | 작은 칩 내부 요소 |
| `shape-medium` | 12px | 메시지, 스낵바 |
| `shape-large` | 16px | 입력, 상태 카드 |
| `shape-extra-large` | 32px | FairSign 대표 카드 |
| `shape-full` | 9999px | 버튼, pill, 단계 칩 |

32px 카드는 FairSign의 M3 Expressive 시각 특징으로 유지합니다. 한 화면의 모든 요소를
32px 카드로 감싸지 않고, 관련 정보 묶음에만 사용해 계층을 만듭니다.

### 7.3 상호작용 높이

- 버튼과 링크형 버튼: 최소 높이 `48px`
- 텍스트 필드: 최소 높이 `56px`
- 체크박스·아이콘 버튼: 시각 크기와 무관하게 클릭 영역 최소 `48px × 48px`
- 인접 터치 대상 사이: 최소 `8px`

## 8. 레이아웃과 반응형

### 8.1 화면 구간

| 구간 | 너비 | 규칙 |
|---|---:|---|
| compact | `< 600px` | 한 열, 좌우 20px, CTA 전체 폭 |
| medium | `600~839px` | 카드 2열 허용, 폼은 한 열 우선 |
| expanded | `≥ 840px` | 필요할 때 supporting pane 사용 |

기본 업무 흐름은 현재와 같이 `max-width: 768px`의 중앙 정렬 세로 레이아웃을 유지합니다.
검증 결과에서 계약서 사실과 법정 근거를 동시에 비교할 필요가 있을 때만 expanded
화면에서 최대 `1120px`의 supporting pane을 허용합니다.

### 8.2 공통 셸

- 상단 앱 바 높이: `64px`
- 본문 좌우 여백: compact `20px`, medium 이상 `24px`
- 본문 상단 여백: `32px`
- 본문 하단 여백: 최소 `64px`
- 앱 바는 sticky로 유지하되 포커스된 요소를 가리지 않아야 합니다.
- 긴 문장은 콘텐츠 폭을 제한하고 줄 길이를 대략 45~75자로 유지합니다.

### 8.3 단계 표시

- 단계는 `업로드 → 확인·수정 → 결과 → 수정본 → 서명 → 보관` 순서를 유지합니다.
- `/complete`는 독립된 7번째 단계가 아니라 5단계 “서명”의 완료 확인 화면입니다.
  `/complete`에서는 “6. 보관”을 활성화하지 않고 서명 완료 상태를 표시하며, 실제
  `/archive`에 진입했을 때만 6단계를 활성화합니다.
- `<ol aria-label="계약서 처리 단계">`를 사용합니다.
- 현재 단계에는 `aria-current="step"`을 제공합니다.
- 현재, 완료, 예정 상태는 색과 함께 레이블 또는 아이콘 형태로 구분합니다.
- compact 화면에서 단계가 두 줄 이상 복잡해지면 “3/6 결과”와 선형 진행 표시로
  축약할 수 있습니다.

## 9. 컴포넌트 규칙

### 9.1 Top app bar

- 왼쪽에 FairSign 브랜드, 오른쪽에 보관함처럼 현재 사용 가능한 단일 행동을 둡니다.
- 브랜드의 `✓`는 장식이면 스크린 리더에서 숨깁니다.
- sticky 배경은 콘텐츠와 구분되도록 불투명도와 하단 경계를 함께 제공합니다.

### 9.2 Button

| FairSign variant | M3 대응 | 용도 |
|---|---|---|
| `primary` | Filled button | 다음 단계, 제출, 서명 요청 |
| `secondary` | Outlined button | 수정, 미리보기, 보조 행동 |
| `ghost` | Text button | 뒤로, 취소, 낮은 우선순위 |

- 한 화면 또는 카드에는 원칙적으로 주 CTA 하나만 둡니다.
- `enabled`, `hover`, `focus-visible`, `pressed`, `loading`, `disabled`를 구현합니다.
- primary는 `primary/on-primary` 조합을 사용합니다. 기존 밝은 청록+흰색 조합은 금지합니다.
- 포커스는 최소 2px 외곽선과 3:1 대비를 제공합니다.
- 로딩 중에는 레이블을 유지하면서 진행 표시를 추가하고 중복 제출을 막습니다.
- “구현 예정” 버튼은 실제 기능처럼 보이는 enabled 상태로 배포하지 않습니다.

### 9.3 Card

- 기본 카드는 `surface`, `outline-variant`, `shape-extra-large`를 사용합니다.
- 클릭 가능한 카드 전체에 키보드 포커스를 제공하고 목적을 설명하는 링크 이름을 둡니다.
- 그림자는 계층 보조 수단이며 선택·오류·상태를 그림자만으로 표현하지 않습니다.
- 중첩 카드는 최대 2단계까지만 허용합니다.

### 9.4 Outlined text field

- 시각적 placeholder와 별도로 항상 보이는 `<label>`을 둡니다.
- 필수 여부, 단위, 형식 예시를 레이블·helper로 제공합니다.
- 오류는 `aria-invalid`, `aria-describedby`와 텍스트 설명으로 연결합니다.
- 날짜, 시간, 금액, 이메일에는 가능한 경우 의미에 맞는 `type`과 `inputMode`를 씁니다.
- 값 입력만으로 다음 화면으로 자동 이동하지 않습니다.
- `LOW`와 `NOT_FOUND` 상태는 아래 신뢰도 규칙을 따릅니다.

### 9.5 Upload area

- 실제 `<input type="file">`을 사용하고 키보드로 실행할 수 있어야 합니다.
- 허용 형식, 최대 크기, 한 장 제한을 선택 전에 표시합니다.
- 선택 전에 이미지의 처리 목적과 “보관 기간과 삭제 동작은 아직 검증되지 않았습니다
  (`UNVERIFIED`).”를 표시합니다.
- 선택한 이미지의 이름·미리보기·다시 선택 행동을 제공합니다.
- 처리 중에는 진행 표시와 “계약서를 읽고 있어요” 같은 상태 문구를 함께 제공합니다.
- 실패 시 원인, 재시도, 직접 입력 대안을 제공합니다.
- 자동 삭제가 검증되기 전에는 삭제를 보장하는 문구를 표시하지 않습니다.

### 9.6 Progress indicator

- 업로드·추출처럼 시간이 걸리는 작업에는 M3 linear 또는 circular progress를 사용합니다.
- 진행률을 모르면 determinate percentage를 만들지 않습니다.
- 상태 문구 영역에는 `aria-live="polite"`를 사용합니다.
- 10초 이상 걸릴 수 있는 작업은 취소 또는 안전한 이탈 방법을 제공합니다.

### 9.7 Snackbar, dialog, inline message

- 짧은 성공·재시도 알림은 snackbar를 사용합니다.
- 입력 오류와 확인 필요 상태는 해당 요소 가까이에 inline message로 표시합니다.
- 서명 요청처럼 외부 상태를 바꾸는 행동은 필요할 때 확인 dialog를 사용합니다.
- dialog의 초점은 내부에 유지하고 닫은 뒤 호출 요소로 되돌립니다.

### 9.8 Legal disclaimer

결과, 수정본, 서명, 완료, 보관 화면에는 다음 문구를 항상 표시합니다.

> 이 서비스는 법정 기준을 자동으로 계산해 알려드립니다. 법률 자문이 아니며,
> 개별 상황은 고용노동부 고객상담센터(1350) 또는 전문가와 상담하시기 바랍니다.

본문보다 작게 표시할 수 있지만 접을 수 없고, `12px` 미만 또는 낮은 대비로 숨기지
않습니다.

## 10. 상태 표현

### 10.1 검증 상태

백엔드 코드 값은 유지하되 사용자 레이블은 다음처럼 표시합니다.

| 코드 | 사용자 레이블 | 색상 역할 | 아이콘 예 | 금지 문구 |
|---|---|---|---|---|
| `OK` | 기준 충족 | `success-container` | 체크 | 정상 계약, 합법 |
| `VIOLATION` | 기준보다 낮음 | `error-container` | 경고 삼각형 | 불법, 위법 사업주 |
| `MISSING` | 계약서에서 찾지 못함 | `warning-container` | 물음표 문서 | 법 위반 확정 |
| `UNKNOWN` | 정보 부족으로 확인 불가 | `surface-container` | 정보 | 문제없음 |
| `OUT_OF_SCOPE` | 계약서만으로 판단할 수 없음 | `secondary-container` | 안내 | 신고 가능/불가능 |

- 코드 이름 `VIOLATION`을 화면 레이블로 그대로 노출하지 않습니다.
- “모두 정상” 대신 “FairSign이 지원하는 항목에서 기준 미달을 찾지 못했습니다”라고
  범위를 한정합니다.
- `UNKNOWN`과 `OUT_OF_SCOPE`는 사용자가 보완할 정보 또는 공식 상담 안내를 제공합니다.
- `OUT_OF_SCOPE`는 현재 `ValidationReport.CheckStatus` 값이 아니라 챗봇·정책 라우팅
  상태입니다. 검증 API 타입에 임의로 추가하지 않습니다.

### 10.2 추출 신뢰도

| 값 | 표시 | 행동 |
|---|---|---|
| `HIGH` | 기본 입력 | 사용자가 그대로 확인하거나 수정 |
| `LOW` | warning 테두리 + “확인 필요” + 원문 근거 | 명시적 확인 유도 |
| `NOT_FOUND` | 빈 입력 + “계약서에서 확인되지 않습니다” | 직접 입력 유도 |

- `LOW`를 노란 배경만으로 표현하지 않습니다.
- `NOT_FOUND`에 추정값을 placeholder나 default value로 넣지 않습니다.
- `source_text`는 계약서 사실 근거로 표시하고 법정 근거와 분리합니다.

### 10.3 문서 상태

| 코드 | 필수 레이블 | 시각 역할 | 필수 통제 |
|---|---|---|---|
| `DRAFTING` | 작성 중 | neutral | 발송·합의로 표현 금지 |
| `REVIEW_REQUESTED` | 근로조건 확인 요청서 | warning | “확인 전 초안” 워터마크 |
| `TERMS_CONFIRMED` | 조건 확인됨 · 서명 전 | secondary | 체결 완료로 표현 금지 |
| `ON_PROCESSING` | 처리 중 | secondary | 완료로 표현 금지 |
| `ON_GOING` | 서명 진행 중 | secondary | 아직 체결 전임을 표시 |
| `COMPLETED` | 체결 완료 | success | 제공자 완료 상태 확인 후에만 |
| `ABORTED` | 중단됨 | neutral | 재시도·새 요청 안내 |
| `PROCESSING_FAILED` | 처리 실패 | error | 원인·재시도 안내 |

문서 상태는 색, 아이콘, 제목, 설명을 함께 표시합니다. 서명 전 문서는 “계약서”가 아니라
“근로조건 확인 요청서”로 부릅니다.

## 11. 화면별 규칙

| 라우트 | 목적 | 필수 UI | 안전·접근성 기준 |
|---|---|---|---|
| `/` | 두 진입 경로 선택 | hero, 경로 카드 2개, 서비스 흐름 | “정상 계약” 단정 금지, 카드 링크 목적 명확화 |
| `/upload` | 사진 선택·추출 | 파일 입력, 미리보기, 진행, 재시도 | 이미지 처리·보관 범위 안내, 직접 입력 대안 |
| `/review` | 사람이 값 최종 확인 | 편집 폼, 신뢰도, 원문 근거 | 모든 필드 수정 가능, 누락 추정 금지 |
| `/result` | 검증 결과와 근거 표시 | 요약, 사실/기준 분리, 결과 카드 | 기준일·계산식·한계, 숫자 프론트 계산 금지 |
| `/contract` | 수정본 확인 | 변경 비교, PDF 미리보기, 문서 상태 | 서명 전 명칭, 경로 B 초안 워터마크, 법적 효력 단정 금지 |
| `/sign` | 서명 요청 | 이메일, 문제 확인, 요청 상태 | 연락처 목적 안내, 409 확인 흐름, 중복 제출 방지 |
| `/complete` | 5단계 서명 완료 확인 | 검증된 상태, 다운로드 | `COMPLETED` 확인 전 완료 표현 금지, 6단계 보관 활성화 금지 |
| `/archive` | 체결 문서·판정 이력 | 목록, 상태, 열기·다운로드 | 실제 저장 API 없으면 mock/미지원 표시, 개인정보 마스킹 |

### 11.1 랜딩 권장 문구

현재 `feat/web`의 밝고 친근한 hero 스타일은 유지하되 “정상”이라는 단정 표현은
사용하지 않습니다.

- 권장 제목: **“사인하기 전에, 계약 조건을 확인해볼까요?”**
- 권장 설명: **“계약서에서 읽은 조건을 확인하고, 지원하는 법정 기준과 비교해
  근거와 함께 보여드려요.”**
- 금지 제목: “이 계약은 정상인가요?”, “불법 계약 잡아드립니다”

### 11.2 결과 요약

- 계약서 사실 카드와 법정 기준 카드는 제목·배경·아이콘을 달리합니다.
- 예상 월급과 차액은 백엔드 값이 `null`이면 카드 자체를 숨기거나 “계산하지 않음”으로
  설명합니다. `—원`처럼 값이 있는 것처럼 표시하지 않습니다.
- 모든 결과 카드에는 가능한 경우 계약서 근거, 법령 근거, 계산식, 한계를 제공합니다.
- 주휴 관련 결과는 시간 요건만 확인했다는 한계를 항상 함께 표시합니다.

### 11.3 위험이 남은 상태에서 진행

- `VIOLATION` 또는 `MISSING`이 있으면 서명 요청 전에 항목 목록을 다시 보여줍니다.
- “알고도 진행” 체크는 기본 선택하지 않습니다.
- 체크박스 레이블에는 무엇을 확인했는지 명시하고, 법적 권리를 포기한다는 의미로
  표현하지 않습니다.
- 백엔드 409 응답을 성공 화면으로 우회하지 않습니다.

## 12. 콘텐츠와 문구

### 12.1 반드시 분리할 정보

1. 계약서에서 확인된 사실
2. 적용 법정 기준과 기준일
3. 백엔드가 수행한 비교 또는 계산
4. 계약서만으로 확인할 수 없는 한계
5. 사용자가 다음에 할 수 있는 행동

### 12.2 문체

- 존댓말과 쉬운 한국어를 사용합니다.
- 한 문장에는 핵심 행동 하나만 담습니다.
- 사용자를 탓하거나 사업주의 고의를 추정하지 않습니다.
- “AI가 판단했습니다” 대신 “계약서에서 읽은 값”, “검증 코드가 비교한 결과”라고
  정확히 구분합니다.
- 사업주 화면에는 “알바생이 입력한 내용입니다. 사실과 다르면 수정해 주세요.”를
  고정 표시합니다.

### 12.3 금지 표현과 대체 표현

| 금지 | 대체 |
|---|---|
| 정상 계약 | 지원하는 항목에서 기준 미달을 찾지 못했습니다 |
| 불법 계약 / 위법 사업주 | 확인된 값이 적용 기준보다 낮습니다 |
| 주휴수당 대상입니다 | 주휴 관련 시간 요건을 충족합니다 |
| 이 계약은 효력이 있습니다 | 서명 상태가 체결 완료로 확인되었습니다 |
| 아직 서명 전이라 계약 효력은 없어요 | 아직 서명 전이며, 체결 완료 상태가 확인되지 않았습니다 |
| 제대로 된 계약서 만들기 | 확인한 조건으로 근로조건 확인 요청서 만들기 |
| 자동 삭제됩니다 | 보관 기간과 삭제 동작은 아직 검증되지 않았습니다 (`UNVERIFIED`) |
| 안전하게 보관됩니다 | 보관 기능이 구현·검증된 경우에만 구체적으로 설명 |
| AI가 법을 판정합니다 | AI는 문서를 읽고, 검증 코드는 기준과 비교합니다 |

## 13. 개인정보 UI

- 주민등록번호와 주소는 검토된 연동이 요구하지 않는 한 수집하지 않습니다.
- 이름과 연락처는 필요한 단계에서만 요청합니다.
- 이메일·연락처 입력 전에 사용 목적을 표시합니다.
- 발송 후 화면에서는 연락처를 마스킹합니다.
- 계약서 이미지, 이름, 연락처를 toast·오류 상세·분석 이벤트에 포함하지 않습니다.
- 이미지 보관 기간과 삭제 구현이 검증되기 전에는 자동 삭제를 약속하지 않습니다.
- 데모, 스크린샷, mock 데이터에는 가상 계약과 가상 인물만 사용합니다.
- 외부 연동이 mock이면 화면과 데모 가이드에 mock임을 표시합니다.

## 14. 접근성 완료 기준

- 모든 기능을 키보드만으로 순서에 맞게 사용할 수 있습니다.
- 모든 상호작용 요소에 보이는 `focus-visible` 상태가 있습니다.
- 텍스트 대비 `4.5:1`, 필수 그래픽·경계 대비 `3:1`을 충족합니다.
- 클릭·터치 영역은 최소 `48px × 48px`입니다.
- 상태를 색상 하나로만 전달하지 않습니다.
- 아이콘 버튼은 접근 가능한 이름을 갖습니다.
- 장식 이모지와 아이콘은 스크린 리더에서 숨깁니다.
- 입력에는 영구 레이블, 설명, 오류 연결이 있습니다.
- 비동기 상태는 `aria-live`로 알리고 초점을 갑자기 이동하지 않습니다.
- 페이지마다 고유한 제목과 하나의 주 `h1`이 있습니다.
- `<html lang="ko">`를 유지합니다.
- 200% 확대와 320px 너비에서 가로 스크롤 없이 핵심 흐름이 동작합니다.
- `prefers-reduced-motion`에서는 불필요한 전환·애니메이션을 제거합니다.
- 드래그나 스와이프만으로 가능한 필수 행동을 만들지 않습니다.

## 15. 구현 매핑

`feat/web`의 현재 이름은 다음 M3 역할로 이전합니다.

| 현재 Tailwind 토큰 | 목표 역할 |
|---|---|
| `brand` | 장식용 `brand-accent`; CTA에는 `primary` 사용 |
| `brand-sky` | 장식용 `brand-accent-secondary` |
| `brand-deep` | `primary` 또는 `secondary` |
| `brand-tint` | `surface-container-low` 또는 `primary-container` |
| `brand-line` | `outline-variant`; 입력에는 `outline` 사용 |
| `ink` | `on-surface` |
| `ink-muted` | `on-surface-variant` |
| `ink-soft` | 장식용 또는 대비가 검증된 보조 토큰 |
| `status-*` | 의미 기반 custom role과 container/on-container 쌍 |

현재 공통 컴포넌트의 책임은 유지합니다.

- `components/ui.tsx` — Button, Card, Pill, SectionLabel
- `components/ScreenShell.tsx` — 앱 바, 진행 단계, 화면 제목
- `components/FieldInput.tsx` — 추출 신뢰도와 원문 근거
- `components/CheckResultCard.tsx` — 판정 결과와 근거
- `components/DocumentStatusBadge.tsx` — 문서 상태
- `components/LegalDisclaimer.tsx` — 상시 안전 문구

현재 구현은 shadcn/ui 패키지 대신 자체 React 프리미티브를 사용합니다. M3 준수를 위해
특정 라이브러리를 강제하지 않으며, 컴포넌트의 의미·상태·접근성·토큰 준수를 검증합니다.

### 15.1 `feat/web` 기준 스냅샷의 미준수 항목

이 문서는 `feat/web`의 시각 스타일을 기준으로 하지만, 커밋 `f939f2a`가 이 문서를 이미
준수한다는 뜻은 아닙니다. 다음 항목은 배포 전에 수정·연동 검증이 필요합니다.

- 랜딩의 “이 계약 정상일까요?”를 11.1의 권장 문구로 교체
- 화면용 “정상/위반” 레이블을 10.1의 제한된 표현으로 교체
- “법정 기준을 모두 충족해요”의 지원 범위를 한정
- “제대로 된 계약서 만들기”를 “근로조건 확인 요청서 만들기”로 교체
- “계약 효력은 없어요” 같은 긍정·부정형 법적 효력 단정을 제거
- 데모 링크만으로 `/complete`에 진입해 `COMPLETED`를 표시하지 않도록 제공자 상태 확인
- mock 보관 목록을 실제 저장·증빙 기능처럼 설명하지 않도록 명시
- 기존 밝은 청록 버튼과 보조 텍스트의 대비를 5장의 목표 토큰으로 교정

## 16. 모션과 피드백

- 기본 전환 시간은 `150~300ms` 범위로 제한합니다.
- hover, press, focus는 M3 state layer처럼 일관되게 적용합니다.
- 페이지 전환 애니메이션이 작업 완료보다 먼저 완료된 것처럼 보이면 안 됩니다.
- 로딩을 성공 아이콘으로 대체하지 않습니다.
- 전자서명·업로드 상태는 백엔드 응답과 일치할 때만 변경합니다.
- 사용자의 입력 오류는 보존하고, 오류 수정 후 다시 제출할 수 있게 합니다.

## 17. 금지하는 디자인

- 흰 글자를 기존 밝은 청록 또는 하늘색 배경에 올린 작은 버튼
- 색상만 다른 상태 칩
- placeholder만 있고 레이블이 없는 입력
- 실제 기능이 없는 enabled 버튼
- mock 데이터인데 실제 사용자 데이터처럼 보이는 화면
- `UNKNOWN`을 성공으로 포함한 전체 성공 요약
- 서명 전 문서를 체결된 계약서처럼 보이게 하는 제목·초록색·축하 효과
- 보관·자동 삭제가 검증되지 않았는데 완료를 보장하는 문구
- 사용자가 체크하지 않은 “알고도 진행” 기본 선택
- 법률 판단을 단정하는 빨간 경고 배너
- 키보드 포커스가 보이지 않는 링크·카드·버튼

## 18. 검수 체크리스트

### 디자인

- [ ] `feat/web`의 청록·하늘·네이비 정체성을 유지했다
- [ ] 모든 색을 역할 토큰으로 사용했다
- [ ] 모바일 320px부터 expanded 화면까지 확인했다
- [ ] 한 화면의 주 CTA가 명확하다
- [ ] loading, empty, error, retry, disabled 상태가 있다

### 안전

- [ ] 계약서 사실, 법정 기준, 계산, 한계를 구분했다
- [ ] “정상/불법/효력” 같은 단정 문구를 제거했다
- [ ] 주휴 결과를 시간 요건으로 한정했다
- [ ] 문서 상태와 워터마크가 `KB.md`와 일치한다
- [ ] 개인정보 보관·삭제를 구현 이상으로 약속하지 않는다
- [ ] mock과 미연동 상태를 숨기지 않는다

### 접근성

- [ ] 텍스트 대비 4.5:1, 비텍스트 대비 3:1을 확인했다
- [ ] 터치 영역이 48px 이상이다
- [ ] 키보드 이동과 focus-visible을 확인했다
- [ ] 아이콘·상태에 텍스트 대안이 있다
- [ ] 폼 레이블과 오류 설명이 프로그램적으로 연결된다
- [ ] 200% 확대와 reduced motion을 확인했다

### 구현 검증

- [ ] `cd web && npm run typecheck`
- [ ] `cd web && npm run build`
- [ ] ESLint 설정 파일이 존재할 때 `cd web && npm run lint`
- [ ] ESLint 설정이 없으면 lint를 통과로 표시하지 않고 `미구성`으로 보고
- [ ] 실제 API와 mock 상태를 구분해 확인
- [ ] 업로드 실패·409·서명 API 실패·재시도 흐름 확인
- [ ] 가상 계약과 가상 인물로 데모 확인

실행하지 않은 검사는 완료로 표시하지 않습니다.

## 19. 변경 절차

1. 스타일 변경 전에 이 문서의 역할 토큰과 컴포넌트 규칙을 확인합니다.
2. 사용자 문구가 바뀌면 `KB.md`와 충돌하지 않는지 확인합니다.
3. 법령 문구, 문서 상태, 개인정보 문구 변경은 계약 안전 검토를 요청합니다.
4. 공통 토큰 변경은 모든 화면의 대비와 상태를 함께 확인합니다.
5. 데모 핵심 화면 변경은 전체 흐름과 실패 상태를 QA 검토합니다.
6. 구현과 문서가 다르면 실제 동작을 확인한 뒤 문서를 함께 갱신합니다.
