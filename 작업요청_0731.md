# 작업 요청 — 2026-07-31

> 마감: **2026-08-03(월) 18:00**
> 담당: **A** AI 파이프라인 · **B** 검증 엔진 · **C** 백엔드·배포·팀장 · **D** 프론트엔드
> 이 문서는 지금 남은 것만 담습니다. 배경은 [분석보고서_2026-07-30.md](분석보고서_2026-07-30.md) 참고.

---

## 0. 지금 상태 — 여기서 시작합니다

**끝난 것 (재작업 불필요)**

| 영역 | 상태 |
|---|---|
| 웹에서 사진→판정→서명 완주 | ✅ |
| Vercel · Railway 배포, CORS, 웹훅 토큰 | ✅ |
| 업로드 크기·MIME, 이메일·이름 검증 | ✅ |
| **계약 이력 Supabase 영속 저장** | ✅ |
| **말 꺼내기 문구 UI** (서사 ②) | ✅ |
| **사업주 경로** (경로 C) — 서명 순서 역전, 경로별 제목·출처 | ✅ |
| **카카오 로그인 백엔드 + 소유자 기반 인가** | ✅ |
| **보관함 API** (`GET /contracts`) | ✅ |
| 테스트 268 · ruff 0 · tsc 0 | ✅ |

**백엔드 API는 전부 준비됐습니다.** 남은 것의 대부분은 프론트 연결과 제출물입니다.

```
GET    /health                       배포 진단 (아래 참고)
GET    /auth/login-url               카카오 로그인 주소
POST   /auth/kakao/callback          인가 코드 → 세션 토큰
GET    /auth/me                      현재 사용자
PATCH  /auth/me/role                 역할 변경
POST   /contracts/extract            사진 → 조건            [로그인 불필요]
POST   /contracts/review-items       확인 필요 항목          [로그인 불필요]
POST   /contracts/validate           법정 기준 판정          [로그인 불필요]
POST   /contracts/validation-state   진행 차단 판정          [로그인 불필요]
POST   /contracts/message            말 꺼내기 문구          [로그인 불필요]
POST   /contracts/preview            PDF 미리보기           [로그인 불필요]
POST   /contracts/analyze-sign       검증→PDF→서명 발송      [로그인 필요]
GET    /contracts                    보관함 목록            [로그인 필요]
GET    /contracts/{id}/status        문서 상태              [로그인 필요]
```

**배포 확인은 `/health` 하나로 합니다.** 6개 신호가 전부 켜져야 정상입니다.

```json
{"modusign":true, "upstage":true, "store":"postgres",
 "webhook_token":true, "kakao_login":true, "cors_origins":[... vercel 도메인 ...]}
```

---

# D — 프론트엔드

> **D의 작업이 지금 가장 큰 병목입니다.** 백엔드는 전부 열려 있는데
> 화면이 없어서 심사자가 볼 수 없는 기능이 3개입니다.

## D-1 【P0 · 3~4h】 로그인 UI

### 배경

백엔드는 준비됐습니다. `analyze-sign`·`GET /contracts`·`GET /contracts/{id}/status`가 **401**을 반환합니다. 지금 프론트는 이 401을 처리하지 못해 서명 단계에서 막힙니다.

**로그인을 요구하는 시점은 서명 발송입니다.** 그 앞(업로드·판정·문구 복사)은 로그인 없이 되어야 합니다. 이건 편의가 아니라 설계입니다 — 열여섯 살이 첫 계약서를 확인하려고 가입부터 해야 한다면 그 벽을 넘지 못합니다. **백엔드 테스트가 이 규칙을 고정하고 있으니 화면에서도 깨지 마세요.**

### 만들 것

**① `web/lib/auth.ts` (신규)**

```ts
const TOKEN_KEY = "fairsign:token:v1";

// ⚠️ sessionStorage 를 쓴다. localStorage 는 탭을 닫아도 남아
//    PC방·공용 PC에서 다음 사람에게 로그인이 넘어간다.
export function readToken(): string | null
export function writeToken(token: string): void
export function clearToken(): void
```

**② `web/lib/api.ts` 확장**

```ts
// 모든 요청에 토큰을 붙인다 (있을 때만)
function authHeaders(): HeadersInit

// 401 을 전용 오류로 승격 — 화면이 "로그인 필요"와 "요청 실패"를 구분해야 한다
export class LoginRequiredError extends ApiError {}

export function getLoginUrl(): Promise<{ authorize_url: string }>
export function completeKakaoLogin(body: {
  code: string; state: string; role: "WORKER" | "EMPLOYER";
}): Promise<{ access_token: string; user: Me }>
export function getMe(): Promise<Me>
export function setRole(role: "WORKER" | "EMPLOYER"): Promise<Me>
export function listMyDocuments(): Promise<ArchiveItem[]>
```

⚠️ **401 응답의 `detail.code`를 구분하세요.**

| code | 뜻 | 화면 |
|---|---|---|
| `LOGIN_REQUIRED` | 토큰 없음 | "로그인하고 계속하기" 버튼 |
| `SESSION_INVALID` | 만료·위조 | "세션이 만료됐어요. 다시 로그인해 주세요" |

둘 다 "요청 실패"로 뭉뚱그리면 사용자가 무엇을 해야 할지 모릅니다.

**③ `web/app/auth/kakao/callback/page.tsx` (신규)**

카카오가 `?code=...&state=...`로 돌려보내는 페이지입니다.

```
1. searchParams 에서 code, state 읽기
2. completeKakaoLogin({ code, state, role })  ← role 은 세션의 현재 역할
3. writeToken(access_token)
4. 로그인 전에 있던 화면으로 복귀 (아래 참고)
5. 실패 시: 사유를 보여주고 "다시 시도" 버튼
```

⚠️ **복귀 경로를 세션에 저장했다가 쓰세요.** 로그인 후 홈으로 튕기면 입력한 조건 20여 개를 다시 채워야 합니다. `sessionStorage`에 `fairsign:return-to` 같은 키로 저장하세요.

⚠️ **`useSearchParams` 는 Suspense 경계가 필요합니다.** `/review` 페이지가 이미 그렇게 되어 있으니 같은 방식으로 감싸세요. 안 하면 `npm run build`가 실패합니다.

**④ `web/app/sign/page.tsx` 게이팅**

로그인하지 않은 상태로 서명 화면에 오면, 폼 대신 로그인 안내를 먼저 보여줍니다.

```
┌─────────────────────────────────────┐
│ 🔐 서명 요청을 보내려면 로그인이 필요해요 │
│                                     │
│ 여기까지는 로그인 없이 확인하셨어요.     │
│ 서명 요청은 상대방에게 실제로 메일이     │
│ 가고, 나중에 이 계약서를 다시 찾을 수    │
│ 있어야 해서 로그인이 필요합니다.        │
│                                     │
│        [카카오로 계속하기]             │
└─────────────────────────────────────┘
```

**⚠️ 왜 로그인이 필요한지 문장으로 밝히세요.** "로그인 필요"만 쓰면 사용자가 이탈합니다. 이 서비스는 로그인을 최대한 늦춘 것이 설계인데, 그 이유가 화면에 안 보이면 그냥 불친절한 서비스가 됩니다.

**⑤ 헤더 로그인 상태**

`web/components/ScreenShell.tsx` 상단에 로그인 상태 표시.
- 로그인 전: `로그인` 버튼
- 로그인 후: `{닉네임}님` + `보관함` 링크 + `로그아웃`

### 완료 조건

- [ ] 로그인 없이 홈→업로드→확인→결과→**문구 복사**까지 완주된다
- [ ] 서명 화면에서 로그인 안내가 뜨고, 로그인 후 **입력값이 그대로 남아 있다**
- [ ] 로그인 후 서명 발송 성공 → `/complete`에 문서 상태가 뜬다
- [ ] 토큰을 임의로 망가뜨리면 "세션이 만료됐어요" 안내가 뜬다
- [ ] `npm run build` 통과

### 검증 방법

```bash
cd web && npm run build && npx tsc --noEmit
```
시크릿 창에서 위 완료 조건 5개를 순서대로 확인.

---

## D-2 【P1 · 2~3h】 보관함 화면

### 배경

`GET /contracts`가 준비됐습니다. **응답에 다운로드 링크는 없습니다** — 유효시간이 10분이라 목록에 실으면 대부분 만료됩니다. 개별 문서를 열 때 `GET /contracts/{id}/status`가 제공자에서 새로 읽습니다.

**응답 형태**
```ts
{
  document_id: string;
  title: string;              // "근로계약서" | "근로조건 확인 요청서"
  status: DocumentStatus;     // 기존 타입 그대로
  signed: number;             // 서명 완료 수
  total: number;              // 전체 참여자 수
  entry_path: EntryPath;      // PHOTO | MANUAL | EMPLOYER
  created_at: string;         // ISO 8601
}[]
```

### 만들 것

`web/app/archive/page.tsx`를 교체합니다. **껍데기는 그대로 두세요** — `ScreenShell step={6}`, `LegalDisclaimer`, 하단 버튼은 유지하고 가운데 "준비 중" 카드만 목록으로 바꿉니다.

```
┌──────────────────────────────────────┐
│ 근로계약서            2026-07-30      │
│ [서명 진행 중]  1/2 서명               │
├──────────────────────────────────────┤
│ 근로조건 확인 요청서   2026-07-28      │
│ [체결 완료]     2/2 서명   [상태 보기] │
└──────────────────────────────────────┘
```

- `DocumentStatusBadge` 컴포넌트를 그대로 재사용하세요
- 로그인 안 했으면 D-1의 로그인 안내 재사용
- 목록이 비었으면: "아직 보낸 계약서가 없어요" + 홈으로 가는 버튼
- 항목 클릭 → `/complete?id={document_id}` 로 이동해 상세 상태·다운로드

⚠️ `/complete` 페이지가 지금은 세션의 `sign.documentId`만 봅니다. 쿼리 파라미터 `?id=`도 받도록 고쳐야 보관함에서 들어올 수 있습니다.

### 완료 조건

- [ ] 서명 발송 후 보관함에 그 문서가 보인다
- [ ] **다른 계정으로 로그인하면 그 문서가 안 보인다** ← 이걸 꼭 확인하세요
- [ ] 항목을 누르면 상태와 다운로드 링크가 나온다
- [ ] 로그아웃 상태에서는 로그인 안내가 뜬다

---

## D-3 【P1 · 0.5h】 이름 불일치 상세 표시

백엔드가 이미 보내고 있는데 화면이 안 씁니다.

```json
409 {"detail": {"code": "NAME_MISMATCH",
  "conflicts": [{"field":"worker_name", "label":"근로자 성명",
                 "on_contract":"김가상", "typed":"김가상아님"}]}}
```

`web/app/sign/page.tsx`의 `signBlock` 렌더링에 `conflicts`를 추가하세요.

```
┌─────────────────────────────────────┐
│ ↩️ 계약서 이름과 입력한 이름이 달라요   │
│                                     │
│  계약서에서 읽음:  김가상             │
│  입력하신 이름:    김가상아님          │
│                                     │
│  어느 쪽이 맞나요?                    │
└─────────────────────────────────────┘
```

⚠️ **어느 쪽이 맞는지 코드가 고르지 않습니다.** 백엔드가 일부러 판단을 사용자에게 되돌리는 설계입니다(`contracts.py:_name_conflicts` 주석). 두 값을 나란히 보여주지 않으면 그 의도가 화면에서 끊깁니다.

스키마가 `.passthrough()`라 데이터는 이미 도착합니다. **렌더만 추가하면 됩니다.**

---

## D-4 【P2 · 0.5h】 입력값 차단 상세 표시

`422 INVALID_CONTRACT_VALUES` 응답에 `blocking_fields`와 `issues`가 들어 있는데 화면은 "입력한 값을 확인해 주세요"만 보여줍니다. `issues[].reason` / `issues[].fix`를 그대로 표시하고 `field`로 포커스를 옮기세요.

---

# A — AI 파이프라인

## A-1 【P0 · 1h】 손글씨 반복 측정 실행 + 산출물 커밋

### 왜 지금 이게 P0인가

**시연을 "잘 읽히는 예쁜 손글씨"로 할 예정입니다.** 그러면 심사에서 반드시 이 질문이 옵니다.

> "그 사진만 되는 거 아닌가요?"

측정값을 숨기면 시연이 마술처럼 보이고, 공개하면 설계가 정당해집니다. **예쁜 손글씨로 시연할수록 실패 케이스 측정이 더 필요합니다.**

```bash
cd ~/AI-Builder-Sprint
set -a; source .env; set +a
python3 spikes/check_extract.py spikes/fixtures --runs 3
```

### 완료 조건

- [ ] `spikes/fixtures/*_runs.json` 커밋
- [ ] 요약 3줄 작성: "N회 중 정확 M건 / 호출 간 값 흔들림 K건 / 가장 자주 틀린 항목"
- [ ] **정확도가 낮게 나와도 그대로 기록** — 사용자 확인 단계가 있어 OCR 정확도가 급소가 아니라는 게 이 제품의 논리입니다(README:97-101). 낮은 수치도 그 논리의 근거가 됩니다

### 선행

**C-4(개인정보 fixture 방침)를 먼저 결정해야 합니다.** 지금 fixture에 실명이 들어 있습니다.

---

## A-2 【P0 · 2h】 AI 활용 기술서

OT 요구 항목: **모델 · API 사용 위치 · 프롬프트/설정 · 테스트·검증 산출물**

| 항목 | 근거 위치 |
|---|---|
| 모델 | Upstage `document-parse`, `information-extract` |
| API 호출 | `app/ai/document_parse.py:31`, `app/ai/extract.py:452` |
| 추출 스키마 | `extract.py:88 build_extraction_schema()` |
| 신뢰도 임계값 | `extract.py:249 CONFIDENCE_THRESHOLD = 0.90` |
| 코드 방어층 | `:313 apply_sanity_check`, `:200 _looks_like_form_label`, `:176 _CHECKMARK` |
| 근거 문장 매칭 | `:411 _find_source_text` |
| 검증 산출물 | `app/evaluation/results.json` (230/230) + `spikes/fixtures/*_runs.json` |
| **AI를 쓰지 않은 곳** | `app/validation/` — 판정·계산 전부 |

⚠️ **마지막 줄이 가장 중요합니다.** "AI를 어디에 안 썼는가"가 이 프로젝트의 차별점입니다. 기술서에 반드시 명시하세요.

---

## A-3 【P1 · 8~11h】 계약 비서 챗봇

### 범위 (팀장 확정)

일반 노동법 Q&A + "제가 주휴수당 받을 수 있나요?" 같은 기본 상담까지.

### ⚠️ 반드시 지킬 것 — 프롬프트로는 부족합니다

README:524가 못 박았습니다.

> **후처리 숫자 검사 없는 챗봇은 배포하지 않습니다** — 안전장치 없는 챗봇은 없느니만 못합니다

AGENTS.md도 마찬가지입니다.

> 법정 계산은 결정론적으로 수행합니다. **LLM이 판단하거나 계산하게 두지 않습니다.**
> 근거가 없거나 다툼이 있는 상황은 `OUT_OF_SCOPE`로 처리합니다.
> **확정적인 법률 판단을 내리지 않습니다.**

### 구조 — LLM 출력을 코드가 3단으로 검사

```
질문 + (있으면) 판정 결과
   ↓
Solar 답변 생성   ← 컨텍스트는 KB.md 발췌만
   ↓
① 출처 검증   KB 출처 ID(SRC-LSA-55 등) 인용 없으면 폐기 → OUT_OF_SCOPE
② 숫자 검증   app/bridge/numbers.py 재사용.
              KB 상수·ValidationReport 에 없는 숫자가 있으면 폐기
③ 확정 결론 탐지  "받을 수 있습니다 / 신고 가능합니다 / 부당해고입니다 /
                 위법입니다" 같은 단정 표현 차단
   ↓
통과한 답변만 표시 + 1350 안내 상시
```

**이 3단이 발표의 핵심 소재입니다.** "LLM을 썼지만 코드가 검사한다"를 실물로 보여줄 수 있습니다.

### 만들 파일

```
backend/app/assistant/
  __init__.py
  knowledge.py    KB.md 를 출처 ID 단위로 쪼개 검색
  solar.py        Upstage Solar 호출 (UPSTAGE_API_KEY 재사용)
  guards.py       ①②③ 후처리 검증
  service.py      조합
backend/app/routers/assistant.py    POST /assistant/ask
backend/tests/test_assistant_guards.py
web/components/AssistantPanel.tsx   결과 화면 하단 또는 별도 탭
```

### 주휴수당 질문을 어떻게 답하는가 (설계 예시)

> **Q. 제가 주휴수당 받을 수 있나요?**
>
> A. 판정 결과로는 **시간 요건을 충족**합니다 — 주 소정근로시간 16시간 30분으로 15시간 이상입니다(근로기준법 제18조제3항).
>
> 다만 주휴수당은 시간 요건 외에 **소정근로일 개근**도 필요한데, 이건 계약서만으로는 확인할 수 없습니다. 실제 지급 여부는 근무 기록에 따라 결정됩니다.
>
> *법률 자문이 아닙니다. 개별 상황은 고용노동부 고객상담센터(1350)에 문의하세요.*

**"받을 수 있습니다"라고 결론 내지 않으면서 도움이 됩니다.** 이게 목표 품질입니다.

### 완료 조건

- [ ] 근거 없는 질문에 `OUT_OF_SCOPE` + 1350 안내
- [ ] 답변에 KB 출처 ID가 없으면 표시하지 않음
- [ ] **의도적으로 틀린 숫자를 답하게 유도해도 차단되는 테스트**가 있음
- [ ] 확정 결론 표현이 차단되는 테스트가 있음
- [ ] 개인정보(계약서 이름·이메일)를 LLM에 보내지 않음

---

# B — 검증 엔진

## B-1 【P1 · 2h】 챗봇 가드 테스트 (A와 협업)

A가 만든 `guards.py`의 **테스트를 B가 작성**하세요.

**이유**: 만든 사람이 자기 안전장치를 테스트하면 통과할 케이스만 씁니다. 판정 로직의 경계값을 가장 잘 아는 사람은 B입니다. AGENTS.md도 *"수정을 직접 수행한 검토자는 그 수정의 유일한 검토자가 될 수 없습니다"* 라고 정하고 있습니다.

**꼭 넣을 케이스**
- 답변에 `10,320원`이 있는데 KB 상수와 다른 경우 → 차단
- 답변에 판정 결과에 없는 시간(`주 20시간`)이 있는 경우 → 차단
- "신고할 수 있습니다" / "부당해고입니다" → 차단
- 출처 ID가 없는 답변 → 차단
- 정상 답변은 통과

## B-2 【P2 · 1h】 판정 로직 자체 재확인

시간이 남으면 확인해 **보고만** 하세요. 코드 수정은 보고 후에.

1. `MINOR_EXT_DAILY_HOURS`/`MINOR_EXT_WEEKLY_HOURS`(연장 1h/5h)가 실제 판정에 반영되는가
2. `minimum_wage_for()`가 표에 없는 연도를 근사할 때, 화면·계약서에 **적용 기준 연도**가 표시되는가
3. 주휴 판정의 "시간 요건까지만"이라는 한계가 `CheckResult.detail`로 사용자에게 전달되는가

⚠️ **대타 근무 시 '소정근로일 개근' 인정 여부는 미확인입니다. 코드에 넣지 마세요.** (AGENTS.md · KB.md 규칙)

---

# C — 백엔드·배포·팀장

## C-1 【P0 · 5분】 main 브랜치 동기화

```bash
cd ~/AI-Builder-Sprint
git checkout main && git merge --ff-only origin/develop && git push origin main
git checkout develop
```

main에 푸시하면 Vercel도 자동 재배포됩니다.

## C-2 【P0 · 30분】 Railway 환경변수 4개 추가 + 배포 확인

로그인이 배포에 반영되려면 필요합니다.

```
KAKAO_REST_API_KEY      (로컬과 같은 값)
KAKAO_CLIENT_SECRET     (로컬과 같은 값)
JWT_SECRET              (로컬과 같은 값)
KAKAO_REDIRECT_URI      https://<vercel-도메인>/auth/kakao/callback   ← 로컬과 다름
```

⚠️ `requirements.txt`에 `PyJWT`를 추가했습니다. Railway가 **재빌드**해야 로그인이 동작합니다.

**확인**: `/health`의 `kakao_login`이 `true`가 되는지.

## C-3 【P0 · 4h】 발표 자료 (총괄)

권장 구성 (본선 7분 발표 순서와 동일하게):

1. **문제** — 벽 3개(모른다 / 말 못 한다 / 증거 없다) + 19.5% · 경상권 23.5% · "몰라서" 40.4%
2. **해결** — ① 알려준다 ② 말하게 한다 ③ 남긴다
3. **차별점** — **판정에 LLM을 쓰지 않는다.** `validation/rules.py` 754줄에 네트워크·LLM import 0건. 268개 테스트가 고정
4. **AI 활용** — Document Parse + Information Extract + 코드 방어층 + **챗봇 3단 검증**
5. **양쪽을 돕는다** — 미작성 1위 이유가 "몰라서". 고발 도구가 아니라 사업주 경로까지
6. **기대 효과 + 확장 방향**

## C-4 【P0 · 결정 즉시】 개인정보 fixture 방침 ← 오늘 결정

`spikes/fixtures/handwritten_01.png`(실제 손글씨 계약서) + `handwritten_01.json`(실명)이 **기본 브랜치**에 있습니다.

`AGENTS.md` 위반:
> 테스트와 데모에는 **가상 계약서와 가상 인물만** 사용합니다.
> `.env`, 자격증명, **실제 계약서, 개인정보**를 커밋하지 않습니다.

**선택지**: (a) 삭제 (b) 가상 인물로 재촬영 (c) 이름만 마스킹
- git 히스토리 잔존을 수용할지도 함께 결정
- **A-1이 이 결정을 기다리고 있습니다**

⚠️ AI 스크리너가 AGENTS.md와 저장소를 대조하면 자기 규칙 위반으로 잡힙니다. "약자 보호"를 내세운 서비스가 개인정보를 부주의하게 다루는 모양이 됩니다.

## C-5 【P1 · 1h】 실발송 anchor 위치 검증

어제 `anchors.py` 실측 보정값이 프로덕션에 **처음** 반영됐습니다. 아직 실발송으로 확인하지 않았습니다.

```bash
python3 spikes/modusign_spike.py send   # TEST_WORKER_EMAIL / TEST_OWNER_EMAIL 확인 후
```

- **왜**: 서명란이 엉뚱한 곳에 놓이면 특별상 핵심 시연이 망가집니다
- 사업주 경로(`employer_first=True`)도 한 번 확인하세요 — 서명 순서만 바꿨지만 실물 확인이 필요합니다
- **완료 조건**: 서명란이 `(근로자 서명)`·`(사업주 서명)` 위에 정확히 놓인 스크린샷

## C-6 【P1 · 0.5h】 웹훅 실동작 로그 확보

서명을 하나 더 하고 Railway → Deploy Logs 확인:

```
모두싸인 webhook: event=document_signed document=xxxxx
문서 xxxxx 동기화: ON_GOING (1/2 서명)
```

두 줄이 뜨면 웹훅이 살아 있는 겁니다. 어제 `remember_document()` 고치기 전에는 `이력에 없는 문서`에서 끊겼습니다.

**특별상 "연동의 견고성 8점" 증빙입니다. 스크린샷 필수.**

## C-7 【P1 · 1~2h】 도메인 연결 (선택이지만 권장)

지금 주소가 68자입니다.
```
ai-builder-sprint-parkganghyun123-sketchs-projects.vercel.app
```

`fairsign.kr` 같은 도메인(1~2만원)을 연결하면:
- 심사자가 **기억할 수 있는 주소**가 됩니다
- 카카오 Redirect URI·발표 슬라이드·구두 안내가 전부 짧아집니다
- "토이 같지 않게"라는 목표에 **비용 대비 효과가 가장 큽니다**

⚠️ 바꾸면 **카카오 Redirect URI · Railway CORS_ORIGINS · Railway KAKAO_REDIRECT_URI** 를 함께 고쳐야 합니다. 할 거면 빨리 하세요 — 나중에 바꾸면 손댈 곳이 늘어납니다.

## C-8 【P1 · 1h】 문서 최종 정리

- `README.md` 기능표를 최종 상태로 (어제 백엔드/화면 분리 표기로 바꿔둠 — 로그인·사업주 경로·보관함 추가 필요)
- `README.md`의 "현재 구현의 한계" 갱신 — **메모리 저장 한계는 이제 사실이 아닙니다**
- `AGENTS.md` 스택 표에 로그인·저장소 추가
- `docs/`의 "심사위원" 언급 중립화 검토 (위험 낮음, 팀장 판단)

---

# 전원 — 제출물

## S-1 【P0 · 3h】 데모 영상 + 백업 2본

**선행**: D-1(로그인 UI) 완료 후 촬영. 로그인 없이 찍으면 서명 단계에서 막힙니다.

**시연 순서** (7분 발표와 같게)
1. 홈 — 역할 선택 (근로자 / 사업주)
2. 사진 업로드 → 20초 대기 → 추출 결과
3. 확인 화면 — **LOW 신뢰도 항목이 노란색**, "이 값이 맞아요" 체크
4. 판정 결과 — `시급 9,500원 < 10,320원`
5. **말 꺼내기 문구 → 복사** ← 이 장면이 핵심입니다
6. 로그인 → 서명 발송 → 메일 → 서명 → 완료
7. (여유 있으면) 사업주 경로 한 번

⚠️ **백업 2본을 로컬에 보관하세요.** 발표 당일 네트워크 장애 대비.

## S-2 【P0 · 1h】 스크린샷 + 테스트 안내 【D】

- 화면 전체 스크린샷
- 테스트 안내: "카카오 로그인 필요. 서명 발송 전까지는 로그인 없이 확인 가능"
- 가상 계약서 샘플 이미지 1장 함께 제공 (`backend/app/evaluation/contracts/contract_01.png` 사용 가능)

---

# 일정

### 7/31 (금)

| | D | C | A | B |
|---|---|---|---|---|
| 오전 | **D-1** 로그인 UI | **C-1** main 동기화 → **C-2** Railway 환경변수 → **C-4** fixture 결정 | **A-1** 손글씨 측정 | **B-2** 판정 재확인 |
| 오후 | **D-1** 마무리 → **D-3** 이름 불일치 | **C-5** 실발송 검증 · **C-7** 도메인 | **A-2** AI 기술서 | **B-1** 준비 |
| 저녁 | **D-2** 보관함 | **C-6** 웹훅 로그 | **A-3** 챗봇 착수 | — |

**성공 기준**: 배포 링크에서 **로그인 → 서명 → 보관함**까지 완주

### 8/1 (토)

| | |
|---|---|
| 오전 | **S-1 데모 영상 촬영** + 백업 2본 · **C-3** 발표 자료 |
| 오후 | **A-3** 챗봇 · **B-1** 가드 테스트 |
| 저녁 | **A-2** 기술서 완성 · **S-2** 스크린샷 |

**이 시점에 제출물 4종이 실재해야 합니다.**

### 8/2 (일)

| | |
|---|---|
| 오전 | 챗봇 마무리 · **C-8** 문서 정리 · D-4 |
| 오후 | **리허설 3회** — 시크릿 창 완주, 발표 타이밍 측정 |
| 저녁 | 발견된 문제 수정. **새 기능 착수 금지** |

### 8/3 (월)

| | |
|---|---|
| 오전 | 최종 점검 (아래 체크리스트) |
| **13:00** | **제출** — 마감 5시간 전. 플랫폼 장애 대비 버퍼 |

---

# 제출 전 최종 체크리스트

**실행 환경**
- [ ] `pytest` 전부 통과
- [ ] `ruff check backend/` 0 errors
- [ ] `npx tsc --noEmit` exit 0
- [ ] `npm run build` exit 0
- [ ] `/health` 6개 신호 — `modusign` `upstage` `store:postgres` `webhook_token` `kakao_login` `cors_origins`
- [ ] `/health/pdf` 브라우저에서 한글 정상 (□□□ 아님)
- [ ] Upstage · 모두싸인 크레딧 잔량
- [ ] `git status` 클린, `.env` 미추적
- [ ] `origin/main` == `origin/develop`

**제출물**
- [ ] 발표 자료
- [ ] 코드 저장소 (README · 실행 방법 · 커밋 내역 · **AI 지침 파일**)
- [ ] 배포 링크
- [ ] 데모 영상 + 백업 2본
- [ ] 스크린샷 + 테스트 안내
- [ ] AI 활용 기술서

**실패 대비**
- [ ] 완주 녹화 2본 로컬 보관
- [ ] Upstage 장애 → **경로 B로 전환** (이미 구현됨. 강점으로 설명 가능)
- [ ] 모두싸인 장애 → 체결 완료 문서 스크린샷 준비
- [ ] Railway 콜드스타트 → 발표 10분 전 `/health` 워밍업
- [ ] 로컬 실행본 노트북에 준비

---

# 발표에서 강조할 것 (근거 있는 것만)

1. **판정에 LLM을 쓰지 않는다** — `validation/rules.py` 754줄, 네트워크·LLM import 0건, 268개 테스트가 고정
2. **AI가 자신 있게 틀리는 경우까지 방어했다** — 실측 `박강현→박강헌`(HIGH)에서 `ALWAYS_CONFIRM` 관문 도출
3. **코드가 판단할 수 없는 건 사용자에게 되돌린다** — 이름 불일치를 조용히 덮어쓰지 않음
4. **확인 못 한 법령은 코드에 넣지 않았다** — 대타 근무 개근, 2026-12-10 시행 예정 휴게시간 개정
5. **챗봇 답변을 코드가 3단으로 검사한다** — 출처·숫자·확정결론
6. **양쪽을 돕는다** — 미작성 1위 이유가 "몰라서"이므로 고발 도구를 만들지 않음. 사업주 경로가 그 증거
7. **회원가입을 늦췄다** — 열여섯 살이 가입부터 해야 한다면 벽을 넘지 못한다. 로그인은 서명 단계에서 처음 요구

---

# 잘라낸 것 — 8/1까지 공식 포기 선언

> ⚠️ 애매하게 남겨두면 팀원이 "혹시 해야 하나" 고민하며 시간을 씁니다.

| 기능 | 판단 |
|---|---|
| Solar LLM 기반 말 꺼내기 문구 | 포기. 템플릿이 이미 `numbers_verified: true`로 동작. 얹으면 리스크만 |
| 사업주 전용 대시보드 | 포기. 사업주 경로는 발송까지만 |
| 근무기록 대조 · 다국어 | 포기 |
| 지역 협력기관 접촉 | 팀장 판단. 본선 가점 +5이나 3일 내 성사 가능성 판단 필요 |
