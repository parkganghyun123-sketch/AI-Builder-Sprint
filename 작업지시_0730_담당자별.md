# 담당자별 작업 지시 — 2026-07-30 (마감까지 4일)

> 근거: [분석보고서_2026-07-30.md](분석보고서_2026-07-30.md)
> 기준 커밋: `develop` @ `e43bc29`
> 원칙: **오늘 안에 "배포 링크로 사진→서명 완주"가 되어야 한다.** 그 외는 전부 부차적이다.

---

## 팀장이 지금 알아야 할 세 줄

1. 백엔드는 실제로 완주한다(실측 200 확인). **막고 있는 건 프론트 한 곳이다** — `confirmed_fields`를 안 보내서 서명 발송이 100% 실패한다.
2. 배포 백엔드의 `CORS_ORIGINS`가 비어 있다(`/health` 실측). 프론트를 올리는 순간 브라우저가 전부 차단한다.
3. 가장 큰 차별점(말 꺼내기 문구)이 백엔드에만 있고 화면에 없다. 1~2시간이면 붙고, 점수 효과는 이 프로젝트에서 가장 크다.

---

# D — 프론트엔드

## D-1 【P0 · Critical · 3~4h】 확인 관문 UI 연결 — 서명 발송 복구

**지금 무슨 일이 벌어지고 있나**

`/contracts/analyze-sign`을 프론트와 **똑같은 body**로 호출해봤다. 결과:

```json
409 {"detail":{"code":"UNCONFIRMED_FIELDS",
      "message":"확인이 필요한 항목이 남아 있습니다.",
      "fields":["사업체명","대표자","임금 금액","임금 형태","근로자 성명"],
      "hint":"... confirmed_fields 에 담아 다시 요청하세요."}}
```

`confirmed_fields`에 5개를 넣고 재호출하면 **200**이 나오고 PDF까지 생성된다(실측). 즉 백엔드는 준비돼 있고 **프론트만 안 보내고 있다.**

백엔드 규칙(`backend/app/review/fields.py`): `ALWAYS_CONFIRM = {worker_name, employer_name, employer_business_name, wage_amount, wage_type}` 5개는 **신뢰도와 무관하게 항상** 확인이 필요하다. AI가 실제로 `박강현`을 `박강헌`으로 confidence HIGH로 틀린 사례가 있어서 만든 관문이다.

**2차 문제**: `web/lib/schemas.ts violationBlockedEnvelopeSchema`가 `problems` 배열을 필수로 요구하는데, 위 409 응답에는 그 키가 없다. `safeParse`가 실패해서 `ViolationBlockedError`가 아니라 일반 `responseError`로 떨어지고, 사용자는 `"/contracts/analyze-sign 요청을 완료하지 못했습니다."`만 본다. **복구 경로가 없다.** `proceed_with_violations: true`로도 못 넘는다(확인 관문이 1단계로 먼저 실행됨).

**해야 할 일**

1. `lib/api.ts`에 `fetchReviewItems(terms)` 추가 → `POST /contracts/review-items`
   - 응답: `{ items: [...], must_confirm: string[] }`
   - `items[]` 각 항목: `field`, `label`, `value`, `confidence`, `source_text`, `priority`, `reasons[]`, `affects_judgment`, `printed_on_contract`
2. `lib/types.ts`의 `AnalyzeSignRequest`에 `confirmed_fields: string[]` 추가
3. `lib/schemas.ts`에 409 응답 3종을 **`detail.code`로 분기**하는 스키마 추가
   - `UNCONFIRMED_FIELDS` → `{code, message, fields[], hint}`
   - `NAME_MISMATCH` → `{code, message, conflicts:[{field,label,on_contract,typed}], hint}`
   - 위반 차단(코드 없음) → `{message, problems[], problem_details[], hint}` ← 기존 스키마
4. 확인 UI: `review` 또는 `sign` 화면에 `must_confirm` 항목별 체크박스 + `reasons` 표시. 체크한 `field` 이름을 `confirmed_fields`로 전송
5. `NAME_MISMATCH` 처리: `on_contract`(계약서에서 읽은 값)와 `typed`(입력값)를 나란히 보여주고 사용자가 고르게 한다. **한쪽으로 자동 덮어쓰지 말 것** — 백엔드가 일부러 고르지 않고 되돌려준다

**완료 조건**
- [ ] 브라우저에서 `upload → review → result → contract → sign → complete` 완주, `/complete`에 `document_id` 표시
- [ ] 확인 안 한 항목이 있으면 **어떤 항목인지 화면에 표시되고 그 자리에서 확인 가능**
- [ ] 이름 불일치 시 두 값이 나란히 보이고 선택 가능

**제출할 결과물**: 완주 스크린샷(6화면) + 409 안내 화면 스크린샷

**⚠️ 하지 말 것**: 백엔드 검증 규칙을 프론트에 복사하지 말 것. `review-items`가 판단하고 프론트는 표시만 한다.

---

## D-2 【P0 · Critical · 0.5~2h】 프로덕션 빌드 검증

`npm run build`를 완주시키지 못했다(실행 환경 타임아웃). `.next/`에 `BUILD_ID`가 없고 `archive`·`complete` 라우트 산출물이 없어 기존 `.next`는 `next dev` 산출물로 보인다.

```bash
cd web && npm run build && npm start
```

**완료 조건**: exit 0, 8개 라우트 전부 산출, `npm start`로 8화면 접속 성공
**예상 문제**: `useSearchParams`를 쓰는 페이지의 Suspense 경계
**제출할 결과물**: 빌드 로그 마지막 30줄

---

## D-3 【P1 · High · 1~2h】 "말 꺼내기 문구" 화면 노출 — 점수 효과 최대

**백엔드는 이미 동작한다** (실측):

```
POST /contracts/message → 200
{"message":"사장님, 안녕하세요. 계약서 다시 보다가 여쭤보고 싶은 게 있어서요.\n\n
계약서에 적힌 시급이 9,500원인데, 올해 최저임금이 10,320원이라고 해서요.
혹시 제가 잘못 본 걸까요?\n\n제가 잘못 본 것일 수도 있어서 확인 한 번 부탁드려요. 감사합니다.",
 "lines":["..."], "numbers_verified":true}
```

`web/` 전체 grep 결과 이 엔드포인트 호출이 **0건**이다. README가 핵심 서사 ②로 내세운 기능이고, 대회 주제 "AI로 인간다움 표현"에 가장 정확히 맞는 기능인데 심사자에게 안 보인다.

**해야 할 일**
1. `lib/api.ts`에 `buildOwnerMessage(terms)` 추가 → `POST /contracts/message`
2. `app/result/page.tsx`에 문구 카드 + **클립보드 복사 버튼**
3. `message`가 `null`이면 표시하지 않음(문제 없는 계약)
4. **`numbers_verified: false`면 문구를 표시하지 말 것** — 숫자 대조 실패 신호다

**완료 조건**: 시급 9,500원으로 검증 → 결과 화면에 문구 표시 → 복사 → 메모장 붙여넣기 성공
**제출할 결과물**: 문구 카드 스크린샷 + 복사 동작 확인

---

## D-4 【P2 · Medium · 2~3h】 `/contracts/validation-state` 연결

백엔드 `contracts.py:93` docstring: *"프론트엔드는 **이 응답만 보고** 다음 단계 버튼을 켜고 끈다. 같은 규칙을 화면에 복사하지 말 것 — 두 곳에 두면 반드시 어긋난다."*

커밋 `8ca792d` 제목이 "프론트·백엔드 공통 기준"인데 프론트 호출이 0건이다. 응답 형태(실측):

```json
{"can_proceed":true, "blocking_fields":[],
 "counts":{"error":0,"warning":1,"info":0},
 "issues":[{"field":"minimum_wage","label":"최저임금","severity":"warning",
            "value":"시급 9,500원 < 2026년 최저임금 10,320원",
            "reason":"...","fix":"...","blocks":false,"step":"result","source":"legal"}]}
```

`issues[].field`로 포커스를 옮기고, `can_proceed`로 다음 버튼을 제어하면 된다. **Day 3로 밀어도 되는 작업이다.**

---

## D-5 【P2 · 1h】 업로드 로딩 UX

Upstage 응답에 **20초쯤 걸린다**. 진행 표시가 없으면 사용자가 멈춘 줄 안다.

---

# A — AI 파이프라인

## A-1 【P1 · High · 1h + API 비용 소량】 손글씨 반복 측정 실행 + 산출물 커밋

`spikes/check_extract.py`는 완성돼 있는데 **산출물 `*_runs.json`이 저장소에 없다.** 현재 AI 증빙은 합성 벤치마크(`results.json` 230/230, 100%)뿐이다.

합성 벤치마크는 `evaluate.py` docstring이 스스로 인정한 대로 "우리가 그린 대로 정확히 읽는가"만 잰다. **심사에서 "100%"보다 "실사진 3회 모두 정확, 흔들림 0"이 훨씬 강한 근거다.**

```bash
cd ~/AI-Builder-Sprint
set -a; source .env; set +a
python3 spikes/check_extract.py spikes/fixtures --runs 3
```

**완료 조건**
- [ ] `spikes/fixtures/*_runs.json` 커밋
- [ ] "N회 중 정확 M건 / 호출 간 값 흔들림 K건" 요약을 AI 활용 기술서에 기재
- [ ] 정확도가 낮게 나와도 **숨기지 말 것.** 사용자 확인 단계가 있어서 OCR 정확도가 급소가 아니라는 게 이 제품의 논리다(README.md:97-101). 낮은 수치도 그 논리의 근거가 된다

**선행 확인 필요**: `spikes/fixtures/handwritten_01.png`(실제 계약서) + 라벨의 실명 `박강현` 처리 방침이 팀장 결정 대기 중이다(아래 팀장 결정 항목). 결정 후 실행할 것.

**제출할 결과물**: `*_runs.json` + 요약 수치 3줄

## A-2 【P2 · 30분】 ruff F811 해소 — 테스트 신뢰성

```
backend/app/ai/tests/test_extract.py:361:5  F811  Redefinition of unused `_normalize_extracted_value` from line 20
```

361행에서 20행의 import를 재정의하고 있다. **상단 import가 죽어서 테스트가 의도한 대상을 검증하지 않을 수 있다.** 다른 5건은 `ruff check backend/ --fix`로 자동 해결된다.

**완료 조건**: `python3 -m ruff check backend/` → 0 errors, `pytest` 208 passed 유지

## A-3 【P1 · 1~2h】 AI 활용 기술서 초안 (C와 협업)

OT 제출 요건: **모델 · API 사용 위치 · 프롬프트/설정 · 테스트·검증 산출물**

기재할 근거 위치:
| 항목 | 위치 |
|---|---|
| 모델 | Upstage `document-parse`, `information-extract` |
| API 사용 위치 | `backend/app/ai/document_parse.py:31`, `backend/app/ai/extract.py:452` |
| 설정/스키마 | `extract.py:88 build_extraction_schema()`, `confidence: True` |
| 신뢰도 처리 | `extract.py:249 CONFIDENCE_THRESHOLD = 0.90`, `:252 _confidence_from_upstage` |
| 코드 방어층 | `:313 apply_sanity_check`, `:200 _looks_like_form_label`, `:176 _CHECKMARK` |
| 근거 문장 매칭 | `:411 _find_source_text` (Document Parse 원문에서 근거 줄 탐색) |
| 검증 산출물 | `app/evaluation/results.json` + `spikes/fixtures/*_runs.json` |
| **AI를 쓰지 않은 곳** | `backend/app/validation/` — 판정·계산 전부. **이게 차별점이므로 반드시 기술서에 명시** |

## A-4 【P3 · 포기 권장】 챗봇 · Solar LLM 문구 생성

- 챗봇: README.md:524가 "숫자 검증 없는 챗봇은 배포하지 않는다"고 못 박았다. 4일 내 안전장치까지 구현은 불가. **포기 선언을 발표 강점으로 전환**하는 편이 낫다
- Solar LLM 문구: 템플릿 버전이 이미 `numbers_verified: true`로 동작한다. 얹으면 리스크만 늘어난다

---

# B — 검증 엔진

> **B의 코드는 현재 가장 건강하다.** `validation/rules.py` 754줄에 LLM·네트워크 import가 0건이고 테스트 75건이 통과한다. 이 상태를 지키는 것이 B의 1차 임무다.

## B-1 【P1 · High · 2h】 `analyze-sign` 통합 테스트 추가

**왜 B가 하는가**: D-1의 결함(`confirmed_fields` 누락)이 **208개 테스트를 전부 통과한 채로 존재했다.** 라우터 계층 테스트가 0건이기 때문이다. 판정 로직 테스트를 가장 잘 쓰는 사람이 이 안전망을 만들어야 한다.

신규 파일 `backend/app/routers/tests/test_analyze_sign.py`, 4경로:

| 경로 | 조건 | 기대 |
|---|---|---|
| 1 | `confirmed_fields` 없음 | 409, `detail.code == "UNCONFIRMED_FIELDS"`, `fields` 5개 |
| 2 | `confirmed_fields` 완비 + 계약서 이름 ≠ 입력 이름 | 409, `detail.code == "NAME_MISMATCH"`, `conflicts` |
| 3 | 시급 0원 | 422, `detail.code == "INVALID_CONTRACT_VALUES"` |
| 4 | 전부 정상 + `proceed_with_violations=true` | 200, `document_id` 존재 |

모두싸인은 monkeypatch로 대체할 것 (**실제 발송·유료 호출 금지**):
```python
async def fake(**kw): return {"id": "TEST-ID", "status": "ON_GOING"}
monkeypatch.setattr("app.routers.contracts.modusign.request_signature", fake)
```

**완료 조건**: 4경로 통과. 프론트가 `confirmed_fields`를 빼면 CI가 실패로 알려준다
**제출할 결과물**: `pytest -v -k analyze_sign` 출력

## B-2 【P2 · 30분】 `FIELD_LABELS` 중복 제거

동일 딕셔너리(23개 필드 라벨)가 두 곳에 있다:
- `backend/app/review/fields.py:73`
- `backend/app/validation/severity.py:60`

한쪽으로 통합하거나 공용 모듈로 뺀다. **오프셋 상수 이중화(§10-H2)가 실제 버그를 만든 전례**가 있으니 같은 패턴은 미리 정리한다.

**완료 조건**: 정의 1곳, `pytest` 208 passed 유지

## B-3 【검토 요청 · 30분】 판정 로직 자체 재확인

시간이 남으면 아래를 확인해 팀장에게 보고할 것. **코드 수정은 보고 후에.**

1. `MINOR_EXT_DAILY_HOURS`/`MINOR_EXT_WEEKLY_HOURS`(연장근로 1h/5h)가 판정에 실제로 반영되는가 — `check_minor_working_hours`에서 사용 여부
2. `minimum_wage_for()`의 "가장 가까운 연도로 대체" 로직이 화면·계약서에 **적용 기준 연도를 표시**하는지 (`CheckResult.standard_year`가 실제로 그 연도를 담는가)
3. 주휴 판정이 "시간 요건까지만"이라는 한계가 `CheckResult.detail`로 사용자에게 전달되는가

**⚠️ 대타 근무 시 '소정근로일 개근' 인정 여부는 미확인 상태다. 코드에 반영 금지** (AGENTS.md·KB.md 규칙).

---

# C — PDF·서명·배포·DB (팀장 겸임)

> ⚠️ **C가 4일간 배포·제출물·백엔드 수정을 모두 감당할 수 있는지 판단이 필요하다.** 아래 C-1~C-3은 위임이 어렵지만, C-5(문서 일치화)는 A 또는 B에 넘길 수 있다.

## C-1 【P0 · Critical · 1h】 Railway CORS_ORIGINS 설정 + Vercel 배포

실측 결과:
```
GET https://ai-builder-sprint-production.up.railway.app/health
{"status":"ok","commit":"e43bc29","modusign":true,"upstage":true,
 "cors_origins":["http://localhost:3000","http://127.0.0.1:3000"]}
```

`CORS_ORIGINS` 환경변수가 Railway에 설정돼 있지 않다. `config.py:39` 주석이 이미 경고한 그 상황이다. **프론트를 Vercel에 올리는 순간 브라우저가 preflight부터 전부 차단한다** → "링크는 있는데 아무것도 안 되는" 상태 = 적격성 Fail.

1. Vercel에 `web/` 배포, `NEXT_PUBLIC_API_BASE_URL=<railway-url>` 설정
2. Railway에 `CORS_ORIGINS=<vercel-domain>` 설정 (쉼표로 여러 개 가능)
3. 로컬 `.env`에도 `CORS_ORIGINS` 항목 추가 (현재 없음)

**완료 조건**: 재배포 후 `/health`의 `cors_origins`에 Vercel 도메인 포함 + 시크릿 창에서 파일 업로드 성공
**선행**: D-2 (`npm run build` 성공)

## C-2 【P0 · Critical · 1h】 업로드 크기·MIME 검증 + 이메일 검증

`routers/extract.py:39`이 `await file.read()`로 무제한 읽는다. 크기 제한·MIME 검증·인증·레이트리밋이 전무하고, **이 요청 하나가 Upstage를 2회 호출한다**(Document Parse + Information Extract).

배포 링크가 공개되는 순간 누구나 크레딧을 태울 수 있다. **CORS는 브라우저만 막고 curl은 못 막는다. 데모 당일 크레딧 소진이 현실적 위험이다.**

추가로 `SignRequestBody.worker_email: str`이 `EmailStr`이 아니라, API 직접 호출로 임의 주소에 모두싸인 서명 요청을 보낼 수 있다.

1. `routers/extract.py`: `MAX_UPLOAD_BYTES = 10 * 1024 * 1024`, 초과 시 413. `file.content_type`을 `{image/jpeg, image/png, application/pdf}`로 화이트리스트, 위반 시 415
2. `schemas.py`·`routers/sign.py`·`routers/contracts.py`: `worker_email`/`employer_email`을 `EmailStr`로 (`pydantic[email]` 필요 여부 확인)

**완료 조건**: 초과 파일 413, 잘못된 MIME 415, 잘못된 이메일 422. 회귀 테스트 3건
**제출할 결과물**: `pytest` 출력

## C-3 【P1 · High · 2h】 anchor 오프셋 단일 출처화 + 실발송 1회 검증

**문제**: `signing/anchors.py:69-70`이 실측 보정값을 "단일 출처"로 선언했는데(`anchors.py:4-11`이 과거 불일치 버그를 기록), `signing/modusign.py:31-32`가 **자체 상수를 정의하고 사용한다.** `modusign.py`는 `anchors`를 import하지 않는다(grep 확인 — anchors import는 `spikes/` 2개뿐).

| | anchors.py (실측 보정) | modusign.py (실사용) |
|---|---|---|
| `SIGN_OFFSET_X` | `-0.010` | `0.028` ← **부호까지 다름** |
| `SIGN_OFFSET_Y` | `-0.246` | `-0.25` |

`anchors.py`가 문서화한 "모두싸인이 세로로 22.5% 밀어놓는다"는 실측 보정이 프로덕션 경로에 없다. **`anchors.py`는 현재 dead code다.**

1. `modusign.py`에서 `SIGN_OFFSET_X/Y`, `ANCHOR_EMPLOYER/WORKER`, 박스 크기를 삭제하고 `from app.signing.anchors import ...`로 교체
2. 가상 인물로 1회 실발송해 서명란 위치 확인
```bash
python3 spikes/modusign_spike.py send   # TEST_WORKER_EMAIL / TEST_OWNER_EMAIL 확인 후
```

**완료 조건**: `grep -rn SIGN_OFFSET backend/app/` 결과가 `anchors.py` 1곳. 실발송 문서에서 서명란이 `(근로자 서명)`/`(사업주 서명)` 위에 정확히 놓임
**제출할 결과물**: grep 출력 + 서명란 위치 스크린샷

**참고**: PDF anchor 텍스트 자체는 검증 완료했다. `pdfminer`로 추출하니 `(근로자 서명)`·`(사업주 서명)` 둘 다 존재하고 한글도 정상이었다. **문제는 오프셋뿐이다.**

## C-4 【P1 · High · 30분】 `modusign.py` httpx 예외 포착

`request_signature`(`:88`)·`get_document`(`:120`)가 `httpx.HTTPError`를 감싸지 않는다. 호출부(`contracts.py:467`, `sign.py:78`)는 `ModusignError`만 잡으므로 **네트워크 타임아웃 시 502가 아닌 500**이 나가고, 프론트는 "서버에서 요청을 처리하지 못했습니다"(재시도 가능)로 오분류한다.

추가로 `ModusignError(f"HTTP {res.status_code}: {res.text}")`가 응답 본문을 예외 메시지에 담는다. 현재는 `type(e).__name__`만 로깅하므로 실유출은 없지만, 상위에서 실수로 로깅하면 계약 정보가 샌다. **`res.text`를 메시지에서 제거할 것.**

모두싸인 특별상 "연동의 견고성(8점)"에 직접 영향한다.

**완료 조건**: BASE_URL을 잘못된 값으로 monkeypatch해 502가 나오는 테스트 통과

## C-5 【P1 · High · 1~2h · A/B 위임 가능】 문서-코드 일치화

AI 스크리너가 **팀당 독립 3회 실행 후 평균**을 낸다(OT p.11). 편차가 크면 심사위원 정밀 재검토 대상이 된다. 문서가 서로 다른 상태를 주장하면 편차가 커진다.

| 파일·위치 | 현재 | 고칠 내용 |
|---|---|---|
| `AGENTS.md:22` | "프론트엔드 … `web/` **(미착수)**" | 3,168줄 구현됨. 현황 반영 |
| `README.md:605` | "PDF 템플릿이 **macOS 시스템 폰트**에 의존" | `Dockerfile`에 `fonts-noto-cjk` + `fc-cache -fv` 있음. 로컬 Linux 렌더 정상 확인. **stale 기술 삭제** |
| 인수인계 문서 §2 | "AI 연동 — … **Solar LLM**" | grep 결과 호출부 0건. 제거 |
| 인수인계 문서 §5-9 | "경로 B 직접 입력 폼 📋 **미착수**" | `review/page.tsx?path=B` + `createEmptyTerms()`로 **구현됨**. 홈 `page.tsx:90`에 링크 있음 |
| 인수인계 문서 §5-3,4,5 | "✅ 구현" | 백엔드는 구현, **UI 미연결**을 구분 표기 |
| `README.md` 기능표 | — | 분석보고서 §5 표를 기준으로 재작성 (상태 6단계 구분) |

**완료 조건**: 문서의 모든 ✅ 주장에 대응 파일·함수가 실재하고, **UI 연결 여부까지 구분 표기**됨

## C-6 【P0 · Critical · 8~10h 분산】 제출물 4종 총괄

OT 적격성 "제출물 완비" — 미충족 시 **채점 없이 심사 제외**. 현재 `README.md:583-587` 체크리스트 5개 전부 미체크.

- [ ] **발표 자료** — 문제 정의 · 사용자 · 핵심 기능 · AI 활용 방식 · 기대 효과
- [ ] **데모 영상** — 가상 인물로 실제 서명 완주 녹화. **실패 대비 백업 2본**
- [ ] **스크린샷** 8화면 + 테스트 계정 안내(로그인 없음 → "계정 불필요, 가상 계약서 사진 첨부")
- [ ] **AI 활용 기술서** (A-3과 병행)
- [ ] **배포 링크** (C-1)

**선행**: D-1, C-1 (동작하는 화면이 있어야 촬영 가능)

## C-7 【P2 · 30분】 웹훅 키 이름 통일

`config.py:20`은 `WEBHOOK_PATH_TOKEN`을 읽는데 `.env`에는 `MODUSIGN_WEBHOOK_SECRET`만 있다. 값이 비면 `_check_token`(`sign.py:225`)이 검증을 건너뛴다 → **토큰 검증 사실상 비활성.**

상태는 `reconcile()`이 모두싸인 API에서 읽으므로 **위조로 상태 조작은 불가**하다(설계가 잘 돼 있다). 다만 엔드포인트 남용(API 쿼터 소모)은 열려 있다. `.env`의 키 이름을 `WEBHOOK_PATH_TOKEN`으로 바꾸고 Railway에도 설정하면 끝난다.

## C-8 【P2 · 15분~4h】 미사용 의존성 정리

`requirements.txt`에 `sqlalchemy==2.0.36`, `psycopg[binary]==3.2.3`이 있는데 코드 사용이 0건이다. **DB를 안 붙일 거면 제거**하는 편이 "선언과 사용이 일치한다"는 신호를 준다.

## C-9 【P3 · 포기 권장】 Supabase DB 연결 · 보관함

- DB: `sign.py:25 _store: dict` — 재배포 시 이력 소실. **한계를 정직히 밝히는 편이 반쯤 붙은 DB보다 안전하다**
- 보관함: `archive/page.tsx`의 현재 문구("목 목록을 보관 기능처럼 표시하지 않습니다")가 **오히려 신뢰를 준다.** 그대로 유지 권장

---

# 팀장 결정 필요 항목 (임의 배정하지 않음)

| # | 항목 | 왜 결정이 필요한가 | 기한 |
|---|---|---|---|
| 1 | **모두싸인 특별상 Task 부합성** | OT는 Task 1(부산 관광 상생 계약 비서) / Task 2(마음을 남기는 서명)를 제시한다. FairSign은 근로계약이라 **어느 쪽도 아니다.** 두 Task 중 선택이 필수인지, "전자서명·CLM을 계약 경험에 실제 구현"이면 되는지 OT 자료만으로 확정 불가. **운영진 문의 필요.** 필수라면 특별상 전략을 재검토해야 한다 | **오늘** |
| 2 | **`spikes/fixtures/handwritten_01.png` + 실명 처리** | 실제 촬영 손글씨 계약서(2.7MB)와 실명 `박강현`이 공개 레포에 커밋됨(커밋 `e289b66`). `AGENTS.md`의 "가상 인물만 사용", "실제 계약서·개인정보 커밋 금지" **정면 위반**. `.gitignore`가 `spikes/*.png`를 무시하는데도 추적 중. 선택: (a)삭제 (b)가상 인물 재촬영 (c)이름만 마스킹. **git 히스토리 잔존 수용 여부도 결정 필요** | **오늘** (A-1 선행) |
| 3 | **C 담당자 업무 재배분** | C(팀장)가 배포·제출물·백엔드 수정을 4일간 모두 감당 가능한가? C-5(문서 일치화)는 A/B 위임 가능 | **오늘** |
| 4 | **프론트 배포 대상** | Vercel 확정? 계정·도메인 준비됐는가? | **오늘** |
| 5 | **API 크레딧 잔량** | Upstage / 모두싸인 각각 얼마 남았는가? A-1 실측 + C-3 실발송 + 데모 리허설 + 본선까지 감당 가능한가? | **오늘** |
| 6 | **서비스명** | "페어사인" 확정? README는 "가안"이라 표기 | 8/1 |
| 7 | **본선 발표자** | 7분 발표 + 5분 Q&A 담당. 취약 질문 대비 필요: "판정에 LLM을 안 쓴다면서 AI 문구 생성은 왜 화면에 없나?", "메모리 저장이면 재시작 시 계약 이력은?" | 8/2 |
| 8 | **지역 협력기관 접촉** | 본선 지역사회 가점 +5. README.md:648이 "현재 확정된 협력 기관 없음". 4일 내 성사 가능성 판단 | 8/1 |
| 9 | **`docs/` "심사위원" 표현 중립화** | `docs/2일_작업계획.md:255,268`, `docs/팀_공유_0729.md:31`, `docs/D_작업요청.md:4`. 내부 전략 메모이며 지시문이 아니라 안전하다고 판단하나, AI 스크리너가 문맥 없이 읽을 위험을 0으로 만들려면 중립화 검토 가치가 있다 | 8/2 |
| 10 | **챗봇·DB·보관함 공식 포기 선언** | 팀원이 "혹시 해야 하나" 고민하며 시간을 쓰지 않도록 **명시적으로 포기를 선언할 것.** 애매하게 남겨두는 것이 가장 비싸다 | **8/1까지** |

---

# 진행 관리 — 팀장 체크포인트

## Day 1 (7/31) 저녁 필수 확인

| 확인 항목 | 담당 | 확인 방법 |
|---|---|---|
| 배포 링크로 서명 발송 완주 | D + C | 시크릿 창에서 직접 완주. **스크린샷 요구** |
| `/health`의 `cors_origins`에 Vercel 도메인 | C | 브라우저로 `/health` 열기 |
| `npm run build` exit 0 | D | 빌드 로그 마지막 30줄 요구 |
| 실발송 서명란 위치 | C | 스크린샷 요구 |
| 손글씨 측정 산출물 | A | `git log --oneline -- spikes/fixtures/` |
| `pytest` 208 + `ruff` 0 errors | B | 출력 요구 |

**Day 1에 서명 완주가 안 되면 Day 2를 전부 여기 쓴다.** 다른 작업을 시작하지 말 것.

## Day 2 (8/1) 저녁 필수 확인

- [ ] 배포 링크에서 ①알려준다 ②말하게한다 ③남긴다가 **전부 화면에 보인다**
- [ ] 8화면 스크린샷 확보
- [ ] `AGENTS.md`·`README.md`에 남은 stale 주장 0건
- [ ] 챗봇·DB·보관함 **공식 포기 선언 완료**

## Day 3 (8/2) 저녁 필수 확인

- [ ] 제출물 4종 실재 + 팀 외부인이 링크만으로 이해 가능
- [ ] 데모 영상 백업 2본 로컬 보관

## Day 4 (8/3) — **13:00 제출** (마감 5시간 전, 플랫폼 장애 대비 버퍼)

---

## 작업 규칙 (변동 없음)

- `main` 직접 푸시 금지, PR로 머지. PR·이슈·커밋은 **포크에서만**
- 같은 파일을 두 사람이 동시에 수정하지 않는다 — 이번 배정에 파일 충돌은 없다
- `.env`, 자격증명, 실제 계약서, 개인정보 커밋 금지
- README·주석에 심사자를 향한 문구 금지 (**실격 사유**)
- 판정 로직에 LLM 사용 금지 — **프로젝트 차별점 전부**
- 실행하지 않은 검사를 통과했다고 보고하지 않는다
- 문서와 코드가 다르면 **코드가 맞다.** 임의 구현 대신 팀장에게 먼저 보고
- 확인 못 한 법령(대타 근무 개근 인정 등)은 코드에 넣지 않는다
